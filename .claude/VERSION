kernel-bundle: portable (in-repo)
version: 2026-07-28.2
source: claude-kernel-bundle (canonical)
notes: |
  これは canonical bundle を本リポジトリへ vendoring したコピー。ドリフト検知用。
  内容を更新するときは canonical 側を直し、本ファイルの version を上げて再配布する。
changes: |
  2026-07-28: canonical 2026-07-28.2 版へ再同期(INSTALL-PORTABLE.md 追補 v4–v7)。
    取り込み(canonical 正本のまま上書き):
    - rules/00-kernel.md: §0 三位一体の表現(テキスト/図/式)、§1.2 gsn-meta-framework
      へのエスカレーション、§3 に三位一体の行、§4 に第三チャネル「結晶化した原則」と
      昇格順(auto memory → 原則 → canonical)。2026-07-25 の本リポジトリ先行改訂
      (§1.4 累積器の実在・基数・§4 名指し委譲)は canonical v6 が backport 済みで
      往復一致 — **本ファイルによる 00-kernel の差分表示はこれで解消**。
    - rules/10-principles.md: 系譜 header(正本ポインタ + UI 節のスコープ注記)へ差替。
      本文 1–31 は往復一致(canonical v7 が本リポジトリ版を backport)。
      「§このリポジトリでの写像」は規約どおりローカル保持(原産である旨を明記)。
    - kernel-log.md / KERNEL-INSTALL-PORTABLE.md / skills/animation-fx(v3.2 Step 1.5
      モーション・アイデンティティ)。
    合流(canonical の追加分のみ取り込み、ローカル拡張は保持):
    - skills/adr/SKILL.md: canonical の §表現(三位一体)を追加。ローカルの §GSN 併設
      (ADR-094 由来・無条件化)は保持。核 §3 の GSN 行は canonical で §1.2
      エスカレーションへ統合されたため、参照先を更新(docs/NAVIGATION.md も同じ行)。
    - templates/CLAUDE.project-template.md: canonical の §モーション・アイデンティティを
      追加。ローカルの台帳基数列・名指し委譲コメントは保持(canonical 未 backport)。
    非取り込み(意図的な差分。canonical は基底、本リポジトリは上位集合):
    - settings.json: PreToolUse に commit-trailers / state-ledger-nudge、PostToolUse に
      commit-trailers を追加配線したまま(canonical は evidence hook のみ)。
    - hooks/evidence-before-commit.sh: staged .gsn lint 節を保持。
    - commands/(adr-validate, gsn-maintain, qc, sqa, ux, validate-all, whiteboard)、
      skills/gsn-meta-framework、hooks/commit-trailers.sh、hooks/state-ledger-nudge.sh、
      .claude/DEVELOPMENT.md は本リポジトリ専用。

  2026-07-25: 00-kernel.md §1.4/§3/§4 を本リポジトリ側で先行改訂(kernel-log
    2026-07-25 の c シグナル)。台帳の置き場所を「project CLAUDE.md が名指しする」
    へ委譲し、§4 に「大きい表・図は名指しした別ファイルに置いてよい」を明記。
    併せて「累積器が実在しなければ規律は発動しない」の明文化、基数(0·1·N)の
    追記対象化、図の置き場所を `docs/state-<entity>.mermaid` 固定からプロジェクト
    定義へ緩和(核にインスタンスを置かない — §4 末尾の自己適用)。
    本文はプロジェクト不変の語彙のみなので canonical bundle へそのまま
    バックポート可能(それまで本ファイルが差分の印)。

  2026-07-19: canonical 2026-07-18 版へ再同期。00-kernel.md(§1.4 状態台帳・
    §2 一拍俯瞰・§6 自己適用)、CLAUDE.project-template.md(状態台帳節)、
    kernel-log.md を取り込み。hooks/evidence-before-commit.sh は本リポジトリ
    ローカル拡張(.gsn lint 節)を保持したまま(canonical との差分は意図的)。
    animation-fx skill も 2026-07-19 版へ更新(principles.md 抽象レイヤー +
    examples/ 3本 + game-vfx / dataviz-appshell / evolution / changelog)。

  2026-07-19: rules/10-principles.md を追加 — docs/PHILOSOPHY.md(正本)の
    蒸留ダイジェストを kernel レーンへ統合(常時 load)。本リポジトリ側で先行
    追加したローカル変更 — 本文はプロジェクト不変の語彙で記述済みのため、
    「§このリポジトリでの写像」節を除いてそのまま canonical bundle へ
    バックポート可能(それまで本ファイルが差分の印)。

  2026-07-11: gsn-meta-framework skill を統合(3モード + gsn_tool.py)。
    animation-fx skill も併せて導入(2D/3D 演出設計)。
    /gsn-maintain コマンドで .gsn 論証木の鮮度メンテ、
    hook に staged .gsn の lint ブロックを追加(fail-open)。

  配線(INSTALL-PORTABLE.md の既定どおり):
    - 核は `.claude/rules/00-kernel.md`。launch 時にファイル名順で自動 load される
      (paths 無し = 常時 load。docs: rules は `.claude/CLAUDE.md` と同優先度)。
      ルート `CLAUDE.md`(プロジェクト・インスタンス)は無改変。
    - 核を二重 load しないこと(INSTALL-PORTABLE.md §6)。グローバル版
      (`~/.claude/CLAUDE.md`)や `@import` 配線と併用しない。
    - テストコマンドの単一の源は `.claude/test-command`(= `pnpm test:context`)。
  複数リポジトリ間の同期は INSTALL-PORTABLE.md「§1.1 との折り合い」を参照。
