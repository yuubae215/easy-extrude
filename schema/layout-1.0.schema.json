{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://easy-extrude.dev/schema/layout-1.0.json",
  "title": "LayoutDSL",
  "description": "Public schema for Layout DSL v1.0 (ADR-045, ADR-055). This is the SHAPE contract: it fixes the closed set of fields and the enum vocabularies. Cross-reference resolution, ref uniqueness, and other MEANING checks stay in LayoutValidator.js (ADR-064 Phase 2). additionalProperties:false makes an unknown field a rejection, mirroring the grasp-contract discipline (PHILOSOPHY #29).",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "entities"],
  "properties": {
    "version": {
      "const": "layout/1.0",
      "description": "Layout DSL version. Bound to LAYOUT_DSL_VERSION by the drift test."
    },
    "meta": { "$ref": "#/$defs/meta" },
    "strategy": {
      "description": "Placement strategy for unpositioned entities. Bound to VALID_STRATEGIES.",
      "enum": ["linear", "grid", "stack", "radial", "manual"]
    },
    "strategyOptions": { "$ref": "#/$defs/strategyOptions" },
    "entities": {
      "type": "array",
      "items": { "$ref": "#/$defs/entity" }
    },
    "constraints": {
      "type": "array",
      "items": { "$ref": "#/$defs/constraint" }
    }
  },
  "$defs": {
    "meta": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string" },
        "description": { "type": "string" }
      }
    },
    "strategyOptions": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "axis": {
          "description": "Axis token for linear/radial strategies. Bound to VALID_AXES.",
          "enum": ["+X", "-X", "+Y", "-Y", "+Z", "-Z"]
        },
        "spacing": { "type": "number" },
        "cols": { "type": "integer" },
        "baseZ": { "type": "number" }
      }
    },
    "vec3": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "x": { "type": "number" },
        "y": { "type": "number" },
        "z": { "type": "number" }
      }
    },
    "vec2": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "x": { "type": "number" },
        "y": { "type": "number" }
      }
    },
    "quaternion": {
      "type": "object",
      "additionalProperties": false,
      "required": ["x", "y", "z", "w"],
      "properties": {
        "x": { "type": "number" },
        "y": { "type": "number" },
        "z": { "type": "number" },
        "w": { "type": "number" }
      }
    },
    "frame": {
      "type": "object",
      "additionalProperties": false,
      "required": ["ref", "name"],
      "properties": {
        "ref": { "type": "string" },
        "name": { "type": "string" },
        "translation": { "$ref": "#/$defs/vec3" }
      }
    },
    "entity": {
      "type": "object",
      "additionalProperties": false,
      "required": ["ref", "type", "name"],
      "properties": {
        "ref": { "type": "string" },
        "type": {
          "description": "Entity type expressible in Layout DSL. Bound to VALID_ENTITY_TYPES.",
          "enum": [
            "Solid", "CoordinateFrame",
            "AnnotatedLine", "AnnotatedRegion", "AnnotatedPoint"
          ]
        },
        "name": { "type": "string" },
        "description": { "type": "string" },
        "placeType": { "type": "string" },
        "ifcClass": { "type": "string" },
        "dimensions": { "$ref": "#/$defs/vec3" },
        "rotation": { "$ref": "#/$defs/quaternion" },
        "position": { "$ref": "#/$defs/vec3" },
        "parentRef": {
          "description": "For a standalone CoordinateFrame, the ref of another entity it is a TF child of (e.g. tcp → robot_base). When present, `position`/`rotation` are the LOCAL offset from that parent; when absent, they are the world pose (world-parented root). Robot TF tree world → robot_base → tcp (ADR-084 §2 revised).",
          "type": "string"
        },
        "robotRole": {
          "description": "Declared role of a CoordinateFrame inside a robot's TF tree (ADR-090): 'base' = world-parented root (where the arm stands), 'tcp' = its tool-point child. This is what makes a robot identifiable as an ENTITY rather than by the magic name `robot_base`, so a scene can carry N robots. Absent = an ordinary frame belonging to no robot.",
          "enum": ["base", "tcp"]
        },
        "graspFeature": { "$ref": "#/$defs/graspFeature" },
        "frames": {
          "type": "array",
          "items": { "$ref": "#/$defs/frame" }
        },
        "vertices": {
          "type": "array",
          "items": { "$ref": "#/$defs/vec2" }
        }
      }
    },
    "graspFeature": {
      "description": "Declares WHERE on a Solid it should be grasped (ADR-119 D2 / ADR-128). OPTIONAL: with no graspFeature the front derives the faces from the declared hand (ADR-118) and the panel says so — absence is a state (\"nobody said\"), distinct from an explicit `anywhere` (\"I looked and chose not to narrow\"). A closed kind-discriminated union, same governance as `gripper` (ADR-118): the kind decides which fields exist. Declared faces are used VERBATIM and never merged with the derived set (ADR-119 D3), so a declaration cannot be silently widened.",
      "type": "object",
      "additionalProperties": false,
      "required": ["kind"],
      "properties": {
        "kind": {
          "description": "`anywhere` = declared and deliberately not narrowed (samples follow the hand's derived faces). `faces` = grasp only on the listed faces.",
          "enum": ["anywhere", "faces"]
        },
        "faces": {
          "description": "Required when kind is `faces`: the box faces worth touching, named by their outward LOCAL axis. At least one — an empty list declares nowhere to grasp, which downstream is indistinguishable from an honest \"no feasible pose\" (原則 #31). A parallel jaw needs a pair of OPPOSED faces: the solver measures the opening from these very samples, so a single face reports almost no width (ADR-118).",
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["face"],
            "properties": {
              "face": { "enum": ["+x", "-x", "+y", "-y", "+z", "-z"] },
              "region": { "$ref": "#/$defs/faceRegion" }
            }
          }
        }
      }
    },
    "faceRegion": {
      "description": "A sub-region of one face, NORMALISED to 0..1 across that face's two in-plane axes (u,v), so it means the same fraction whatever the solid's size — a region in metres would stop covering the face the day the box is resized. Omitted = the whole face. A grasp needs AREA, so this is a rectangle and never a single point (a jaw needs finger width, a cup needs its diameter).",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "uMin": { "type": "number", "minimum": 0, "maximum": 1 },
        "uMax": { "type": "number", "minimum": 0, "maximum": 1 },
        "vMin": { "type": "number", "minimum": 0, "maximum": 1 },
        "vMax": { "type": "number", "minimum": 0, "maximum": 1 }
      }
    },
    "constraint": {
      "type": "object",
      "additionalProperties": false,
      "required": ["source", "target", "semanticType"],
      "properties": {
        "source": { "type": "string" },
        "target": { "type": "string" },
        "jointType": {
          "description": "URDF kinematic joint type, or null for a topological-only link. Bound to VALID_JOINT_TYPES.",
          "oneOf": [
            { "type": "null" },
            { "enum": ["fixed", "revolute", "continuous", "prismatic", "floating", "planar"] }
          ]
        },
        "semanticType": {
          "description": "Domain semantic type. Bound to VALID_SEMANTIC_TYPES.",
          "enum": [
            "fastened", "mounts", "aligned",
            "contains", "adjacent", "above", "connects",
            "references", "represents", "bounded_by"
          ]
        },
        "properties": {
          "type": "object",
          "description": "Domain-open property bag (connector, distance_mm, voltage_V, note, ...). Kept open on purpose — property names are extensible.",
          "additionalProperties": true
        }
      }
    }
  }
}
