# 120. 評価できなかった objective は 0 点ではない — 分母に居座る空位を外す

- Status: Accepted (**全 Decision 実装済み** — D2/D3 は 2026-08-12 スタブレーン + クライアント、D1 は 2026-08-14 実ソルバ)
- 実施記録: 鍵の不在で「測っていない」を運ぶ規律 (D2) と、それを 0 のバーで描かない
  提示 (D3) は、スタブレーン (`mocks/graspStub/solve.js`) と `src/view/GraspScoreMath.js` +
  `GraspSearchPanel` で 2026-08-12 に実装。**分母の式 (D1) は 2026-08-14 に決着**
  (DEF-013 — `GREP:` trigger が発火した最初の例)。順序をスタブ先行にしたのは、`core/` を
  先に直してもスタブが `reach_margin: 0` を載せ続けるかぎり画面で確認できないため
  (フロントは `plan{}` を集めていないので、既定の実行ではリーチ範囲が常に未宣言)。
  D1 の実装で分かったことは §D1 実装で分かったこと に記録。
- Date: 2026-08-11 (D2/D3 実装 2026-08-12・D1 実装 2026-08-14)
- Deciders: yuubae215, Claude
- 段: **G-0** (`docs/grasp/implementation-order.md` — grasp レーンの順序表)。D1 は G-1 (ADR-121) の前提
- Supersedes / Superseded by: なし (ADR-105「未検証は問題なしではない」のスコア層版)

## Context

`core/engine/scoring.py:weighted_sum` はこうなっている:

```python
for name, w in weights.items():
    weight_sum += w                    # 評価できなくても分母に入る
    s = objective_scores.get(name)
    if s is not None:
        total += w * s                 # 分子には入らない
return total / weight_sum
```

**評価できなかった objective は、分母に満額で効いて分子に 0 で効く。** docstring は
「寄与 0 (評価不能な objective)」と*意図として*書いているが、これは
**「評価不能」と「0 点」を同じ数に潰している**。

実害はもう出ている。`reach_margin` はリーチ範囲 (`plan.reachMin/reachMax`) が
未宣言だと常に 0 になる。それは「余裕ゼロ」ではなく「測っていない」なのに、
同じ数として totalScore を押し下げている。候補間の順位は一様スケールなので変わらないが、
**絶対値が下がる** — 契約が「絶対基準なのでリクエスト間で比較可能」と明言している、
その比較可能性が静かに壊れている。

ADR-105 が場の集約で潰した「未検証と問題なしの混同」と同じ形で、今度はスコア層。

## Decision

### D1 — 重み付けされていても評価できなかった objective は、分子からも**分母からも**外す

`weight_sum` に加えるのは、実際に評価できた objective の重みだけ。totalScore は
「評価できたものの加重平均」になり、リクエスト間の比較可能性が保たれる。

### D2 — 評価しなかったことは `objectiveScores` の**鍵の不在**が表す

`core/` は既に未知の名前を返り値から落としている (`evaluate_objectives`)。同じ規律を
「知っているが評価できなかった」にも適用する。閉じたスコア層に「評価できなかった名前」の
欄を足す案 (版上げが要る) は却下 — ADR-060 が抑えている「optional 兄弟を生やさない」に
触れるうえ、不在で表現できることに欄を作る理由がない。

### D3 — クライアントは鍵の不在を「測っていない」として描く

バーを 0 で描かない。何が評価されなかったかを言う。これは提示なのでクライアント側
(原則 #29)。

## Consequences

### 得られるもの

- リーチ範囲を宣言していないリクエストのスコアが、宣言しているものと比較できるようになる。
- 重心 (ADR-121) を入れたとき、重心不在で `com_offset` が評価できなくても、
  他の候補が不当に低く見えることがない — **この ADR が先に要る理由**。

### 実装で分かったこと — 同じ欠陥がスタブにも居た

`core/` を直す前にスタブを開いたら、**同じ形の欠陥が二人目の生産者にも独立に居た**:
`solve.js` はリーチ範囲が未宣言のとき `reachMargin = 0` を載せていて、コメントは
`// undeclared envelope → no margin to report` と*意図*まで書いてあった。`core/` の
docstring が「寄与 0 (評価不能な objective)」と書いていたのと同じ形である。

意図が書いてあるのに値が嘘をつく、というのがこの ADR の主題そのもので、**書いた人が
その瞬間に問われる場所が無い**かぎり二人目・三人目が同じ形を再生産する。だから成果物は
散文ではなく検査にした (原則 #19 Q3) — スタブは契約の第二の生産者なので、
`conformance.test.js` が `core/` と同じ規律で縛る。

### D1 実装で分かったこと (2026-08-14) — **D1 だけでは主症状が消えない**

D1 は「評価できなかった objective を分母から外す」と書いてあり、その分岐は
`objective_scores` に**鍵が無い**ことで発動する。ところが `core/` は
`reach_margin` を **評価済みの 0.0** として返していた: `NormSpec.normalize()` が
退化 (`span <= 0`) と無限 (`reachMax` 未宣言 = `inf`) の両方で 0.0 を返しており、
鍵は常に在る。**つまり D1 の条件分岐には一度も入らない** — 分母だけ直しても、
この ADR が名指しした当の症状 (「`reach_margin` は常に 0 で totalScore を押し下げる」) は
1mm も動かなかった。

欠けていたのは D2 の `core/` 側だった。D2 は「評価しなかったことは鍵の不在が表す」と
決めているが、**何をもって「評価しなかった」とするか**を書いていない。答えは
**絶対基準の不在**である: 0-1 に写す基準が無いなら、その objective は測れない。
よって `NormSpec.normalize()` は `Optional[float]` を返し、`evaluate_objectives` が
`None` を鍵ごと落とす。判定は 1 箇所 (原則 #1)。

教訓は ADR-109 D3 / ADR-115 と同型: **決定は在ったが、それを発動させる条件が
どこにも書かれていなかった。** 「鍵が無いとき」と書いた側は、鍵を作る側が
何をしているかを見ていない。二人の間に検査が無いと、両方が自分の担当範囲では
正しいまま、全体としては何も直らない。

**3 人目の生産者。** `core/easy_extrude_core/recommendation/ranking.py` の
`weighted_average` に同型の欠陥が独立に住んでいた。docstring は
「weights に在って values に無いキーは寄与 0」と*意図*を書いており、スタブと
`core/` で見たのと**同じ形**である (意図が書いてあるのに値が嘘をつく)。propose レーンは
真偽値を返さないので契約違反ではないが、欠けた信号を 0 として平均すると
「その信号では似ていない」と「その信号が取れなかった」が同じ順位に潰れ、similarity の
絶対基準性 (ADR-077 §3) が候補ごとに揺れる。同じ規律で直した。

**GSN 側の穴 (記録のみ — 新 ADR は起こさない)。** DEF-013 に対応する GSN ノードは
`assumption` で、同じ goal には既にスタブレーンの `solution` が吊られていた。
`check-gsn-debt.mjs` が数えるのは「支えが**ひとつも**無い goal」なので、
**部分的に支えられた goal にぶら下がる宣言済みの穴には欄が無い** — この ADR を
片付けても負債の個数 30 は動かない (実測で確認)。原則 #31 がその原則の道具自身に
出た形なので、`docs/PHILOSOPHY.md` の Yellow Cards に 1 例目として記録した
(2 例目が出たら検査へ昇格)。

### 払うもの

- **応答の数値が変わる。** スキーマは変わらないので版上げ対象外だが、`core/` の
  数値テストは動く。既存の記録と比較するときは境界をまたぐ。
  実測 (2026-08-14): フロントの既定重み (reach 0.6 / clearance 0.4 / stability 1.0) で
  リーチ範囲を宣言しない実行では、分母が **2.0 → 1.4** になり全候補のスコアが
  **1.4286 倍**。分子は同一なので**順位は不変**である (この場合に限る — 候補ごとに
  評価できる軸が違えば順位も動く)。
- 「全部評価できなかった」ときの totalScore が未定義になる (分母 0)。**実装では 0.0 を
  返す**ことにした。「全部 0 点」と同じ数になるが、区別は `objectiveScores` が空である
  ことが運ぶ — 値ではなく鍵の有無が答える (D2 と同じ規律で、第二の表現を作らない)。

### 検証 (証拠)

**値で比較すると「評価不能」と「0 点」はまさに同じに見える。だから検査は鍵の不在を問う**
— 検査の書き方そのものがこの ADR の主張である。

| 主張 | 問い所 | 状態 |
|------|-------|------|
| 評価不能な objective が分母に入らない | `mocks/graspStub/conformance.test.js`「評価できない objective に重みを付けても totalScore は動かない」 | ✅ スタブレーン |
| 同じ主張を実ソルバで (単体) | `core/tests/test_engine.py`「評価不能な objective を分母から外す」— 重みを 1e6 積んでも動かない / 測れた上での 0 は分母に入る | ✅ 2026-08-14 |
| 同じ主張を実ソルバで (パイプライン) | 同上「評価不能を 0 点として採点しない」— フロントの既定リクエスト (plan{} 無し) を再現し、応答の鍵の不在と、重み 5 倍でも totalScore・順位が一致すること。**単体だけにしないのは、算術は不完全な `objectiveScores` が実際に流れてくることを示さないから** | ✅ 2026-08-14 |
| 絶対基準が無い軸は評価済みにならない | 同上「リーチ範囲未宣言なら reach_margin は鍵ごと出ない」+ `NormSpec` の 2 退化が `None` を返すこと。**D1 が発動する前提**なのでここが本体 | ✅ 2026-08-14 |
| 同じ規律が propose レーンにも効く | `core/tests/test_recommendation.py`「取れなかった信号は分母に入らない」 | ✅ 2026-08-14 |
| 評価不能と 0 点が区別できる (ワイヤ) | 同上「リーチ範囲を宣言していなければ reach_margin は 0 ではなく鍵ごと出ない」— 宣言あり側との対照込み | ✅ |
| 評価不能と 0 点が区別できる (画面) | `src/view/GraspScoreMath.test.js`「未評価は 0 点と別物」+ e2e S8 | ✅ |
| 母集団が要求した重みである (不在を数えられる) | `GraspScoreMath.test.js`「要求した objective は全部行になる」 | ✅ |
| 測れなかったことが無言の省略にならない | e2e S8 — 画面に `not measured: <名前>` が出ること | ✅ |

論証木: `docs/gsn/adr-120-unevaluated-is-not-zero.gsn`
