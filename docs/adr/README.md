# Architecture Decision Records (ADR)

This directory records the project's design decisions.

## Rules

- File naming: `ADR-NNN-kebab-case-title.md`
- Status: `Proposed` / `Accepted` / `Deprecated` / `Rejected` / `Superseded by ADR-NNN`
- When superseding a decision, update the old ADR's status and add a new ADR (do not delete)

## Index

| No. | Title | Status | Date | Related |
|-----|-------|--------|------|---------|
| [ADR-001](ADR-001-voxel-based-shape-representation.md) | Voxel-based Shape Representation | **Superseded by ADR-007** | 2026-03-20 | ADR-007 |
| [ADR-002](ADR-002-two-modeling-methods.md) | Two Modeling Methods (Primitive Box / Sketch→Extrude) | Accepted | 2026-03-20 | ADR-004, ADR-007 |
| [ADR-003](ADR-003-orbit-control-middle-click.md) | Orbit Control Migrated to Middle-Click | **Rejected** | 2026-03-20 | ADR-006 |
| [ADR-004](ADR-004-edit-mode-adapts-to-object-type.md) | Edit Mode Adapts to Object Type | Accepted | 2026-03-20 | ADR-002, ADR-005 |
| [ADR-005](ADR-005-object-hierarchy-dimensional-classification.md) | Object Hierarchy with 1D/2D/3D Dimensional Classification | Accepted | 2026-03-20 | ADR-004 |
| [ADR-006](ADR-006-right-click-cancel-context-menu.md) | Right-Click = Cancel / Context Menu | Accepted | 2026-03-20 | — |
| [ADR-007](ADR-007-cuboid-based-shape-representation.md) | **Cuboid-based Shape Representation** | Partially superseded by ADR-020 (entity renamed `Cuboid` → `Solid`) | 2026-03-20 | ADR-001 |
| [ADR-008](ADR-008-mode-transition-state-machine.md) | **Mode Transition State Machine — Logical Consistency Policy** | Accepted | 2026-03-20 | ADR-002, ADR-004 |
| [ADR-009](ADR-009-domain-entity-types-cuboid-sketch.md) | **Domain Entity Types: Cuboid / Sketch** | Partially superseded by ADR-020 (renamed to Solid / Profile) | 2026-03-20 | ADR-002, ADR-005, ADR-007, ADR-020 |
| [ADR-010](ADR-010-domain-entity-behaviour-methods.md) | **Domain Entity Behaviour Methods (DDD Phase 2)** | Accepted | 2026-03-20 | ADR-009 |
| [ADR-011](ADR-011-application-service-scene-service.md) | **Introducing the ApplicationService Layer — SceneService (DDD Phase 3)** | Accepted | 2026-03-20 | ADR-009, ADR-010 |
| [ADR-012](ADR-012-graph-based-geometry-model.md) | **Graph-based Geometry Model (Vertex / Edge / Face / Solid)** | Accepted | 2026-03-20 | ADR-005, ADR-009, ADR-011 |
| [ADR-013](ADR-013-domain-events-scene-service-observable.md) | **Domain Events — Making SceneService Observable (DDD Phase 4)** | Accepted | 2026-03-20 | ADR-011, ADR-010 |
| [ADR-014](ADR-014-edit-mode-sub-element-selection.md) | **Edit Mode Sub-Element Selection (DDD Phase 6)** | Accepted | 2026-03-20 | ADR-004, ADR-012 |
| [ADR-015](ADR-015-bff-microservices-architecture.md) | **BFF + Microservices Architecture** | Accepted (Phase A/B/C 実装済み。**Phase D は 2026-08-12 に再評価** — 9 項目中 4 項目を廃止 (現在のレイヤ写像と矛盾)、5 項目は生きた残し DEF-019 / DEF-020。「Priority TBD after Phase C」の満期は Phase C 完了 2026-04-15 に到来しており 4 か月間誰も判定しなかった — ADR-123 §力学 1) | 2026-03-20 | ADR-011, ADR-012, ADR-013 |
| [ADR-016](ADR-016-transform-graph-scene-relationships.md) | **Transform Graph — Spatial Relationships Between Scene Objects** | Accepted | 2026-03-21 | ADR-012, ADR-015 |
| [ADR-017](ADR-017-websocket-session-geometry-service.md) | **WebSocket Session Design and Geometry Service (Phase B)** | Accepted | 2026-03-21 | ADR-015, ADR-016 |
| [ADR-018](ADR-018-coordinate-frame-entity.md) | **Coordinate Frame Entity — Object Origin Hierarchy** | Accepted | 2026-03-23 | ADR-005, ADR-009, ADR-011, ADR-013, ADR-016 |
| [ADR-019](ADR-019-coordinate-frame-phase-b.md) | **CoordinateFrame Phase B — Nested Hierarchy and Rotation Editing** | Accepted | 2026-03-23 | ADR-005, ADR-008, ADR-016, ADR-018 |
| [ADR-020](ADR-020-domain-entity-taxonomy-redesign.md) | **Domain Entity Taxonomy Redesign — Solid / Profile / Frame / Annotation** | Accepted | 2026-03-25 | ADR-007, ADR-009, ADR-010, ADR-012, ADR-018, ADR-019 |
| [ADR-021](ADR-021-unified-local-geometry-graph-interface.md) | **Unified Local-Geometry Graph Interface — extend graph model to MeasureLine and Profile** | Accepted | 2026-03-25 | ADR-012, ADR-020 |
| [ADR-022](ADR-022-undo-redo-command-pattern.md) | **Undo / Redo via Command Pattern** | Accepted | 2026-03-26 | ADR-008, ADR-010, ADR-011, ADR-012 |
| [ADR-023](ADR-023-mobile-input-model.md) | **Mobile Input Model — Touch Gesture Model and Device Detection** | Accepted | 2026-03-29 | ADR-003, ADR-006, ADR-014 |
| [ADR-024](ADR-024-mobile-toolbar-architecture.md) | **Mobile Toolbar Architecture — Fixed-Slot Layout and Context-Sensitive Actions** | Accepted | 2026-03-29 | ADR-008, ADR-023 |
| [ADR-025](ADR-025-ifc-semantic-classification.md) | **IFC Semantic Classification of Scene Objects** | Accepted | 2026-04-01 | ADR-020, ADR-021, ADR-022, ADR-013 |
| [ADR-026](ADR-026-lynch-urban-classification.md) | **Lynch Urban Classification of 2D Map Objects** | **Superseded by ADR-029** | 2026-04-01 | ADR-029 |
| [ADR-027](ADR-027-wasm-geometry-engine.md) | **Wasm Geometry Engine — Three-Layer Architecture with Zero-Copy Data Path** | Accepted | 2026-04-05 | ADR-007, ADR-012, ADR-017 |
| [ADR-028](ADR-028-anchored-annotations-scene-graph.md) | **Anchored Annotations & Scene Graph API** | Accepted | 2026-04-06 | ADR-012, ADR-016, ADR-018, ADR-019, ADR-021 |
| [ADR-029](ADR-029-spatial-annotation-system.md) | **Spatial Annotation System — AnnotatedLine/Region/Point with Place Types** | Accepted | 2026-04-08 | ADR-020, ADR-021, ADR-022, ADR-025, ADR-013, ADR-028 |
| [ADR-030](ADR-030-spatial-link.md) | **SpatialLink — Typed Semantic Edges Between Annotated Elements** | Accepted | 2026-04-09 | ADR-029, ADR-028, ADR-013, ADR-020, ADR-022 |
| [ADR-031](ADR-031-map-mode-interaction-model.md) | **Map Mode Interaction Model & Visual Language** | Partially superseded by ADR-093 (§8 Animations のみ; §1-§7 は Accepted) | 2026-04-11 | ADR-023, ADR-024, ADR-029, ADR-006 |
| [ADR-032](ADR-032-geometric-host-binding.md) | **Geometric Host Binding — Map Elements Mounted on Scene Objects** | Accepted | 2026-04-13 | ADR-029, ADR-030, ADR-016, ADR-018, ADR-019 |
| [ADR-033](ADR-033-coordinate-frame-phase-c.md) | **CoordinateFrame Phase C — Interface Contract Model** | Accepted | 2026-04-15 | ADR-018, ADR-019, ADR-032, ADR-030 |
| [ADR-034](ADR-034-coordinate-frame-placement-policy.md) | **CoordinateFrame Placement and Pose Policy** | Accepted | 2026-04-20 | ADR-033, ADR-032, ADR-030, ADR-018, ADR-019 |
| [ADR-035](ADR-035-fastened-chain-propagation.md) | **Fastened Constraint CF-Chain Propagation and Cycle Detection** | Accepted | 2026-05-02 | ADR-032, ADR-033, ADR-030, ADR-018, ADR-019 |
| [ADR-036](ADR-036-solid-arbitrary-rotation.md) | **Solid Arbitrary Rotation — R key, Corner-Baking** | Partially superseded by ADR-040 (§"Known Limitation" の corner-baking のみ) | 2026-05-02 | ADR-007, ADR-019, ADR-022, ADR-012, ADR-040 |
| [ADR-037](ADR-037-body-frame-architecture.md) | **Body Frame Architecture — CF-Primary Entity Model** | Accepted | 2026-05-07 | ADR-018, ADR-019, ADR-033, ADR-034, ADR-035, ADR-036 |
| [ADR-038](ADR-038-urdf-link-taxonomy.md) | **URDF-Style Link Taxonomy: Kinematic + Semantic Two-Layer Classification** | Accepted | 2026-05-08 | ADR-030, ADR-032, ADR-016, ADR-037 |
| [ADR-039](ADR-039-operation-state-machine.md) | **Runtime Operation State Machine — FSM-First Design Pattern** | Accepted | 2026-05-09 | ADR-008, ADR-022, ADR-030 |
| [ADR-040](ADR-040-solid-data-model-redesign.md) | **Solid Data Model Redesign — Primary Triple (_position, orientation, localCorners)** | Accepted | 2026-05-14 | ADR-036, ADR-035, ADR-022, ADR-012 |
| [ADR-041](ADR-041-semantic-inference-suggestions.md) | **Semantic Inference Suggestions — Geometric Heuristics for SpatialLink Proposals** | Accepted | 2026-05-17 | ADR-030, ADR-038, ADR-039 |
| [ADR-042](ADR-042-unified-entity-transform-mental-model.md) | **Unified Entity Transform Mental Model — Fixed-Slot Transform Policy** | Accepted | 2026-05-20 | ADR-023, ADR-024, ADR-037, ADR-039, ADR-040 |
| [ADR-043](ADR-043-2d-3d-spatial-link-semantics.md) | **2D/3D Spatial Link Semantics — `bounded_by` and Clearance Evaluation** | Accepted (実装済み — Phase 1〜4 すべて。本文 §Deferred の 3 項目も `✅ Implemented`。**2026-08-12 昇格** — Draft のまま 2 か月半放置され、登録簿が「判断の未成熟」として残しの母集団から除外していたが根拠が偽だった。ADR-123 §力学 2) | 2026-05-21 | ADR-029, ADR-030, ADR-038 |
| [ADR-044](ADR-044-5w1h-function-mapping.md) | **5W1H Function Mapping — Homomorphic Bridge Between Natural Language and Automation** | Draft | 2026-06-01 | ADR-030, ADR-039, ADR-041, ADR-022 |
| [ADR-045](ADR-045-external-layout-api.md) | **External Layout API — CLI/REST-Driven Scene Composition via Layout DSL** | Accepted | 2026-06-09 | ADR-044, ADR-015, ADR-022, ADR-030, ADR-037, ADR-040 |
| [ADR-046](ADR-046-context-dsl.md) | **Context DSL — 要件文脈の一級データ構造化と仕様への追跡可能コンパイル** | Accepted (実装済み — `src/context/` Schema/Validator/Compiler、ゴールデン 8/8。**2026-08-12 昇格**。**探索的 MVP が判断を閉じた実例**で、実装して初めて interval の確定方式が Decision エンティティ経由だと決まった — この 1 例が ADR-123 D4 で gate ではなく census を選ばせた) | 2026-06-10 | ADR-044, ADR-045, ADR-037, ADR-030, ADR-047 |
| [ADR-047](ADR-047-context-demo-layer.md) | **Context Demo Layer — 要求文脈の可視化オーバーレイ (UncertaintyGhost / Inspector / StoryBar)** | Accepted | 2026-06-11 | ADR-046, ADR-045, ADR-040, ADR-041 |
| [ADR-048](ADR-048-link-network-layered-layout.md) | **Link Network 決定的階層レイアウト — 力学レイアウト廃止と親子構造の可視化** | Accepted (§2.1 ノード集合と §2.2.1 は ADR-094 で改訂、§2.1 幾何割当と §2.3 過密縮退は ADR-095 で改訂。§2.1 決定性・§2.3 寸法・§2.4 契約は有効) | 2026-06-12 | ADR-030, ADR-037, ADR-038, ADR-094, ADR-095 |
| [ADR-049](ADR-049-requirement-conflict-model.md) | **Requirement / Conflict モデル — KPI 由来の許容領域・衝突検出・交渉クラスター** | Accepted | 2026-06-14 | ADR-046, ADR-047, ADR-044, ADR-035 |
| [ADR-050](ADR-050-context-first-project-model.md) | **Context-First Project Model — 要求/衝突/交渉 の PoC から本番機能化** | Accepted | 2026-06-14 | ADR-049, ADR-046, ADR-047, ADR-045, ADR-022, ADR-013, ADR-011 |
| [ADR-051](ADR-051-requirement-intake.md) | **要件入力（Requirement Intake）— あいまい要件を起点化する複数入口アーキテクチャ** | Accepted (全 4 フェーズ実装済) | 2026-06-16 | ADR-052, ADR-050, ADR-049, ADR-047, ADR-046, ADR-044, ADR-022, ADR-013 |
| [ADR-052](ADR-052-5w1h-ubiquitous-language.md) | **5W1H ユビキタス言語 — NL ⇄ データの Mutual 構造（Why ルートの正準ツリー）** | Accepted (Phase 1 + Phase 2 + Phase 3 + Phase 4 実装済) | 2026-06-17 | ADR-046, ADR-044, ADR-049, ADR-048, ADR-050, ADR-051 |
| [ADR-053](ADR-053-robotics-kpi-methods.md) | **ロボティクス KPI メソッド — 測定器としての運動学/軌道/干渉計算と可視検証ループ** | Accepted (Phase 1+2+3 実装済) | 2026-06-20 | ADR-038, ADR-047, ADR-049, ADR-052, ADR-027, ADR-015, ADR-017 |
| [ADR-054](ADR-054-ui-dsl-bff-grasp-walkthrough.md) | **UI → DSL → BFF → Grasp API Verification Walkthrough** | Accepted | 2026-06-22 | ADR-045, ADR-050, ADR-046, ADR-015, ADR-017, ADR-053 |
| [ADR-055](ADR-055-scene-layout-dsl-mutual.md) | **Scene ⇄ Layout DSL Mutual — a Normal-Form Inverse for the Geometry Layer** | Accepted (Phase 1 実装済) | 2026-06-22 | ADR-045, ADR-052, ADR-050, ADR-054, ADR-040, ADR-037, ADR-038 |
| [ADR-056](ADR-056-computable-structural-isomorphism.md) | **Computable Structural Isomorphism on the Synonym Quotient — Canonical Form, Diff, Reconcile** | Accepted (実装済) | 2026-06-23 | ADR-052, ADR-055, ADR-044, ADR-050, ADR-051, ADR-049 |
| [ADR-057](ADR-057-grasp-search-ui.md) | **Grasp Search UI — 右ドックの宣言/検証パネル（スコア優先・ゴーストは後続）** | Accepted (実装済) | 2026-06-30 | ADR-054, ADR-050, ADR-047, ADR-053, ADR-049, ADR-052, ADR-055 |
| [ADR-058](ADR-058-context-authoring-fork-and-tweak.md) | **Context オーサリング UX — 例を土台に編集する（fork & tweak）** | Accepted (Phase 1 + seed chips + UX 具体化 + Phase 2 in-place 編集 実装済) | 2026-06-30 | ADR-051, ADR-050, ADR-046, ADR-049, ADR-052, ADR-057 |
| [ADR-059](ADR-059-grasp-candidate-spatial-ghost.md) | **Grasp 候補の空間ゴースト — 数値を「掴める姿」に翻訳する（段階化）** | Accepted (段1 実装済 2026-07-03; 段2 は門2 待ち) | 2026-06-30 | ADR-057, ADR-053, ADR-047, ADR-054, ADR-045, ADR-060 |
| [ADR-060](ADR-060-grasp-contract-data-governance.md) | **Grasp Contract のデータ構造統治 — 決定層は閉、pose は kind 判別の有界 union** | Accepted (upstream 実装済み確認 2026-07-01; 本リポジトリ追従済 2026-07-03 — BFF 型再生成 + 消費コード ADR-059 段1) | 2026-06-30 | ADR-059, ADR-057, ADR-054, ADR-056 |
| [ADR-061](ADR-061-grasp-diagnostics-funnel-feedback.md) | **Grasp Diagnostics ファネル — 棄却の集計事実を「効いた感」フィードバックへ導出する** | Accepted (実装済) | 2026-07-04 | ADR-060, ADR-057, ADR-059, ADR-054 |
| [ADR-062](ADR-062-ux-upgrade-three-layer-policy.md) | **UX アップグレード三層方針 — 証明は接続先、契約はカノニカル、体感はゲーム感覚** | Accepted (全フェーズ実装済) | 2026-07-05 | ADR-063, ADR-060, ADR-061, ADR-059, ADR-058, ADR-057, ADR-054, ADR-053, ADR-051, ADR-050 |
| [ADR-063](ADR-063-selection-first-intake-wizard.md) | **選択優先インテーク — ウィザード・パラメトリックアセット・KPI 式カタログ（白紙入力不能の前提）** | Accepted (全 5 フェーズ実装済) | 2026-07-05 | ADR-062, ADR-051, ADR-058, ADR-050, ADR-049, ADR-045, ADR-047 |
| [ADR-064](ADR-064-rigor-retrofit-program.md) | **Rigor 側の遡及プログラム — CI ゲート化・DSL スキーマ昇格・未契約ワイヤの明示宣言・play の検証** | Accepted (Phase 1 実装済) | 2026-07-08 | ADR-062, ADR-060, ADR-054, ADR-045, ADR-046, ADR-053, ADR-015, ADR-017 |
| [ADR-065](ADR-065-front-playfulness-program.md) | **体感層の全面遊戯化プログラム — front は遊び場、back は証明のまま** | Accepted (全 7 フェーズ + 全候補実装済 — スナップ係合フラッシュ完了 2026-07-12 でプログラム完結; Widening 1 は ADR-066 で amend) | 2026-07-09 | ADR-062, ADR-064, ADR-061, ADR-059, ADR-058, ADR-047, ADR-066 |
| [ADR-066](ADR-066-motion-tier-delight-allowance.md) | **Motion Tier に Tier D「delight」を追加 — 遊び心を禁じず統治する** | Accepted | 2026-07-12 | ADR-065, ADR-062, ADR-064, ADR-067 |
| [ADR-067](ADR-067-viewport-stage-boot-reveal.md) | **ビューポート常設ステージ + 起動リビール — Tier D の最初の適用** | Accepted | 2026-07-13 | ADR-066, ADR-065 |
| [ADR-068](ADR-068-camera-focus-flight-living-selection.md) | **カメラ・フォーカスフライト + 生きた選択 — ナビの Tier D と実体の Tier A/F** | Accepted | 2026-07-13 | ADR-067, ADR-066, ADR-065 |
| [ADR-069](ADR-069-ux-parity-pass.md) | **UX パリティ・パス Phase 1 — ギズモを CameraFlight へ / Link Network は動きでなく状態で凡例化 / 位相リンク切断 / Frame 命名統一** | Accepted (Phase 1; Phase 2→ADR-070・Phase 3→ADR-071・Phase 4→ADR-072 すべて実装済 — パス完結) | 2026-07-14 | ADR-068, ADR-048, ADR-030, ADR-038, ADR-037 |
| [ADR-070](ADR-070-entity-identity-ifc.md) | **エンティティ同一性 — 3D ラベル機構の一般化(CF 情報化 + Solid/Mesh ラベル)と IFC クラスの活性化(視覚ティント)** | Accepted (決定2=A 視覚+軽い意味; 実装済) | 2026-07-15 | ADR-069, ADR-025, ADR-034, ADR-037 |
| [ADR-071](ADR-071-placement-defaults.md) | **物理的妥当性の配置既定 — スタック既定 ON + 地面下アシスト(ハード拘束でなく警告 + 補助; 基礎/杭を守る)** | Accepted (案A アシスト既定; 実装済) | 2026-07-15 | ADR-069, ADR-040 |
| [ADR-072](ADR-072-map-mode-polish.md) | **2D マップ研磨 — 一致ポーズ投影スワップ + CameraFlight で出入り / マップ配置の undo 化(lifecycle 語彙合流) / 端点スナップ係合フラッシュ再利用** | Accepted (実装済 — ADR-069 Phase 4 = パリティ・パス完結) | 2026-07-15 | ADR-069, ADR-068, ADR-031, ADR-022, ADR-065 |
| [ADR-073](ADR-073-no-creation-time-naming-form.md) | **生成時に命名フォームを出さない — Frame + マップオブジェクトは無言自動命名で即時生成、改名は後の別操作（Map FSM を pending 廃止で 2 状態化）** | Accepted (実装済) | 2026-07-16 | ADR-069, ADR-037, ADR-031, ADR-072 |
| [ADR-074](ADR-074-bff-core-api-contract.md) | **BFF <-> コアAPI の I/O 契約 — バージョン不一致を実行時に弾く境界の明文化** | Accepted (§6 置き場所のみ ADR-082 で改訂) | 2026-06-21 | ADR-075, ADR-076, ADR-082 |
| [ADR-075](ADR-075-stage0-judgement-engine.md) | **段階0 判定エンジンの設計方針 — 離散候補生成 -> 安い順フィルタ -> 加重和スコア -> 上位N件** | Accepted (実装済) | 2026-06-21 | ADR-074, ADR-076 |
| [ADR-076](ADR-076-core-api-endpoint-layer.md) | **コアAPI エンドポイント層 (HTTP 境界) — contractVersion ガード・エラー envelope・BFF 背後隔離** | Accepted (実装済) | 2026-06-21 | ADR-074, ADR-075 |
| [ADR-077](ADR-077-recommendation-similarity-lane.md) | **推薦/類似レーン — 曖昧な対応づけを embeddings で propose/rank するのみ、等価性は決めない (ADR-056 の decide/propose 境界)** | Accepted (propose-only core + HTTP 境界 実装済) | 2026-06-23 | ADR-074, ADR-075, ADR-076, ADR-052, ADR-056 |
| [ADR-078](ADR-078-bin-picking-scene-entities.md) | **bin-picking シーンのエンティティモデル — 障害物集合を属性 (static/dynamic) から導出、cone (許容角) と approach (進入角) を別軸に** | Accepted (実装済) | 2026-06-27 | ADR-074, ADR-075, ADR-076, ADR-077 |
| [ADR-079](ADR-079-search-diagnostics-proof.md) | **判定の証明 (feasibility funnel + near-miss) を UX 資産として返す — ワイヤに載せるのはソルバが決定した事実だけ** | Accepted (実装済) | 2026-07-04 | ADR-074, ADR-075, ADR-076 |
| [ADR-080](ADR-080-menu-choreography-extrude-front.md) | **残余モーション空白の被覆 — ポップオーバー入場振付 (ChromeMath 拡張) + 押し出し成長前線 (Tier A)** | Accepted (両 Phase 実装済) | 2026-07-19 | ADR-065, ADR-066, ADR-068 |
| [ADR-081](ADR-081-domain-staged-validation-fallback-ladder-kpi.md) | **ドメイン段階バリデーション (見える/届く/掴める) + 運用フォールバック階梯の設計時 KPI 検証** | Accepted (Phase 1-3 実装済; Phase 4 と収束仮説は未着手) | 2026-07-19 | ADR-049, ADR-053, ADR-057, ADR-061, ADR-063, ADR-074, ADR-075, ADR-076, ADR-078, ADR-079 |
| [ADR-082](ADR-082-absorb-contract-submodule.md) | **契約 submodule の repo 内吸収 — 壁は repo 分離ではなく CI ガードで守る** | Accepted (実装済) | 2026-07-19 | ADR-074, ADR-064, ADR-060, ADR-079, ADR-081 |
| [ADR-083](ADR-083-robot-base-placement-grasp-contract.md) | **ロボット base position をユーザーが動かし、grasp-search 契約に正式に載せる (optional 追加、version 据え置き)** | Accepted (契約は現行; フロント実装は ADR-084 §2 で CoordinateFrame entity に置き換え済み) | 2026-07-20 | ADR-074, ADR-075, ADR-078, ADR-057, ADR-060 |
| [ADR-084](ADR-084-tcp-oriented-ik-entity-grounded-geometry.md) | **TCP 姿勢基準の許容角判定 + ロボット base/TCP の CoordinateFrame 実体化 — grasp宣言の生座標を廃し幾何の正本をLayout DSLに一本化** | Accepted (全 Phase 実装済; §2 の「独立2フレーム」は ADR-085 で TF 親子化に改訂) | 2026-07-20 | ADR-083, ADR-081, ADR-078, ADR-074, ADR-018, ADR-034 |
| [ADR-085](ADR-085-robot-tf-tree-selectable-fast-grasp-entry.md) | **ロボットを TF 親子ツリー (world→robot_base→tcp) で接地 + 骨格を直接選択可能に + grasp-search を無フォームで開く** | Accepted (全 3 点 実装済) | 2026-07-22 | ADR-084, ADR-083, ADR-055, ADR-051, ADR-018, ADR-034 |
| [ADR-086](ADR-086-deterministic-boot-slice-in-required-gate.md) | **e2e は非必須のまま、boot 保証の決定的スライス (起動時 ReferenceError 不在) だけを必須 gate に落とす** | Accepted (実装済 — PR #341) | 2026-07-22 | ADR-064, ADR-085 |
| [ADR-087](ADR-087-cf-grounded-object-model-robot-visibility-outliner.md) | **CF 接地オブジェクトモデルの統一 + ロボット可視性を Outliner の所有下へ (ヘッダートグル撤去)** | Accepted | 2026-07-22 | ADR-037, ADR-084, ADR-085 |
| [ADR-088](ADR-088-tcp-seed-derived-from-urdf-fk.md) | **robot_base→tcp の既定 seed を URDF スケルトンの FK から導出 — フランジ位置の真実の源を URDF に一本化し手写し定数を廃す** | Accepted (実装済) | 2026-07-23 | ADR-084, ADR-085, ADR-053, ADR-018 |
| [ADR-089](ADR-089-launch-home-screen-layout-template-entry.md) | **起動ホーム画面 — 工程レイアウトテンプレ選択を起点にする（Blender 式スキップ設定つき）** | Accepted (実装済) | 2026-07-23 | ADR-067, ADR-051, ADR-063, ADR-065, ADR-066, ADR-045, ADR-055 |
| [ADR-090](ADR-090-robot-identity-multi-robot-zero-robot-state.md) | **ロボットの同一性を名前から実体へ — 複数台の受け入れ、0 台を一級の状態に、grasp は選択された 1 台で解く (契約は不動)** | Accepted (実装済) | 2026-07-25 | ADR-084, ADR-087, ADR-060, ADR-082 |
| [ADR-091](ADR-091-default-doc-first-intake-system-owned-refs.md) | **入力の起点を「白紙からの生成」から「既定値入り完成文書の訂正」へ — ref をシステム所有にする** | Proposed | 2026-07-25 | ADR-063, ADR-058, ADR-051, ADR-050, ADR-089, ADR-090 |
| [ADR-092](ADR-092-prompt-dilution-commit-observability.md) | **常時ロードから一般モデル挙動の写しを外し、観測はプロンプトではなくコミットに刻む (−42.4%、Task-Class は決定的導出)** | Accepted (実装済) | 2026-07-25 | ADR-082, ADR-086 |
| [ADR-093](ADR-093-map-annotation-visual-language-per-entity-phase.md) | **Map 注釈の視覚言語を刷新 — アニメーション位相を実体の同一性から導き「基数 N の lockstep」を構造で消す** | Accepted (実装済) | 2026-07-25 | ADR-031, ADR-029, ADR-065, ADR-066, ADR-067, ADR-072, ADR-043 |
| [ADR-094](ADR-094-link-network-tf-tree-fused-origin-node.md) | **LINK NETWORK を TF ツリーへ回帰 — Solid と Origin CF を 1 ノードに融合し、木辺を骨格に戻す (モバイルでは唯一の常設構造ビュー)** | Accepted (実装済み — 純粋 `computeLayout()` + 融合ノード + 骨格化した木辺) | 2026-07-26 | ADR-048, ADR-037, ADR-038, ADR-030, ADR-090, ADR-093 |
| [ADR-095](ADR-095-link-network-indented-outline-tree.md) | **LINK NETWORK を「1 ノード 1 行」のインデントツリーへ — 希少な軸 (固定幅) に可変量 (兄弟数) を割り当てるのをやめ、縮退の向きをラベルからリンクへ反転させる** | Accepted (実装済み — 行=ノード / x=深さ、`denseMode` 廃止、余木辺の決定的レーン割当、パネル内スクロール) | 2026-07-26 | ADR-094, ADR-048, ADR-093 |
| [ADR-096](ADR-096-visibility-two-axes-declared-defaults.md) | **可視性を「永続 (eye) × 文脈 (選択)」の直交 2 軸にし、既定を種と入口ごとに宣言する — 目が開いているのに何も見えない状態を消す** | Accepted (実装済み — `VisibilityAxes` 純粋合成 + `SceneService.applyEntityVisibility()` 単一書き手、既定表の宣言化、`_hideRobotByDefault()` 削除、書き手の静的ガード) | 2026-07-26 | ADR-087, ADR-094, ADR-090, ADR-089, ADR-037 |
| [ADR-097](ADR-097-support-is-entity-state-not-gesture-side-effect.md) | **接地を「ジェスチャの副作用」から「実体の状態」へ — 配置方針を型で宣言し、pose の唯一の入口で強制する (床下は事故ではなく宣言)** | Accepted (実装済み — `domain/placement.js` の宣言表 + 純粋な方針適用、`applyPreviewTranslation` を唯一の並進入口へ、`_applyStackSnap`/`_isMapObject` 廃止、ドラッグ平面を方針の関数へ、入口の個数検査) | 2026-07-26 | ADR-071, ADR-032, ADR-040, ADR-090, ADR-093, ADR-083 |
| [ADR-098](ADR-098-support-domain-is-policy-not-kind.md) | **「何の上に載れるか」を種から方針へ — stack assist の型ゲートを外し、支持プローブを方針表の隣で宣言する** | Accepted (実装済み — `stackAssistApplies()` + `SUPPORT_PROBE_BY_KIND` の宣言、種の門 2 枚の撤去、プローブ 4 メソッドの `instanceof` 撤去、種分岐の個数検査、差分ペアの e2e 3 本) | 2026-07-26 | ADR-097, ADR-071, ADR-085, ADR-096, ADR-083 |
| [ADR-099](ADR-099-selection-round-trip-one-entry.md) | **選択の往復を 1 つの入口へ — hover が click を殺す再構築の閉路を断ち、選択を「見えること」まで含む 1 つの決定にする** | Accepted (実装済み — `SelectionManager` の 5 verb + `_apply()` 単一遷移、`_objSelected`/`_selectedIds` の getter 化、hover 由来の SVG 全再構築を `layoutSignature` で遮断、選択が contextual 可視性を要求、溢れの上界 `FULL(claim) === 選択集合`、入口の個数検査) | 2026-07-26 | ADR-094, ADR-095, ADR-096, ADR-097, ADR-090, ADR-093 |
| [ADR-100](ADR-100-color-says-state-not-kind.md) | **色は「実体の種」ではなく「状態」を語る — 既定サーフェスを中立にし、アクセントを 1 つに畳み、宣言外の色を ratchet で止める** | Accepted (実装済み — 役割語彙への改名、`entityDefault` = 中立グレー、`accentActive` 退役と `accent`/`stageGlow` への分割、`snapTone`/`measure` の hue 移動、`theme/semantic.js` の対象外宣言、宣言外リテラルの ratchet 790/206、選択の描き手 9 種の列挙検査、コントラスト検査) | 2026-07-26 | ADR-065, ADR-064, ADR-062, ADR-093, ADR-067 |
| [ADR-101](ADR-101-pose-writers-derive-from-request-not-rendered-state.md) | **pose を計算する入力は「要求 + セグメント開始の写し」だけ — 補助が自分の出力を測り直すのをやめる (描画済みの状態は query 専用)** | Accepted (実装済み — `_applyStackAssist` の live 読み撤去、入口が適用済み delta を渡す形、座り Z を絶対目標へ、live プローブの query 専用宣言、writer の鮮度検査、同一要求の回帰 2 本) | 2026-07-27 | ADR-098, ADR-097, ADR-071, ADR-085, ADR-090 |
| [ADR-102](ADR-102-census-tables-need-a-denominator.md) | **列挙表は母集団を持つ — 「場所を並べた表」を廃し、覆うべき集合をコードから導出する (原則 #31 の自己適用)** | Accepted (実装済み — 表の 4 種型付けと `place-list` の語彙からの排除、pose 決定の呼び出し閉包を母集団へ、`SUPPORT_SURFACE_BY_KIND` の宣言化、`COLOR.accent` 消費者の分割、登録簿 `CENSUS_REGISTRY` の自己登録、走査道具の `src/census/` 一本化) | 2026-07-30 | ADR-098, ADR-101, ADR-097, ADR-100, ADR-099, ADR-096, ADR-090, ADR-093 |
| [ADR-103](ADR-103-map-is-a-viewpoint-not-a-mode.md) | **Map はモードではなく視点 — 台帳の負債 #1 を「禁止」ではなく「再分類」で閉じる** | Accepted (実装済み — `SceneView.setProjection()` 単一書き手 + 毎フレーム導出の ortho、`PlaceToolController` への再分類、Map ボタン/ツールバー/pinch/pan/wheel の撤去、`DS_PENDING`・`setFogSuspended` の退役、退役形と投影書き手の個数検査) | 2026-08-02 | ADR-031, ADR-073, ADR-093, ADR-068, ADR-008, ADR-104 |
| [ADR-104](ADR-104-ownership-proposal-and-the-receipt.md) | **編集の住所は所有権で決まり、記録は人の行為から始まる — 所有権・提案・証憑モデル** | Accepted (実装済み — 鍵の集合 `Keyring` / 実体ごとの `owner` と 2 種の 0 / 権限 3 状態 `EDIT_PERMISSION` / 提案・議題の遷移表と guard / 承認 = 主張更新 + 証憑追記の 1 コマンド / context/0.5 加算 (`proposals[]`・`agenda[]`) / R10・R11 / 全 9 通りの権限事例と退役語彙の個数検査) | 2026-08-02 | ADR-050, ADR-049, ADR-052, ADR-056, ADR-077, ADR-090, ADR-100, ADR-065, ADR-103 |
| [ADR-105](ADR-105-unexamined-is-not-clear.md) | **「検証されていない」は「問題が無い」ではない — 発見の集約を場の外へ出す** | Accepted (実装済み — `DiscoverySummary` の 2 union (`unexamined`/`examined`) と 4 種の検査 union、未宣言の種で throw する宣言表、ヘッダの 3 カウンタ + 3D 左上の KPI HUD、書き手を `contextSetDiscovery` 1 つへ畳み配線を文書の辺 (`contextLoaded`/`contextChanged`) へ、`Checks` タブの常設スロット化、N パネルの entity-scope 入口 (D5 の反証仮説を採用)、左端占有の所有者 `EdgeOccupancy`、母集団を導出する個数検査 + e2e 4 本) | 2026-08-02 | ADR-104, ADR-103, ADR-090, ADR-085, ADR-060, ADR-050, ADR-102, ADR-106 |
| [ADR-106](ADR-106-the-floor-is-a-table-not-a-strip.md) | **場は表であって縦帯ではない — 器を中身の形へ合わせ、右端の排他を「住所」で解く** | Accepted (実装済み — 場とチュートリアル Inspector を下部展開パネルへ、右端 280px スロットの退役 (排他 7 箇所 + `inspectorOpen` シフト + `+280` 項が**書く理由を失って**消えた)、器のタブを 「解消」「記録」の 6 枚へ (値域は `view/FloorTabs.js` の有界な宣言で未宣言値は throw)、出ていく 5 タブに住所 (Checks→HUD 展開 / Grasp→N パネル / Assets→AddMenu / Wizard・Intake→文書の入口 `DocIntakeLayer` **暫定**)、`context.loaded` の写しを退役、下端の占有所有者 `EdgeOccupancy.bottomEdgeOffset` + 段の宣言表、母集団を導出する個数検査 5 本 + e2e 3 本) | 2026-08-02 | ADR-050, ADR-047, ADR-105, ADR-104, ADR-103, ADR-102, ADR-063, ADR-094, ADR-107 |
| [ADR-107](ADR-107-selection-has-two-kinds.md) | **選択できるものが 2 種になる — 文書の変数を同じ 1 つの選択モデルへ入れ、混在を型で表現不能にする** | Accepted (実装済み — kind 判別の union (`empty`/`entities`/`variables`)、混在は `makeSelection` が throw、種ごとの宣言表 2 つ (3D の姿 / N パネルの body) が未宣言の種で throw、場の行列の変数見出しが窓、変数選択で未確定帯 + 束縛実体の DIMMED (上界は N=25 で焼く)、入口の census は**作り直さずに**通った。Outliner の意味側は Phase 3 と同時) | 2026-08-02 | ADR-099, ADR-096, ADR-106, ADR-050, ADR-094, ADR-095, ADR-102, ADR-060 |
| [ADR-108](ADR-108-entrances-are-verbs-not-objects.md) | **入口は動詞であって対象ではない — 「動詞 × 対象」の直積をヘッダから畳む** | Accepted (実装済み — 常設入口を desktop 7 / mobile 7 へ (ファイル系 6 → 2・始める系 5 → 1)、対象は宣言表の引数。ratchet は**超えても下回っても** fail し、母集団は `Header.jsx` の JSX から構文で導出。種ごとの宣言表 5 つが未宣言の値で throw。Node Editor は `toggle-surface` へ再分類。条件つき表示の入口は**消滅** (消さずに理由つき disabled) し、入口の個数が接続状態の関数でなくなった。Tour には入口が 1 つも無かったので再入口を新設。48 行すべてに住所が付き、被覆は検査が数える) | 2026-08-02 | ADR-089, ADR-051, ADR-047, ADR-063, ADR-065, ADR-050, ADR-060, ADR-100, ADR-106, ADR-102 |
| [ADR-109](ADR-109-a-deferral-is-a-declaration-not-a-memory.md) | **残しは宣言であって記憶ではない — 「後でやる」に母集団と満期を与える** | Accepted (実装済み — IA Phase 6。残しを id / 所在 / **満期条件** / **ticket** / lane を持つ登録簿 `docs/DEFERRAL_LEDGER.md` の行にし、母集団は登録簿ではなく**語彙から導出**して「宣言外の残しの個数」を ratchet で縛る (31 箇所 — 超過も下回りも fail)。満期は**コード側 `PROVISIONAL_UNTIL` と登録簿の `満期=ADR-NNN` の両方**を機械が読み (指す ADR が Accepted になったら fail)、逆向きにも問う (実在しない残しの宣言 = 0 件)。**D6 は実装中に追加** — D3 は半分しか実装されておらず、登録簿の満期 10 行は全部散文だった = 力学 1 が成果物の中で再生産されていた。機械が読めない満期 6 件は第 2 の予算として宣言 (嘘の trigger は満期が無いことより悪い)。実測 4 件 — 満期切れの `PROVISIONAL_UNTIL` (写し 7 箇所)・ADR-060 の完了済み宣言 3 件・段を持たない項目 2 件・宣言を持たない残し 1 件。原則 #31 の 3 例目で、原則は足さず**検査だけ**を足す。`pnpm test:deferrals` が CI gate) | 2026-08-04 | ADR-102, ADR-103, ADR-100, ADR-106, ADR-107, ADR-108, ADR-060, ADR-098 |
| [ADR-110](ADR-110-grasp-is-a-check-beside-its-subject-not-an-object-of-the-floor.md) | **把持探索は場の対象ではなく、主語の隣の検査である — 動詞を失った行の再分類** | Accepted (実装済み — IA Phase 5.1。`FLOOR_TARGETS` を 3 行にし `grasp` は `RETIRED_FLOOR_TARGETS` へ**移設** (行き先 = N パネル)、器を開かない引数を呼び出し閉包から数えて 0 個、入口の個数は desktop 7 / mobile 7 のまま **ratchet を書き換えずに**通った。選択が無いときの経路は N パネルの**常設**スロットが理由つき disabled で持ち、理由は `GRASP_BLOCKED_KIND` の 2 種 (未選択 / ロボット 0 台 — ADR-090)。**実装で変わったこと**: 前提の住所は `REQUIRES` ではなく `EntityScopeChecks` — ヘッダから出た以上そこに足すと空回りする宣言が 1 行できる。基数を渡さない呼びは throw する) | 2026-08-04 | ADR-108, ADR-105, ADR-106, ADR-090, ADR-099, ADR-107, ADR-109 |
| [ADR-111](ADR-111-the-outliner-has-a-semantic-side.md) | **Outliner に意味側を与える — 選べるのにどのナビゲータにも載っていない実体を無くす** | Accepted (実装済み — IA Phase 3.6。幾何 ⇄ 意味の側 (`outlinerSide`、基数は常に 1)、意味側の住人は共有変数だけ、行は**窓** (`onSelectVariable` を通るので入口は 5 verb のまま — `SelectionOwnership.test.js` は**作り直さずに**通った)、住所を持たない選択可能種 **0 個** を `SELECTABLE_KINDS` から導出、意味側の 0 は 2 種 (`SEMANTIC_SIDE_KIND`)。**実装で 2 件の第二の源が出た**: 「文書を採ったか」の導出がヘッダと 2 箇所に書かれていたので `documentAdopted` へ畳み、`contextSetVars` が場の中だけの書き手だったので文書の辺へ移した — 後者は `contextEnd` が `variables: []` を書いていた ADR-105 D4 型の欠陥を含み、**捕まえたのは e2e**) | 2026-08-04 | ADR-107, ADR-099, ADR-105, ADR-103, ADR-106, ADR-109 |
| [ADR-112](ADR-112-the-document-intake-address-becomes-permanent.md) | **文書の入口の住所を恒久にする — 満期の過ぎた暫定を、更新ではなく決着で畳む** | Accepted (実装済み — IA Phase 5.2。`DocIntakeLayer` を恒久住所として確定し、`PROVISIONAL_UNTIL` は**更新ではなく削除** (repo 内 0 個 — `pnpm test:deferrals` が数える)。覆う範囲の規則を**面の名前から役割へ** (`OVERLAY_COVERAGE_BY_ROLE`、未宣言の役割で throw)、空ける幅の所有者は `EdgeOccupancy.transientOverlayLeft`。**実装で D2 の粒度が変わった**: 可否は「いま見せている面」ではなく「器が**見せうる**面」— 側を切り替えるスイッチ自体が覆われる側に住んでおり、覆った瞬間に規則を発動させる手段が消えることを e2e が示した (原則 #16)。教訓: 満期は他人の判断に相乗りせず、自分の問いが決着する事象に置く) | 2026-08-04 | ADR-106, ADR-108, ADR-111, ADR-051, ADR-063, ADR-109 |
| [ADR-113](ADR-113-one-claim-on-the-screen.md) | **画面を占めるのは 1 つ、順序を決めるのも 1 つ — 枚数に欄を与える** | Accepted (実装済み — IA Phase 5.3。出所はユーザー報告 2 件。帰属の述語 `isInsideDropdown` を唯一の所有者にし再実装 **5 箇所**を回収 (`IDENTITY_RULES` に `scope:'all'` を導入して `.jsx` も走査 — 初回実行が想定外の 3 実装を名指しした)、判定を `pointerdown` へ (`click` では React の flush 後で押された行が既に DOM から外れており、**切り離されたノードはどこにも含まれない**)、`⋯` の段を 1 つの union へ (閉が段を持てない = `{closed, verb}` が表現不能)、`home` + `templateGalleryOpen` を `uiStore.stage` **1 欄**へ統合 (2 枚同時が表現不能・解放は主張の同一性つき)、段 (`stage`/`coach`/`dialog`) と z を `ScreenClaim` へ集約し面が自分で z を選べないように (`ImportModal` の `zIndex:10000` を発見)、母集団を走査から導出する census で**宣言外の面 0 枚**と**自分で z を選んだ面 0 枚**の両方を数える。報告 1 行目は実測の結果**仕様どおりの動作**と決着し、2 ギャラリーの語彙の作り分けは DEF-011 として宣言) | 2026-08-04 | ADR-089, ADR-051, ADR-108, ADR-105, ADR-102, ADR-109, ADR-103, ADR-100 |

| [ADR-114](ADR-114-reachable-is-not-declared.md) | **届く入口の個数は、宣言された入口の個数ではない — 端の幅と自由度に欄を与える** | Accepted (実装済み — 出所はユーザー報告 3 行 (N パネルしまえない / ダブルタップのズーム誤爆 / Pan できない)。3 件とも「*在るもの*は数えられ、**在らないもの**に欄が無い」同じ形だった。上端は高さだけが資源として扱われ**幅に所有者が居なかった**ため、mobile ヘッダが 438px を要求して 320〜393px のどの実機幅でも N トグルが 1px も画面に無く、DOM には在るので `HeaderEntranceCensus` は 7 個を数え緑を出していた (**2 度目** — `ui_layout.md` の "Mobile Header Overflow" が同じ症状を Map ボタンへの 1 箇所のパッチで直しており、ADR-103 の退役でパッチごと消えていた)。`TOP_EDGE_OCCUPANTS` + `MIN_SUPPORTED_WIDTH` で予算を宣言し、描画も同じ表を読む。カメラ自由度の母集団を `CAMERA_DOF` 側に置き **pan の割当 0 個**を可視化して `DOLLY_PAN` へ。`dblclick` を候補に降格し純粋述語 `acceptDoubleTap` が距離・オービット・空振りで却下 (空振りは `_focusSphere` のシーン全体フォールバック = 最大のカメラ跳躍だった)。backdrop を drawer から導出し `_toggleNPanel` を唯一の入口に。狭レイアウト述語の 4 実装を `isNarrowViewport` へ畳み、入力の粗さは `hasFinePointer` と名前で分ける。検査自体が一度素通りした記録つき — e2e 初版は `header.children` を測っており、flex-shrink するラッパのせいで負の対照が緑を出した) | 2026-08-06 | ADR-108, ADR-105, ADR-106, ADR-104, ADR-103, ADR-102, ADR-100 |
| [ADR-115](ADR-115-a-printed-number-is-not-a-check.md) | **印字された数は検査ではない — 観測トレーラの欠測に境界と予算を与える** | Accepted (実装済み — 出所は「コミットから傾向を評価して」という指示で、**傾向より先に母集団が壊れていた**。ADR-092 §4 は「塞げない穴は数える」と正しく決めたが、`metrics:commits` を読む step は**どの workflow にも無く**、印字される数を誰も見ていなかった — 「数える」と*宣言したこと*と*読む機械が在ること*は別の事実 (ADR-109 D6 の再生産、今回は観測装置自身)。実測 56 件中 **14 件が無トレーラ** (被覆 75%)。欠測はランダムでなく連続ブロックで、`claude/ia-redesign-1xzsxz` は 1 本目だけ刻まれ以降 10 本が 26 時間欠測。機構はスクラッチ repo で再現 (commit のみ ✅ / `commit && push` 連鎖 ❌ / push 済みブランチの 2 本目 ✅ — **コマンドの形だけが効く**)。ブロック状になる理由は穴ではなく**抑止の粒度**で、欠測はコミット単位・助言はセッション単位 1 回だった。`check-commit-trailers.mjs` が 4 つを問い、母集団の境界は**書き手を追加したコミット**から導出する (日付で近似して同日の導入前 3 件を数え 71% と誤った — 実際に踏んだ)。**空の母集団は「全件 OK」と区別できない**ので境界の決定不能を fail にし、gate の checkout を `fetch-depth: 0` に。穴は塞いでいない — 数えただけ (塞ぐ側は DEF-012 / ADR-116)) | 2026-08-08 | ADR-092, ADR-100, ADR-102, ADR-103, ADR-109, ADR-114, ADR-116 |
| [ADR-116](ADR-116-stamp-at-commit-creation-not-after.md) | **観測トレーラはコミット生成時に刻む — amend の後追いをやめる** | Accepted (実装済み — 書き手を PostToolUse + `--amend` から repo 管理の `.githooks/prepare-commit-msg` へ。ADR-092 が git hook を却下した 2 理由 (transcript を知らない / 人間のコミットにも付く) は、**PreToolUse が刻むための根拠だけを置く**ことで両方解けた — 「刻む」と「根拠を持つ」を 1 つのイベントで満たそうとしていたのが元の制約だった。コミットオブジェクトの前に走るので連鎖が無関係になり、amend しないので SHA も動かない。**実装で 1 点変わった**: 予備経路 (PostToolUse) を消さず残置 — `core.hooksPath` が張られていない clone では 主経路が走らず、消すと*何も刻まれず今より悪くなる*。冪等なので二重刻印しない。連鎖の助言も消さず**条件を狭めた** (hook が張られていない clone だけ。張られていれば誤発動 — 核 §6 シグナル(b))。`--amend` は git から判別できない (`--amend` も `-C` も `$2=commit`) ので、コマンドの意図をマーカーで運ぶ — 負の対照で基準を誤ると `feat/none` に化けることを確認。ラボ実測 4 形: 連鎖 → 刻印済 / 人間のコミット → 刻まない / 600 秒前のマーカー → 刻まない / マーカーは使い切り。`core.hooksPath` は SessionStart hook が張り、**既に別の値が在れば奪わない**。**実装当日にもう 1 件踏んだ**: マーカーを書く `node -e` がモジュールのパスを argv で渡しており、`isMain` (`import.meta.url === file://${argv[1]}`) が真になって CLI が起動、マーカーは一度も書かれず**主経路は最初から動いていなかった** — それでも予備経路が全部拾うので**被覆率は 100% のまま**だった。ADR-115 の計数は結果を数えており経路を数えないので、主経路の死は原理的に見えない。気づけたのは予備経路の告知 (「SHA が変わりました」) が毎回出ていたからで、検査ではない (= ADR-109 D3 と同じ形: 宣言は在る、読む機械が無い)。修正はパスを env で渡すことと、`scripts/prepare-commit-msg.test.mjs` で **hook を実際に走らせる** 8 件 — 一時 repo に `core.hooksPath` を張り、連鎖 / 人間のコミット / 古いマーカー / 使い切り / `--amend` の基準 / マージを焼いた) | 2026-08-08 | ADR-092, ADR-115, ADR-100, ADR-102 |
| [ADR-117](ADR-117-a-search-needs-an-object-and-a-demo-needs-no-backend.md) | **探索には掴む対象が要り、デモにはバックエンドが要らない — 死んだ主経路と、配れる静的スタブ** | Accepted (実装済み — 出所は「grasp まで最速で着く UX シナリオ + フロントだけの簡易ロジック」という依頼で、スタブを書く前に**主経路が死んでいた**。4 件が直列: (1) request に `target` が無く `core/` は target 不在を空サンプルに落とすので UI からの全実行が `candidatesGenerated: 0` — 0 は正しい形の答えなので何も赤くならず、しかも 0 件分岐は「レイアウトの形状を確認せよ」と *request が運んでいない幾何*について指示していた。(2) 重みが `{reach, clearance}` でソルバの登録名 (`reach_margin`/`approach_clearance`/`grasp_stability`) と一致せず、`evaluate_objectives` は未知名を黙って捨てるので全候補が `totalScore: 0.0` — スライダーは動くのに答えが変わらない。(3) starter `cell_robotics` は "Robot Cell" を名乗りながらロボットの CoordinateFrame を1 つも持たず、ADR-085 の無フォーム入口は Run が閉じたパネルへ着いていた — 既存 e2e は `toBeVisible()` しか見ておらず *到達*は焼かれ *使えること*は焼かれていなかった。(4) 直しても現れず、`_reconstructEntity` が `parentId == null` を「親が見つからない」と読んで **world 親のフレームを全部捨てて**いた(`skipped++` — 誰も読まないカウンタ)。ロボット base は構造上 world 親なので、文書が宣言しているロボットについてgrasp が「シーンにいない」と言っていた。**どれか 1 つでも残れば主経路は死んだまま**。対策は D1 対象宣言(解法ではなく宣言 — 前例 ADR-084 §2。N で先頭へ倒さない = ADR-090 と同型) / D2 objective 語彙を 1 箇所にし**母集団を `core/` のソースから導出** (書き並べたら `place-list` — ADR-102) / D3 world 親の取り込み + 親→子整列 / D4 `mocks/graspStub/` の transport double。静的配布が要件なので dev middleware は落選、SW より軽い `fetch` 差し替えで**`Response` は本物**を返し status 分岐は出荷コードが走る。スタブは response 契約の**第二の生産者**なので`core/` と同じ縛り (schema・contractVersion・ファネル恒等式・段の排他・vacuous gate) を受けるが、**数値の一致は求めない** (求めれば虚構を凍結する)。GitHub Pages はスタブレーンでビルド = インストール無しで触れる URL。**混入しないことは dist/ を実測して焼く** — 「フラグの陰だから tree-shake される」は bundler の挙動についての主張で、書いた直後に実際に落ちた (`import.meta.env?.` の optional chaining が define 置換を外し、スタブ全体が遅延チャンクで製品ビルドに載っていた)) | 2026-08-08 | ADR-085, ADR-090, ADR-084, ADR-102, ADR-100, ADR-115, ADR-116, ADR-060 |
| [ADR-118](ADR-118-a-hand-is-a-kind-and-the-face-follows-it.md) | **ハンドは種別であり、掴む面はハンドに従う — 吸引を一級にし、near-miss の量を名乗らせる** | Accepted (実装済み — 出所は「grasp の意味にエア吸引ハンドの想定も入れて。ひとまずチャックと吸引ぐらいで」の 1 行。契約の `gripper` は閉じたオブジェクトで `required:["maxOpening"]`、説明文も "parallel-jaw" と名乗り、**平行ジョーが型として焼き込まれていた**。二つのハンドは測る量が違う (ジョーは対向面の距離、吸引は接触点まわりの平坦パッチ) ので、平らに同居させると「開口 0 の吸引ハンド」が表現可能になる → D1 `gripper` を kind 判別 union へ (ADR-060 が pose に与えた統治を request 側へ)。**kind は required** — optional 追加の先例 (ADR-083/084) は*追加*の話で、既定へ倒せる欄を作ってよいという意味ではない (吸引を宣言したのに幅で判定される嘘は、応答が正しい形をしているぶん最も気づきにくい)。D2 `openingNearestMiss` は**閉じた層の required** で名前も単位も平行ジョー専用だったので、種別つき `graspNearestMiss` へ置換し **contractVersion 4→5**。段階案 (吸引は null) は却下 — メーターがチャックには在り吸引には無い差が理由なしに生まれる。D3 **掴む面を kind から引く** — これは正しさの修正で、`core/` は対象幅を「サンプルの閉じ軸射影の広がり」として測るのに ADR-117 の上面 3x3 は `d/2` しか張らず、幅 300mm の箱が 150mm として評価され**閉じられないハンドが通っていた** (ちょうど 2 倍楽観的)。吸引には上面こそ正しい面だったので、欠陥は「雑」ではなく**種別を言わずに片方だけを想定していた**こと。D4 `core/` に naive 吸引ゲート (既存の `surfaceSamples` で計算できるので対象側の契約は不変)。**実装で 2 件踏んだ**: (a) 吸引ゲートの初版はカップが面の縁からはみ出しても不足量 0 を返した — パッチは*平坦でないサンプル*だけでなく**面がそこで終わること**でも切れる。(b) ADR-117 のスタブが grasp 以外まで BFF を名乗っていた — `/auth/token` に答えるだけで `bffConnected` が真になり WS ジオメトリチャネルまで進む。**スタブレーンを有効にして全 e2e を流したとき初めて**露見 (無関係な smoke 3 本が落ち、フラグ無しでは通る)。ADR-117 では grasp の 7 本しか流していなかった) | 2026-08-11 | ADR-081, ADR-060, ADR-117, ADR-090, ADR-084, ADR-100, ADR-102 |
| [ADR-119](ADR-119-a-target-is-a-contract-and-where-to-grasp-is-an-input.md) | **掴む対象は契約であり、どこを掴むかは入力である — 無宣言の主役に契約を与える** | Accepted (**D1 / D2 / D3 とも 2026-08-14 実装済み** — 本文が決めた順序どおり前半から。`target`/`obstacles`/`sampling` をリクエストスキーマへ宣言し、各オブジェクトは閉じたが**宣言そのものは open のまま**にした (スキーマ自身が「Layout DSL が詳細を統治する」と書いている意図的な設計で、閉じると統治が移る)。この非対称をテストで焼いた — `hardConstraints` は通り `target.graspHint` は落ちる。`sampling` は二択のうち「載せる」しか成立しなかった (`core/` が既に読み templates が既に送っている以上、対象外宣言は事実に反する)。検証は走っている 2 端で 8 本。contractVersion は 5 のまま。**実装中に 3 本目の端が死んでいるのを発見 (DEF-029)** — `packages/grasp-contract` 自身の conformance suite は script が在るのにルートからも CI からも呼ばれず、走らせると ajv strict でコンパイルすら通らないため、example の `gripper` が `kind` を欠いたまま v5 に取り残されているのを誰も見ていない (宣言は在るが読む機械が無い = ADR-115 と同型)。直すと response スキーマに触れて版上げになるので次の意図的な bump に相乗りさせる。判断は未了 (D2 の UI が未設計) なので Status は上げず、実装先行は `DRAFT_WITH_IMPLEMENTATION` に宣言。ADR-117 が載せた `target`/`obstacles`/`sampling` は `additionalProperties:true` の隙間を素通りしており、**探索が成功するかを決めている当の欄が契約を持たない唯一の欄**だった (原則 #29 の二状態のどちらでもない)。D1 現在の形をそのまま宣言 (request 側なので版上げ不要)。D2 Layout DSL の Solid に任意の把持フィーチャを足して「どこを掴むか」を宣言可能にする — 把持点はワークの機能に依存するので幾何から導けるとは限らない。**宣言していないことと宣言された「どこでもよい」を別状態に**する (原則 #31)。D3 宣言が勝つ 1 方向でマージしない — マージすると「ここ以外は掴むな」が静かに広げられる。UI は未設計 (面や領域を指すのは ADR-099 の選択モデルの外側)) **D2/D3 は同日 ADR-128 が閉じた**: 本文が「この ADR では決めない」と残した UI の所有者を ADR-128 が決め (入力はパネル内のチップ、確認は 3D)、把持フィーチャの語彙・文書への書き込み経路・検査が同じ PR で入った。実装で本文の想定が 2 つ動いた — (a) 状態は 2 つではなく **4 つ**必要だった (読めない宣言を `derived` へ倒すと「宣言したのに無視された」= D3 が禁じる嘘になり、しかも下流では`candidatesGenerated: 0` という*正しい形の答え*になる)、(b) **宣言は ADR-118 の欠陥を正面玄関から連れ戻せる** — `core/` は把持幅をサンプル点の広がりから測るので、平行ジョーで 1 面だけ宣言すると幅がほぼ 0 になり閉じられないハンドを通す。対向面を黙って足すのは D3 のマージ禁止に触れるので gap として止める。**宣言を足す行為は、導出が守っていた不変条件を無効化しうる**。| 2026-08-11 | ADR-117, ADR-118, ADR-099, ADR-084 |
| [ADR-120](ADR-120-unevaluated-is-not-zero.md) | **評価できなかった objective は 0 点ではない — 分母に居座る空位を外す** | Accepted (**全 Decision 実装済み** — D2/D3 は 2026-08-12 スタブ + クライアント、**D1 は 2026-08-14 実ソルバ** (DEF-013 決着 — `GREP:` trigger が発火した最初の例)。**D1 の実装で分かった最重要点: D1 だけでは主症状が消えない** — `core/` は `reach_margin` を*評価済みの 0.0* として返しており、鍵が在る以上「鍵が無いとき分母から外す」分岐に一度も入らない。欠けていたのは D2 の `core/` 側で、**何をもって「評価しなかった」とするか**が書かれていなかった。答えは**絶対基準の不在** (`NormSpec.normalize()` が退化と無限の両方で `None` を返す) で、判定は 1 箇所。決定は在ったが発火条件がどこにも無かった形 = ADR-109 D3 / ADR-115 と同型。**3 人目の生産者**も出た — `recommendation/ranking.py` の `weighted_average` に同型の欠陥が独立に住み、そこでも*意図*が docstring に書かれていた (propose レーンなので契約違反ではないが同じ規律で直した)。実測: 既定重みでスコアは 1.4286 倍 (分母 2.0 → 1.4)、分子が同一なので**順位は不変**。**GSN 側の穴を記録** — DEF-013 の GSN ノードは solution の隣の `assumption` だったので、支えが 0 個の goal だけを数える G2 の母集団に入らず、片付けても負債 30 は動かない (Yellow Cards に 1 例目)。`weighted_sum` は評価できなかった objective を**分母に満額・分子に 0** で扱い、docstring はそれを意図として書いているが、**「評価不能」と「0 点」を同じ数に潰している**。実害はもう出ていて、`reach_margin` はリーチ範囲未宣言だと常に 0 = 「余裕ゼロ」ではなく「測っていない」なのに totalScore を押し下げ、契約が明言する「絶対基準なのでリクエスト間で比較可能」が静かに壊れている。ADR-105 が場の集約で潰した「未検証と問題なしの混同」のスコア層版。D1 評価できた重みだけで加重平均。D2 評価しなかったことは**鍵の不在**が表す (閉じた層に欄を足す案は ADR-060 の統治で却下)。D3 クライアントは 0 のバーではなく「測っていない」と描く。**着手順はスタブ → クライアント → `core/`** — `core/` を先に直しても、スタブが `reach_margin: 0` を載せ続けるかぎり画面では確認できない (フロントは `plan{}` を集めないので既定の実行ではリーチ範囲が常に未宣言)。**実装で分かったこと**: 同じ欠陥が二人目の生産者 (`mocks/graspStub/solve.js`) にも独立に居て、そこでも*意図*がコメントに書いてあった — 意図が書いてあるのに値が嘘をつく形は、書く瞬間に問われる場所が無いかぎり再生産される (原則 #19 Q3 → 成果物は検査)。提示側の母集団は**要求した重み**で、返ってきた鍵ではない — 不在にはノードが無いので、在るものを辿る数え方では原理的に出てこない (原則 #31)。**ADR-121 の前提** — 先に入れないと重心不在で全候補が不当に低く見える) | 2026-08-11 | ADR-105, ADR-060, ADR-121, ADR-081, ADR-117, ADR-100 |
| [ADR-121](ADR-121-centre-of-mass-is-declared-estimation-is-a-lane.md) | **重心は宣言された事実であり、推定は別レーンである — 仮定を値に化けさせない** | Proposed (未実装 — ADR-118 と同じ PR で起票。ADR-120 が先に要る。いまの `grasp_stability` は進入ベクトルと法線の内積 = 正対度しか見ておらず、偏心した対象を端で掴む危うさが原理的に表現されていない。ADR-118 で吸引が入ったことで、重心は「あったほうがいい」から**「無いと吸引の判定が成立しない」**へ格上げ (カップ軸からのずれがモーメントになりシールを剥がす)。D1 `target.centerOfMass` を kind 判別 union (`measured` / `assumedHomogeneous`) で足す — 値と一緒に**出所**が運ばれる。D2 不在のとき**図心へ黙って倒さない** — 倒すと ADR-090 の「原点に立つ無限リーチの幽霊ロボット」と同じ形で、仮定が値に化けて画面が実測と区別できなくなる。D3 材質・寸法・類似からの**推定は propose レーン** (ADR-056/077 の動詞境界)、ワイヤに乗るのは決定された事実だけ。D4 `grasp_stability` の意味は変えず `com_offset` を別 objective として足す — 書き換えると既存の応答値が黙って変わる) | 2026-08-11 | ADR-118, ADR-120, ADR-056, ADR-077, ADR-060, ADR-090 |
| [ADR-122](ADR-122-pickable-and-yield-are-two-questions.md) | **「そもそも取れるか」と「何個取れるか」は別の問い — 1 つのドアを共有していた** | Proposed (未実装 — ADR-118 と同じ PR で起票。**発見が 2 つ**: (a) 把持ゲート (`NaiveParallelJawGraspChecker` / `NaiveSuctionGraspChecker`) は**ロボットを一切要求しない**のに per-candidate パイプラインのリーチ→IK の後ろに住んでおり、フロントは ADR-090 のゲートでロボット無しのリクエストを送れない — **2 つの問いが 1 つのドアを共有していて、幽霊ロボットの穴を塞いだときに形状 x ツールの問いも一緒に塞がった**。(b) ADR-078 のシーン層と `POST /pick-sequence` は**実装済みなのに誰も呼んでいない** (BFF ルート無し / クライアント無し / 中立スキーマ無し)。D1 pickable を独立した問いに (ADR-090 は緩めない — 同居させたことが原因なので)。D2 pick-sequence に契約と入口を与える。D3 **配置は宣言せず集合から導出する** — 一度採った二軸案 (姿勢の出所 x ピックが乱すか) を畳んだ。崩れるかどうかは状態変化ではなく「ワーク同士が被って見えなくなる」「ピック時に互いに干渉する」で、どちらも配置された集合の組み合わせから導出でき、しかも既に `derive_obstacles` と`viewable_entities` が導出している。導出できるものを宣言すれば第二の源になる。D4 歩留まりは決定的レーンの外 — 実験計画 + 統計的計測で、ソルバの入力に戻さない (原則 #24 の閉路を作らない)。独立した確率問題として置くことで個々の問題が疎になる) | 2026-08-11 | ADR-078, ADR-090, ADR-081, ADR-118, ADR-060 |
| [ADR-123](ADR-123-a-deferral-is-not-written-in-one-notation.md) | **残しは一つの記法で書かれない — 第二の登録簿を畳み、Draft の除外を解く** | Accepted (実装済み — 出所は「ADR 未着手や、判断を残したものを教えて」→「管理面の見直し」。ADR-109 の 5 つの問いは緑だったが、**緑の外側に統治されていない領域が 3 つ**あった。力学 1: 母集団を残しの*語彙*から導出したのに、repo で最大の残し register (`docs/ROADMAP.md`、生きた行 **29**) は絵文字の優先度表と英語の "Backlog" という**別の記法**で書かれ、語彙ヒット **0 件**で一行も母集団に入っていなかった — 語彙を 1 語ずつ足す経路では永遠に届かない。ADR-015 の「Priority TBD after Phase C」は Phase C 完了 (2026-04-15) に満期が来ていたのに 4 か月誰も判定しなかった。力学 2: Draft 除外の**根拠が 3 本中 2 本で偽**だった (ADR-043 は Phase 1〜4 実装済み、ADR-046 は `src/context/` 一式 — 実物が台帳を追い越していた)。力学 3: **言及と実装は grep で区別できない** — 「Draft を参照する `src/` ファイルの個数」は ADR-044 で偽陽性を出す (5 ファイルは*言及*で、名指しされた `FunctionMatcher.js` 等は 1 つも存在しない)。力学 4: #73 で GitHub (`closed/completed`、閉じた PR 0 件) / ROADMAP (🟢 生きている) / コード (React 移行のついでに部分実装) が**三様に食い違う**。D1 ROADMAP を凍結アーカイブへ。D2 register を 2 本に (登録簿 = 設計判断を伴う残し / Issues = 機能要望) し、**委譲行 DEF-021** で宣言 — ADR-109 D5 の警告は分けることではなく*黙って*分けることを禁じている、と読む。D3 Draft は「判断の未成熟」ではなく**判断の未完了**なので母集団へ入れ満期を持たせる。D4 Draft の実装先行を **Q6** で数える — **gate ではなく census**。gate なら ADR-046 は書けず、しかも今回の 2 件は実装先行ではなくStatus の上げ忘れなので 1 件も防げない。**検査は数え、宣言 (`DRAFT_WITH_IMPLEMENTATION`) が分類する**。D5 満期 trigger を `PATH:` / `GREP:` へ拡張。**実装で 3 件出た**: (a) 4 形目 `GONE:` が要った — DEF-008 の満期は「`DECLARED_GAPS` が消えたとき」で、「残しが片付いたとき」という最も普通の形が出現を待つ 3 形では書けなかった。(b) 「check-adr-status と同じ文法で読む」は**コメントに書いてあっただけ**で、実物は 4 方言のうち表形式を読めず ADR-027 / ADR-032 がマップから丸ごと欠落 → Q3 が「実在するのに存在しない」と報告。文法を `scripts/adr-status.mjs` へ切り出した。(c) その修正の**直後に** ADR-032 が header `Proposed` / 索引 `Accepted` / 実装 13 ファイルの三様ずれを起こしていたと判明 — 読めない方言はその ADR を検査の外に置く) | 2026-08-12 | ADR-109, ADR-102, ADR-103, ADR-100, ADR-115, ADR-015, ADR-043, ADR-046, ADR-032 |
| [ADR-124](ADR-124-a-ratchet-that-counts-prose-measures-documentation.md) | **散文を数える ratchet はドキュメント量を測っている — 指標を 0 へ向かえる数に戻す** | Accepted (実装済み — 出所はユーザーの 3 問「deferrals の個数は今後増えていくだけ?」「節を消せば決着、が分からない」「運用にフィードバックして。また起こるでしょ?」— 3 つとも当たっていた。**力学 1**: ADR-123 直後の宣言外 99 件の内訳は docs/adr 68 / docs その他 29 / **src 1** / scripts 0 で、コードに残る残しは 1 件。98 件は残しについて*述べている*文で、**片付けても減らず考えるほど増える** — 0 へ向かえない数に ratchet を掛けても予算ではなく体重計である。**力学 2**: ADR-123 は失敗形を 7 つ観測して機械に降ろしたのは 2 つだけ、残り 5 つを散文に置いて出荷していた (散文の規則は守られないと同じ ADR に書きながら)。**力学 3**: 「満期が来る」は「片付く」ではなく**検査が落ちる**で、前回の運用説明そのものが誤っていた。D1 `docs/**` は「残りの作業を宣言する見出し」の配下だけ数える (除外表を作らず**文書構造で**絞る。キーワードは見出し先頭 12 文字以内 — 後ろの語は主題ではなく修飾)。D2 **Q7** 別記法の register を数える (優先度表)。D3 索引と header の Status を突き合わせる。D4 Status を読む文法が 1 つであることを数える。D5 満期 trigger の意味を書き下し DEF-021 を一意マーカーへ。**指標を直したら指標が仕事を始めた**: 宣言外 99 → 1、初回実行で DEF-025 (`ADR-076 §Still deferred` に**5 か月**在った) を掘り出し、Q7 が 2 つ目の記法 (validation レポートの P1〜P4 表) を発見、D3 が実測 3 件の食い違い (ADR-007/009/036) を検出。**Q7 の初版は自分が防ぐはずの欠陥を再生産した** — 文字列 🔴🟡🟢 を数えて*言及*まで拾い 9 件を誤検出し、構造 (表の行) で絞り直した) | 2026-08-12 | ADR-123, ADR-109, ADR-102, ADR-103, ADR-100, ADR-115, ADR-032, ADR-076 |
| [ADR-125](ADR-125-an-obligation-belongs-to-the-event-that-fires-it.md) | **義務は、それを発火させる事象の側に置く — 条件つき退役を ADR ライフサイクルの欄にする** | Accepted (実装済み — 出所はユーザーの「条件付き退役も設計してほしい。これ、ADR 運用するライフサイクルで**必須**なんじゃない?」。正しかった。**力学 1**: 条件つき退役は corner case ではなく **5 回**起きており、しかも無関係な文脈にまたがる (ADR-112 の `PROVISIONAL_UNTIL` は満期が無言で過ぎ写し 7 箇所が同時に古くなった / ROADMAP §Phase S-4 の退役指示は誰も実行しない / ADR-103 の `DS_PENDING` は 3 リリース残った / ADR-100 の退役色 / ADR-060 の 5 項目は宣言だけが嘘を出し続けた)。**力学 2**: 検査は既に **2 回手書きされている** (`RETIRED_MODE_SHAPES` / `RETIRED_SELECTION_COLORS`) — 仕組みが無いのではなく、**思い出した人だけが書いていた**。**力学 3**: 語彙では届かない (削除動詞 × 条件マーカーの 2 要素で実測 87 hit、ほぼ全部が偽陽性 — `Fired when` やメソッド名の `delete`)。**力学 4 (根本)**: 義務が**消される側**に書かれていた。条件 A が起きるのは別の場所 (ADR の Status 遷移) で、**A が起きた瞬間に誰も B を読み返さない**。消される側は自分が死ぬべき時を知っているが、その時が来たことを知らない。D1 ADR ヘッダに `Retires:` 欄 (ADR-125 以降**必須**、「なし」も宣言。番地は登録簿の満期 trigger と同じ `PATH:` / `GREP:` 語彙)。D2 発火は Status 遷移で**両方向**を問う (Accepted なら在ってはならず、Proposed/Draft なら在らねばならない — 退役の腐敗は緑を出す)。D3 消せない退役は退役ではなく**残し**なので登録簿へ降ろす (`Retires:` は「履行済みの義務の記録」へ収束する)。D4 A がまだ ADR として無いときは登録簿が受け、満期を「A が起票されたとき」にする — DEF-020 が既にこの形。**この ADR 自身が最初の保持者**で DEF-026 を退役させ、変異で確認済み) | 2026-08-12 | ADR-124, ADR-103, ADR-100, ADR-109, ADR-112, ADR-060, ADR-017 |
| [ADR-126](ADR-126-a-deferral-that-is-a-claim-belongs-to-the-argument.md) | **主張である残しは論証木に住む — GSN の「宣言された未支持」に母集団・個数・満期を与える** | Accepted (実装済み — 出所はユーザーの「残しを GSN の要素として書いておけば忘れずに管理されないか。散文にすると忘れる。GSN 自体も hook にすれば良いのでは」。**半分は既にそうなっていた** — `support-exploring`/`support-unexplored` の goal が **30 個**あり、子 assumption に「未着手。決着させる検査は〜」と**残し + 満期が型のあるノードとして**書かれていた (語彙・記法の問題が原理的に無い)。**残り半分が欠けていた**: `gsn_tool.py` は支えが 0 で*未宣言*なら error にするが、**宣言済みなら永久に緑**。`check_artifacts` の hard error も `solution` の下だけで `assumption` の下は意図的に warning。つまり ADR-109 力学 1 (満期が無言で過ぎる) が GSN レーンで再生産され、個数の ratchet も無く、`report` の集計は `test:gsn` が走らせないので**印字ですらなかった**。さらに力学 3: 同じ事実が既に 2 か所 (DEF-013↔`RealSolverStillDividesByEveryWeight`、DEF-015 の 1 行↔exploring goal **3 個**) で、**粒度は GSN のほうが正しい** — 登録簿は自分の census を満たすために行を増やしていた。D1 種類で分ける (**知識的な残し = GSN / 義務 = 登録簿・`Retires:`**)。D2 `.gsn` の母集団を **ADR-126 以降必須**の cutoff で閉じる (`Retires:` と同じ判断。遡及しない — 11 本の穴は登録簿が受け続ける)。D3 宣言された未支持の**個数**を ratchet。D4 assumption に**登録簿と同じ trigger 語彙**で満期を書かせ、検査が現れた日に「exploring を solution へ昇格させよ」と落とす。**実装で 2 件**: (a) `support-` prefix で数えてはいけない — 第 3 のラベル `support-verified` は「支えが在る」= 逆の意味で、prefix 版は 30 を 57 と報告した (prefix は符号を持たない → 種を列挙し未宣言の種で落とす)。(b) 前段の grep が数えた「31」は 1 件多く、adr-105 の summary が散文で `support-unexplored` に**言及**していた — ADR-124 が名指しした欠陥が**それを書いた本人の計測に出た**。文法と評価器は `scripts/expiry-trigger.mjs` へ切り出し (GSN 側で同じ文法を書きかけて止めた — ADR-123 の欠陥の再生産だった)) | 2026-08-12 | ADR-125, ADR-124, ADR-123, ADR-109, ADR-115, ADR-102, ADR-103 |

| [ADR-127](ADR-127-a-known-structure-is-what-makes-a-closed-form-possible.md) | **閉形式を可能にするのは「構造が既知であること」— UR の解析解を実 IK として注入する** | Accepted (**`core/` 側 2026-08-14 / フロント配線 2026-08-14 (ADR-128) — DEF-030 決着**。出所はユーザーの「UR なら解析解が有名なので、IK も比較的簡単に入れられるのでは」。当たっていた — **新しい方向ではなく宣言済みの残し DEF-005 (ADR-081 Phase 4) の回収**で、材料は 3 つとも揃っていた: UR は閉形式で解け (Hawkins 2013 / ros-industrial)、寸法は `public/robot/skeleton_arm.urdf` が UR5e の公表 DH 値として既に宣言し、`IkSolver` Protocol は「実装は**解析的でも**よい」と継ぎ目を空けていた。動機は原則 #29 — `ikSolvable` は契約上「決定された事実」なのに中身は手首コーンの代理で、関節限界も特異点も見ておらず、**弱いことがワイヤから読めない**。D1 `robot.kinematics` を kind 判別の閉じた union に (kind が解法を選ぶ — 閉形式は既知の構造にのみ在る)。D2 **機種名ではなく寸法**を運ぶ (名前は解く側に対応表 = 第二の源を要求する)。D3 宣言があるときだけ切り替え、**壊れた宣言は素朴判定へ落とさず throw** (無言の格下げは嘘)。D4 関節限界の不在は「無限」ではなく「検査していない」。D5 代表解は決定的に 1 つ (段階0 が問うのは可解性なので 8 解を出さない)。D6 フランジ frame の規約は**宣言**であって導出ではない (`pose_codec` の候補 frame とは 180° 違う gauge)。contractVersion は 5 のまま (request 側の追加)。**実装で分かったこと**: 肘の平面を x-z で解いていて真値 1.100 に対し 1.149 が出た — **桁も符号も妥当なので値を見ても気づけない**。捕まえたのは FK 往復で、かつ FK 自身を DH から手計算で独立に固定していたから (両方を同じ思い込みで書いていたら緑になっていた)。θ6 も atan2 の符号でちょうど π ずれ、これも「もっともらしい角」だった。**自己整合な誤りは往復検査を通る** — 同じ理由で D6 の規約がフロントの描画と一致するかは `core/` 内では原理的に決着せず DEF-030 とした。フロント配線も同じ残しに乗るので当日は画面に届いていなかったが、**同日 ADR-128 が 3 項目とも閉じた**: フロントが宣言を送るようになり、6 数は URDF から**読み出す**ので比較すべき第二の写しが消え (引き受けた冗長そのものが無くなった)、D6 の規約は「アプリが描くフランジ面」という外部の証人に照合された) | 2026-08-14 | ADR-081, ADR-084, ADR-088, ADR-090, ADR-118, ADR-060 |
| [ADR-128](ADR-128-a-declaration-that-cannot-be-written-is-not-a-declaration.md) | **書けない宣言は宣言ではない — 掴む場所の入力面を決め、届いていない宣言を配線する** | Accepted (実装済み 2026-08-14。出所はユーザーの「ADR-119 の D2/D3 を閉じて、フロントまで届いていない UIUX を到達させて」。**同じ形の欠陥が 3 つ同時に開いていた** — どれも「バックエンドは正しい・テスト済み・到達不能」で、**生産する側からは見えない**種類である (URDF は正しく、欄は正しく、ソルバは正しい): (1) ADR-119 D2/D3 は「掴む場所を宣言できる」と決めたが宣言する手段が無かった、(2) ADR-127 の解析解 IK はフロントが `robot.kinematics` を送らないので一度も走っていなかった、(3) フロントが `plan{}` を集めないので `reach_margin` は永久に未評価で、**そのすぐ下に reach weight スライダーが載っていた** (UI の穴を回避するためにスタブへ専用シナリオが生えていた)。D1 入力面は**パネル内** — 3D で面を指すのは ADR-099 の選択所有者に触る話で、ADR-119 が切り離した線を越えない。代わりに**確認を 3D に返す**が、描くのは宣言の*意図*ではなく**ワイヤに載るサンプル点そのもの** (意図の絵は payload とドリフトしうる = ADR-117 が壊れていた形)。副産物として導出グリッドも出るので、「あなたはどこをとは言っていない」が**物に付いた絵**になる。D2 語彙は kind 判別の閉じた union、状態は **4 つ** (`derived`/`declared-anywhere`/`declared-faces`/`malformed`) — 前 2 者は*同じサンプルを出す*ので、点群を比べる検査では区別の消滅が見えず、検査は状態名について書く。D3 消去は `anywhere` の書き込みではなく**鍵の削除**。D4 権威は**文書**で書き手は既存の doc-edit 1 経路 (実行時パラメータだとリロード・エクスポート・undo で消える)。D5 平行ジョーに 1 面だけの宣言は **gap** — 対向面を黙って足さない。D6 運動学は **URDF から導出**し写しを作らない (DEF-030 は「一致の検査」を計画していたが、読み出しにすれば比較対象が存在しない)。D7 フランジ規約の照合先は**フロントが描く形** (`core/` 内では自己整合な誤った規約も往復を通る)。D8 リーチ範囲は宣言で、off = **鍵ごと省略**。**実装で分かったこと**: **宣言は ADR-118 の欠陥を正面玄関から連れ戻せる** — 「ここだけ掴め」は完全に自然な操作なのに、`core/` が把持幅をサンプルの広がりから測るせいで幅がほぼ 0 になる。**宣言を足す行為は、導出が守っていた不変条件を無効化しうる** (導出は「ハンドが触る面」を知っていたが、宣言は知らない)。そして **3 つの穴が同じ 1 箇所に落ちた** — 配線先はすべて `runGraspSearch` の request 組み立てで、「バックエンドは在るがフロントが宣言を送らない」は**場所を共有する欠陥のクラス**である) | 2026-08-14 | ADR-119, ADR-127, ADR-120, ADR-118, ADR-088, ADR-099, ADR-106, ADR-100 |
| [ADR-129](ADR-129-a-declaration-outlives-the-instance-it-was-written-on.md) | **宣言は、それを書いたインスタンスより長生きする — 配置・据付姿勢・掴む領域を文書の実体 (ref) に預ける** | Accepted (**実装済み 2026-08-14 — D1/D2/D3 を 1 つの MVP として。段 G-5 完了**。出所はユーザーの「後始末が良いように設計したい。これらはユーザ(タイプ)をキーとすべきエンティティなり属性です。下手にインスタンスに依存して後始末が悪くなるような設計をしたくありません」。**前段の観測は 3 件の欠落として並んだが、並べ方が間違っていた** — ワーク配置がループに入らない / 据付姿勢が契約に無い / 掴む領域が書けない、の 3 つは 3 機能ではなく **1 つの所有権の欠落の 3 つの現れ**である。力学は寿命の不一致: `L(配置) = L(セッション) < L(実体)` なので後始末は「たまたま一緒に消えた」に依存し、**偶然は検査できない**。しかも壊れ方が黙っている — ワークを動かして Run すると **古い位置での正しい答え**が返り、棄却ファネルにも出ない (原則 #31)。ADR-117 がこれを散文の既知の限界に書いてから 3 日で、同じ形の穴が 2 つ増えた (DEF-031 / DEF-032)。**初稿は前提を 1 つ誤っており、ユーザー指摘で撤回した** — 初稿は「`position`/`rotation` の受け皿は既に在り、無いのは線だけ」を判断の基礎に据えていた。事実としては正しいが基礎に据えると**規則が反転する**: 受け皿の有無を境界にすると「まだ名前の無い要求は書き戻せない」が規則になり、**要求が言葉になっていないから 3D で見せて確定・合意する**というこのアプリの前提そのものを禁じる (語彙が先に在るなら 3D は要らず、テキストと数値入力で足りる)。受け皿が在ったのは 2 属性についての**偶然**で、常態は無いほう。誤りの形は ADR-102 の「母集団を持たない表」と同じ — 手元の 3 件を数えて一般規則を書こうとした。D0 宣言の同一性は文書の実体 `ref`。**D0′ 記録は行為の側から取り、導出物から逆に推定しない** — ジェスチャは要求を述べる**行為**であり行為は主張である (`createEditAdmissibleCommand` が領域ドラッグを `stated` な主張として書き戻す先例 = ADR-104 力学 2)。これが ADR-054/055 の逆コンパイル禁止と**両立する**理由でもある: 禁じられているのは*導出物から源を推定すること*で、**行為をその場で記録すること**ではない — 境界は**行為か推定か**であって受け皿の有無ではない。D1 ジェスチャの**確定**は文書への書き込み (`ref` を持つ実体に限る。ドラッグ中は書かない = 中間姿勢は宣言ではない)。D2 `robot.baseOrientation` を契約に足す (request 側なので**版は上げない** — 次の意図的な bump は ADR-122 D2 のままで DEF-029 を相乗りさせない)。D3 掴む領域をパネルから (u/v の軸名は `inPlaneAxesOrThrow` から**表示するだけ**)。D4 **3 つで 1 つの MVP、分割しない** — 半分だけ真の規律は規律ではなく、それが今日の状態だから。D5 後始末の検査は**往復 (fixpoint) で書き、値では書かない**。**D6 名前をまだ持たない主張は MVP の外へ置くが「対象外」とは書かない** — 原則 #29 の二状態でいえば*契約あり*でも*明示的対象外*でもなく **まだ決めていない**で、対象外と書けば次に問われたとき「決着済み」に見える (初稿の誤りがまさにその形)。DEF-034 が受け、数えるのは主張の個数ではなく**判断が未了である 1 件**。却下: A シーンを探索時に読む (最も安いが**後始末では最悪**)、C 配置も数値欄で入力 (入力面が 2 つになる)、E 受け皿の有無を規則にする (初稿。規則が反転する))。**実装で本文の想定が 3 つ動いた**: (1) **前提が 1 つ足りなかった** — doc-edit は `_clearScene()` を通って**文書が作っていない実体を毎回消して**おり、D1 はそれを G/R の 確定ごとに起こす。しかも D1 自身が「`ref` を持たない実体は欠落ではなく**状態**」と 書いているので、**この ADR の前提が今日は成り立っていなかった** → **ADR-131** として 先に閉じた。「未検証」と宣言したコスト (選択・カメラ・ゴースト) は実測すると**保たれて おり**、壊れていたのは**挙げなかったほう**である。(2) **書き込みの順序が振る舞いの一部** — `_clearScene` は最初の await より前に走るので同期的で、確定の途中で書くと後片付けが 破棄済みの view を触って落ちた。分類と書き込みを分け、書き込みを confirm の最後へ (順序も検査で焼いた — コメントに書いた順序は次の編集者に見えない)。(3) **混在した選択は undo が 2 つに割れる** — 宣言済みと素の実体を一緒に動かすと doc-edit と MoveCommand の 2 エントリになる。**畳まない** (畳むとどちらかの種が undo を静かに失う)。D3 は本文の 読みどおり「呼び出しだけ」で済んだ) | 2026-08-14 | ADR-128, ADR-119, ADR-117, ADR-104, ADR-105, ADR-102, ADR-055, ADR-097, ADR-101 |

| [ADR-130](ADR-130-a-search-owns-its-subject-the-selection-does-not.md) | **探索は自分の主語を所有する — 選択は主語ではない** | Accepted (実装済み 2026-08-14。出所はユーザーが ADR-129 (段 G-5) の手順を実際に通そうとしたとき: 「対象を選ぶところで base tcp を持ったロボットを選択して、みたいなエラーが出るんだけどなんでだと思う？」。**手順が自分の次の一歩で自分のゲートを踏んでいた** — 掴む対象を選ぶには選択をロボットから 外すしかないのに、`EntityChecks` は選択が変わるたび入口の可用性を再評価し続けていた。力学 1: 主語に源が 2 つあり、**Run が読むのは片方だけ**。入口の可用性はビューポートの選択 (`nPanelData.robotRole`)、実際に解く相手はパネルの pick (`_selectedRobotId`) で、`openGrasp()` は選択を**一切読んでいなかった** — 入口が要求した宣言は通った瞬間に捨てられていた。力学 2: 出た文は**嘘**である (下のパネルは主語を保持し Run は通る)。原則 #11 が名指しする *無言の no-op* の**鏡像**で、阻まれていないのに阻止の理由を出す — 画面が仕事をしているように 見えるぶん議論しにくく、ユーザーは自分の手順を疑う。力学 3: `selectRobot` は 1 台なら選択と 無関係に解決し N 台なら null なので、**欠陥は 2 台目の日に初めて見える** (ADR-090 が同一性に ついて閉じた形が、選択の側に残っていた)。**コードを読んでも見えない**理由は、`EntityChecks` が読んでいないのが `context.grasp` = 「探索が生きているか」の欄だから — 読んでいない欄は無い欄と区別がつかない (原則 #31)。同型が一段上にもあり、「入口」という語が **入る前**の問いと**入った後**の状態を 1 行で受けていて、2 つ目には答えが無かった。D1 主語の権威は探索スライス (`context.robots.selectedId`)、選択は表示の写しであって premise では ない。D2 入口が主語を**書く** (`robotForFrameId` — base でも tcp でも同じロボット。ロボット以外の選択では**消さない**: 消すと「掴む対象をクリックする」が主語を黙って落とす)。D3 返り値を kind の union (`OPEN`/`BLOCKED`/`LIVE`) にし、`LIVE` は理由の欄を**持たない** — 「生きているのに阻止理由が在る」がこの欠陥そのものなので表現不能にする (原則 #2)。union を返すだけでは描く側が `available` の 2 分岐へ潰し直せるので、kind ごとの振る舞いを 宣言表にし**未宣言の kind で throw**。`LIVE` では阻止理由ではなく**主語**を述べる (未 pick は「選択が悪い」ではなく「まだ言っていない」)。D4 探索が生きていないときの判断 (ADR-110 D4 / ADR-105 D5) は **1 ビットも変えない**。D5 数えるのは在る文ではなく **出てはいけない場面で出る文の個数**で、母集団は画面ではなく `DECLARED_NPANEL_KINDS` + 「選択なし」から**導出**する。却下: A 行を消す (ADR-110 の判断を無言で取り消す)、B 黙って disabled (原則 #11 が禁じる無言のボタン)、C 選択を主語にする (選択は 1 つなのに 主語は 2 つ要る → 「いまの選択はどっちの役か」というモード変数が生まれ、ADR-103 が潰した 「軸を 1 変数に潰す」形の再生産)) | 2026-08-14 | ADR-110, ADR-105, ADR-090, ADR-129, ADR-103, ADR-108, ADR-102 |
| [ADR-131](ADR-131-a-regeneration-swaps-the-projection-not-the-scene.md) | **再生成は投影を差し替える — シーンを全消しするのではない** | Accepted (実装済み 2026-08-14。**ADR-129 D1 の前提として、実装前に書き込み先の経路を辿って見つかった** — doc-edit は必ず `applyContextDoc(regenerate:true)` → `importFromJson({clear:true})` → `_clearScene()` を通り、`_clearScene` は**全実体を dispose** する。戻るのは文書が宣言した実体だけなので、`ref` を持たない 実体 (Shift+A の箱・複製・輸入メッシュ・計測線) は **doc-edit のたびに消えていた**。実測: `ピックテーブル` を Shift+D で複製 (Outliner 12 行) → 掴む場所を宣言 → **10 行**。エラーも警告も出ない。**消滅には状態が無い** — 削除は正当な削除とまったく同じイベントを出すので、ログでも区別できず、*在るもの*を辿る検査は何も言わない (原則 #31)。今日これが稀にしか起きなかった のは doc-edit が稀だからで、**ADR-129 D1 はこれを G/R の確定ごとに起こす** — 頻度が上がったから 欠陥になるのではなく、頻度が低かったから見つからなかった。しかも **ADR-129 自身の前提と矛盾**して いた: D1 は「`ref` を持たない実体は欠落ではなく**状態**」と書いており、状態であるからには宣言の 書き換えごとに消えてはならない (ADR は「選択・カメラ・ゴーストが保たれるか未検証」と書いたが、実測すると選択とカメラは保たれ、消えるのは**実体そのもの**だった)。根は語の多義: 「再生成」が *入れ替え* (文書の読み込み・破棄 — 全消しが正しい) と *投影の差し替え* (宣言を 1 つ書き換える) の 2 つを指しており、**強いほうが既定**だった。D1 再生成は投影の外側を触らない。D2 母集団は `ref` では なく**投影の footprint** (`ref` で判定すると Origin CF や `ensureRobotFrames` の upgrade など**派生で生えたもの**を数え落とし、それを「未宣言」として保存して次の投影と id が衝突する)。D3 全消しは消えず、意味の合う場面 (読み込み・破棄) に残る — 2 つの意味を**分離**したのであって 片方を消したのではない。D4 再生成の入口を 1 本 (`_projectScene`) に畳む (規則を持つ経路と持たない 経路が並ぶと欠陥は必ず持たないほうに住む — ADR-097)。D5 link は両端が残るときだけ残る (片端の 消えた link は弱い link ではなく**壊れた** link)。D6 検査は「消えてよかったものの個数」で書き、逆向き (保存が投影まで覆っていないこと) も同じファイルで問う。却下: A `ref` で判定 (派生を数え 落とす)、C 退避して復元 (退避先が第二の源)、D 差分適用 (方向は正しいが過大)。**限界の宣言**: 部分的な取り壊しは `_worldPoseCache` / `_mountLocalPositions` / `_fixedJointTransforms` を id 単位で 掃除するが、**4 つ目の辞書が生えた日にここが静かに壊れる** — 個数を数える検査は持っていない) | 2026-08-14 | ADR-129, ADR-128, ADR-049, ADR-097, ADR-102, ADR-131 |
| [ADR-133](ADR-133-a-container-is-five-solids-with-a-declared-role.md) | **コンテナは5つの直方体 + 宣言された role — ワーク配置(整列/ばら)と障害物のbox化を front-only スコープで足す** | Proposed (`/whiteboard` セッションで確定。ADR-078 の「エディタ側への申し送り」+ ADR-122 D2/D3 のうち、BFF `/pick-sequence` 配線を除いた front-only 部分。Container を Layout DSL 専用 entity type にし、コンパイル時に containerRole (`bottom`/`wallXNeg`/`wallXPos`/`wallYNeg`/`wallYPos`) 宣言つき Solid 5枚 + workpieces バッチ由来の N Solid に展開 — `robotRole` (ADR-090) と同型の統治。障害物宣言を sphere から box (`kind` 判別 union、request側 optional 追加で contractVersion 不変) に拡張し、`core/` に `NaiveOBBCollisionChecker` を追加 (回転を考慮した OBB — scatter 配置の全軸ランダム回転ワークに AABB は緩すぎるため)。front は宣言のみ、干渉の解法は `core/` のまま (契約の "Declaration only" 境界は不変)。OBB を超える厳密な半空間・薄板干渉は DEF-036 へ切り出し) | 2026-08-15 | ADR-078, ADR-122, ADR-090, ADR-117, ADR-060, ADR-118, ADR-119 |
| [ADR-132](ADR-132-a-search-is-about-what-is-on-the-screen.md) | **探索は画面に在るものについて解く — 入口は文書を採らない** | Accepted (実装済み 2026-08-14。**出所はユーザーが実際に押したとき**: 「grasp search をすると、急に画面が切替わって、tcp教示点ピック、tcp教示点プレースと出るんだけど、これは何？もともとロボットと把持する対象の関係性をモデリングしてたけどリセットされちゃう」+「ロボットが初期シーンに存在するけど非表示になっているのが違和感。初期シーンで見えなかったら気付かなくない？それだったら最初からヒエラルキーにも存在しないのと変わらないような気が」。その 2 語は `examples/cell_robotics_context.json` の実体で、**シーンが壊れたのではなく別の文書が読み込まれて入れ替わった** — `openGrasp` が `!ctxService.loaded` を見て `quickStartExample('cell_robotics')` を呼び、それは `loadContext` → `_projectScene({preserveUndeclared:false})` → `_clearScene()` を通る。**力学 1: `loaded` は「失うものが在るか」の答えではない** — 読んだのは*文書*についての boolean で、失われたのは*シーン*の中身。シーンは文書なしで N 個の実体を持てるが**未宣言実体の個数には欄が無い** (原則 #31) ので、この分岐は一度もその問いを聞いていない。ADR-131 が `applyContextDoc` で同じ形を閉じ母集団 (`_undeclaredIds`) まで作りながら、**入口には持ち込まれなかった** (ADR-131 D3 の「全消しは意味の合う場面に残る」の意味は『ユーザーが**別の文書を採ったとき**』であり、ここはその動詞を選ばれてもいないのに借りていた)。**力学 2: 迂回には理由があった — 主語が 2 つの源から**。ロボットは live scene (`resolveRobots` + `worldPoseOf`)、掴む対象は文書 (`getCompiled().layoutDsl`)。**どちらを単独で読んでも欠陥は無く、並べたときにしか現れない**。帰結は 2 つ: シーンに直接置いた箱は最初から探索の対象になれない (だから入口に迂回が生えた)、ビューポートで動かしても文書を再コンパイルするまでサンプルが動かない — `_graspTargets` の doc コメントが**自分でそう書いて ADR を予告していた** (「closing it means deciding which side owns object geometry, which is an ADR, not a patch」)。**力学 3: 逆写像は 3 か月前から在り誰も呼んでいなかった** — `decompileLayout` (ADR-055) は fixpoint law までテストされて生産経路からの呼び出しが **0 件**。機構が在ることと読む機械が在ることは別の事実である (ADR-115 と同型)。**力学 4: 見えないロボットは基数 1 を基数 0 として提示していた** — boot が 1 台作り ADR-096 §Decision 3 がその既定を `explicit:false` と宣言していた。**どちらも単体では正しく、合わせると欠陥**で、ユーザーの指摘どおり*最初から存在しないのと変わらない*。D1 幾何は live scene の decompile から (両方の主語が同じ源に揃う)。D2 宣言 (`graspFeature`) は**文書が持ち主のまま `ref` で join** — シーンは宣言を運ばないので丸ごと採ると ADR-119 D3 の嘘になる。半分ごとに持ち主が違うことこそが事実である。D3 源は状態なので画面に出す (`sourceLabel` は**未宣言の源で throw**) — 「文書が何も宣言していない」と「文書を読んでいない」は同じ対象を生むので、源だけがこの 2 つを区別する (ADR-120 の鍵の不在と同型)。D4 quick-start を**ガードせず削除** — ガードされた破壊経路は条件 1 つで復活し、次に「速い開始」が欲しくなった入口は grep で見つける (ADR-102 の手)。**順序が逆だと改悪**である: 探索できるようにする前に入口を消すとユーザーは何もできなくなる。D5 boot は 0 台で起動 (`seed` / `entry` 引数ごと削除)。`ROBOT_BASE_SEEDED` は**残る** — 読み込んだシーンのロボットは他人のセルの家具である。D6 数えるのは「消えてよかったものの個数」(実体の**集合**差 — 個数比較は同数の別物への入れ替えに緑を出し、それがまさにこの欠陥の形) と「退役した形の個数」。却下: A 未宣言実体の個数で分岐 / B 確認ダイアログ (どちらも破壊は止まるが**源の非対称を残す**ので文書無しのシーンは依然探索できない)、C `resolveGraspTargets` をシーンから直接 (源が 3 つになる)、E シーンを丸ごと採る (`graspFeature` が全部消える)。**限界の宣言**: join の正しさは 1 例でしか見ていない (文書を読み込んだ状態で Solid を動かす実機回帰が無い — GSN の exploring goal が受ける)、孤児になった宣言を出す経路は未実装で **ADR-115 の形を自分で 1 つ作った** (DEF-035)) | 2026-08-14 | ADR-131, ADR-129, ADR-119, ADR-115, ADR-103, ADR-102, ADR-096, ADR-090, ADR-055, ADR-054 |
| [ADR-134](ADR-134-a-project-open-boundary-confirms-only-when-something-is-at-stake.md) | **プロジェクト境界の確認は、失うものが在るときだけ求める — テンプレ選択の無言リセットを塞ぐ** | Accepted (実装済み 2026-08-15。`/whiteboard` セッションで挙げられた 4 スレッド (A〜D) のうち Thread A。出所はユーザー観察: New Context (Template Gallery) でテンプレを選ぶと、未確定の編集があっても確認なしでシーンが破棄され undo でも戻せない — `ContextController.selectTemplate` → `_projectScene({preserveUndeclared:false})` → `_clearScene()` → `AppController._onContextLoaded()` の `commandStack.clear()`。「確認ダイアログ不要」はコード側のコメントが ADR-051 §7 を根拠に主張していたが、§7 の原文はデモ入口 (`ContextDemoController`、毎回無条件で確認) 向けの決定で、Gallery 側の「フッタで足りる」は Phase 2 実装時の別解釈だった (ADR-051 全体は supersede しない — その§7 の amend)。D1 `selectTemplate` の入口に `commandStack.canUndo` を条件にした confirm ゲートを 1 つ追加し、破壊的な読み込みを `_loadTemplate(meta)` に切り出す。`canUndo === false` (プロジェクト境界後にまだ何も undo 可能な編集をしていない) は今までどおり即座にロード (摩擦ゼロ)、`true` は既存の `showConfirmDialog` パターン (`ContextDemoController.enter()`、delete-robot/delete-frame ガードと同型) で確認を求め、キャンセルなら何もしない (ギャラリーは開いたまま)。同一性の所有者は新設せず既存の単一権威 `CommandStack.canUndo` を消費するのみ。却下: B 常に確認 (最多操作である「真っさらな状態から選ぶ」にまで摩擦を課す。Goal「失うものが無ければ追加の摩擦を課さない」に反する)、D 破棄実体数を文言に出す (`_clearScene()` の dispose 集合と undo スタックは別物で、二値判定で足りる Goal に対し過剰実装)。検証は `src/controller/ContextController.test.js` の 3 ケース (canUndo=false は即ロード / canUndo=true は confirm 経由でのみロード、cancel はロードしない / 未知 id はどちらの分岐でも確認もロードもしない) — `pnpm test:context` 558/558 green。副産物: `ContextController.js` の JSON import 4 本に `with {type:'json'}` を付け、この module を初めて bare `node --test` でユニットテスト可能にした (Vite の JSON ローダには影響しない — `pnpm build` で確認済み)) | 2026-08-15 | ADR-051, ADR-131, ADR-047 |
| [ADR-135](ADR-135-reachability-is-drawn-not-declared-the-discarded-ik-solution-rejoins-the-wire.md) | **到達可否は bool ではなく腕の形で示す — 捨てられている IK 代表解を契約経由で運ぶ** | Accepted (実装済み 2026-09-15。出所はユーザーの「本当にティーチング姿勢や把持姿勢がリーチするのか視覚的に確認したい」— 当初のリアルタイムティーチング要望から、対象は既存の把持探索フローの候補のみ・リアルタイムでなくて良いとスコープを絞って確定。**2 つの継ぎ目が既に揃っていた**: (a) `UniversalRobotsIkSolver.solve()` (ADR-127) は既に 6 関節の代表解を計算しているのに `ik_solvable()` が bool 一枚に握り潰して捨てていた (ADR-120 の「測らなかった値」と違い**測ったのに捨てている**)、(b) `RobotStage.setJointValues()` の doc は元々「a future grasp-contract response」を想定して空けられた継ぎ目。新しい解法ロジックは不要で「運ぶだけ」。D1 契約に `reachSolution` を kind 判別の閉 union で追加し `contractVersion` 5→6。D2 パイプラインが解を捨てずに持ち回る (FK 往復チェック必須 — ADR-127 の「自己整合な誤りは往復検査を通る」の教訓)。D3 関節配置を書く唯一の入口を新設。D4 ADR-059 の既存 ghost コールバックへ配線。**実装時に 4 点を改訂** (本文 §実装時に判明した差分): (1) 第二の枝は `unsolved` ではなく **`undeclared`** — IK で落ちた候補は短絡で `candidates[]` に載らないので「到達不可の候補」は構造的に存在せず、実際に要るのは「`robot.kinematics` 未宣言で誰も関節を決めていない」の宣言だった (素朴ソルバの占位解 `(angle,)` を `solved` で載せると**誰も決めていない腕**を描く)。型側にも `JointSolution(IkSolution)` を足し、分岐を長さでなく型で行う (原則 #2)。(2) D3 は **N=1 の設計**だった — 入口を `RobotStageSet.previewSolution(robotId, joints|null)` へ上げ、名指し以外の全 stage を rest に戻す (ADR-093 と同型、N=2 で焼く)。(3) `ROBOT_REST_POSE` は 4 関節しか列挙しておらず「rest へ戻す」が `shoulder_pan` を戻せなかった (**行を持たない 0** — 原則 #31)。(4) 配線先は panel ではなく `GraspController._syncGhost()` の**早期 return より手前**、退出時の rest は `disposeGhost()` 側 (原則 #32)。却下: B 解専用の別エンドポイント (真実が 2 源に分裂、§1.1)、C 対象なしの自由 TCP 姿勢チェック、D リアルタイムジョグ。台帳は `docs/STATE_LEDGER.md` の「候補の関節配置」「腕が描く関節配置」2 行。検証: `pnpm test:core` 179 / `pnpm test` 1293 / `pnpm test:contract` 30 / `pnpm test:stub` 33 / `pnpm build` すべて green。GSN 論証木は `docs/gsn/adr-135-...gsn`) | 2026-09-14 | ADR-127, ADR-128, ADR-060, ADR-090, ADR-093, ADR-120 |
| [ADR-136](ADR-136-world-unit-is-millimeters-robotics-converts-at-the-wire.md) | **世界単位は mm — ロボット/URDF/grasp-contract はワイヤ境界でだけメートルに変換する** | Accepted (実装済み。出所はユーザーの「アセットサンプルと Add で追加する縮尺が違わないか」「ロボットも Add のペデスタルと大きさが全然違う」の 2 段の指摘。実測すると Solid/Layout DSL は mm を生数値で world-unit に使う一方、`skeleton_arm.urdf`(実物 UR5e、ROS 標準のメートル)と grasp-contract の `plan.reachMin/reachMax`・`robot.base` 例値・`core/tests/data/grasp-req.json` は一貫してメートル前提で、無変換のまま同じ Three.js world 座標を共有していた。見た目のズレ(ロボット骨格が 1000 分の1近いサイズで描かれる)だけでなく、`GraspController._resolveRobotDeclaration()` が mm 系の `worldPoseOf()` 結果をそのまま `robot.base` に、`surfaceSamplesFor`/`obstaclesExcluding`(Layout DSL 由来、mm)をそのまま `target`/`obstacles` に乗せており、reach 判定の距離計算自体が意味を失っていた(ADR-119 が「対象幾何の文書由来/シーン由来の非対称」として保留していた実害側)。応答側も同型 — `GraspGhostView.showCandidate()` が `cartesianFrame.position`(メートル)を mm 系シーンへ無変換で置く。**Option A(world-unit=mm を正、ロボティクス境界だけ変換)を採用** — Option B(world-unit=m、Solid/Layout DSL 側で変換)は `examples/`/`templates/`/`schema/`(公開スキーマ)と未知の量の既存 scene-JSON を巻き込み、変換点を列挙しきれない。新設 `src/domain/worldUnits.js`(`MM_PER_METER`/`mmToM`/`mToMM`)を軸に変換をちょうど 4 箇所へ集約: (1) `RobotStage` コンストラクタで URDF ルートに `scale.setScalar(1000)`、(2) `robotSkeleton.js` の `TCP_LOCAL_SEED` を ×1000、(3) `GraspController` 送信側で `robot.base`/`target.surfaceSamples[].point`/`obstacles[].center,.radius`(+ `GraspDeclarationCatalog.visionFromViewportCamera`)を ÷1000、(4) `GraspController` 受信側で候補姿勢の `position` を ×1000 してから ghost/最近傍判定へ渡す。副次的に `CuboidModel.createInitialCorners()` の既定キューブを ±1(2mm)から ±50(100mm)へ、`ROBOT_FRAME_DEFAULTS`/`ROBOT_ADD_OFFSET` を ×1000 へ改訂(ユーザー報告の起点だった Add の既定サイズも解消)。ドメイン層(`graspTargets.js`・`GraspGhostMath.js`)は単位を知らないまま純粋: 変換は境界役の `RobotStage`/`GraspController`/`GraspDeclarationCatalog` にだけ置く(原則 #3)。`contractVersion` は不変(フィールドの意味の明確化であって構造変更ではない — ADR-083/084 の先例)。GSN 論証木は `docs/gsn/adr-136-world-unit-is-millimeters.gsn`) | 2026-09-18 | ADR-084, ADR-119, ADR-127, ADR-129, ADR-090 |
| [ADR-137](ADR-137-the-boot-view-is-framed-not-hard-coded.md) | **起動画面もフレーミングの入口 — 単位の決定は「変換する箇所」ではなく「world-unit を消費する箇所」を数える** | Accepted (実装済み 2026-09-19。出所は ADR-136 実装の `/whiteboard` 遡及レビュー — 実装時に俯瞰を通していなかったため、レビューを依頼されて初めて出した。**ADR-136 の 5 つの変換点はすべて正しい** (方向・対象ともに 1 つずつ確認済み。セルテンプレ上で Add ▸ Robot すれば骨格は mm 系ペデスタルと同縮尺で描かれ、ADR-136 の Goal は達成されている)。にもかかわらず**起動直後のビューポートが空になっていた** — Outliner は 'Cube · selected'、画面は 0 個。取り残された 3 つの定数はどれも world-unit スケールを持つのにメートル時代のままで、`_restStarterCube()` の重心 `z=0.5` (+ `localCorners × 0.5`) が 100mm キューブを 50mm に縮めて底面を **z=−24.5mm** へ沈め、`SceneView` の `camera.position.set(6,-4,3)` が原点から 7.81mm = **半径 25mm のキューブの内側**、`far=100` が 2828mm 先のロボットを**丸ごと far 平面の外**に置いていた。ズームアウトしても点になるだけで復帰不能 (grid も再スケールされない)。**力学 1: 枚挙の母集団が「変換する箇所」だった** — ADR-136 Lens notes の「動かすノードは正確に 4 つ」は変換点としては正しいが、単位の決定が動かすのは *world-unit で書かれたあらゆる定数の意味* で、変換を必要としない 3 つは定義上その枚挙に現れない (原則 #31 — 母集団を『在る変換』で定義した瞬間、消費者は外へ落ちる)。一段細かい同型として、「既定キューブの大きさ」という 1 つの事実に**書き手が 3 人**いて (`createInitialCorners` / `SceneService` stagger / `_restStarterCube`)、ADR-136 は 2 人に届き 3 人目に届かなかった (§1.1)。**力学 2: 規則は在ったが起動経路がそれより古い** — `docs/CODE_CONTRACTS.md` は既に "Any new 'frame the scene' entry point must route through `fitCameraToSphere`" と書いており、テンプレ読み込み・文書採用・STEP インポートは全部通っている。通っていない入口は**起動ひとつ**で、それが最初に見る画面だった。**力学 3: 見た目の主張を、見た目を見ないレーンが承認していた** — ADR-136 は 5 主張のうち 4 つを `node --test` で焼き、5 つ目 (骨格のスケール) だけを「手動確認」の散文 + GSN 借金 (満期 `PATH:e2e/robot-scale-parity.spec.js`、`DEBT_BASELINE` 34→35) に降ろした。**その手動確認は実行されず**、全レーン green のまま起動画面が真っ黒になった。D1 `CuboidModel.DEFAULT_HALF_EXTENT` を export して既定サイズの権威を 1 つにし、`_restStarterCube()` は**寸法を触らず置くだけ**にする (接地が半径から導出される。起動キューブは Add 既定と同寸の 100mm へ)。D2 起動を `_frameScene()` 経由の `fitCameraToSphere` 入口にする。開幕ショットの画角は「カメラを引く margin」ではなく**「フレーミングする球の半径」** (`BOOT_VIEW_RADIUS=300mm`) で保つ — margin 案は実装中に採ったが、grid と fog がその margin を知らないため正しく構図された開幕ショットが霧で半分沈み、破棄した (カメラ・grid・fog が同じ 1 つの数から導出される形へ)。`SceneView` の構築時カメラ姿勢は**方向のみ** (`BOOT_VIEW_DIRECTION`) にし、距離とクリップ面はフレーミングの所有物とする。D3 `_addRobot()` も同じ入口を通す (2828mm 先に生えるものは 100mm の視界にも 100mm の far にも収まらない — 原則 #11、STEP インポートと同じ扱い)。D4 `e2e/robot-scale-parity.spec.js` を起こし、`window.__easyExtrude.worldScale()` で**値ではなく個数**を測る (`solidsOutsideView` — 「シーンに在るが画面に無い」は欄を持たないので在るものを辿る検査は素通りする。母集団は種別の列挙から作る)。副次的に `GLYPH_MIN_WORLD=0.05` (mm 系では 0.05mm = 永久に効かない床) を `MM_PER_METER*0.05` へ、`_focusSphere`/`_frameLayoutDsl` の裸のメートル時代の floor (0.5 / 1 / 10) を `MIN_FRAME_RADIUS`/`EMPTY_SCENE_RADIUS` へ。却下: A 3 定数を mm へ書き換えるだけ (症状は消えるがカメラ定数は『世界の大きさ』の第三の源として残り、次の単位変更でまた取り残される)、C 現状維持 (回避操作の無い初回体験の破損)。**検証は「落ちること」まで確認済み** — 計測だけを修正前コードへ移植した worktree で同 spec を走らせると `minSolidZ` が 24.5、`far` が 100 を返して fail する (落ちない検査は何も守らない)。`pnpm test` 1293/1293 green・`typecheck` green・`pnpm test:e2e` **58 passed / 10 skipped**・`test:contract` 30・`test:adr`/`test:deferrals`/`test:gsn`/`test:gsn-debt` すべて green。骨格高さ実測 **574mm**、起動カメラ距離 **780mm** (逆算値と一致)。ADR-136 の実測記録 (「1157 本中 1147 green、fail 10 は既存」) も訂正した — 依存インストール済みの環境で同コミットを測り直すと **1293/1293 green・fail 0** で、『既存の失敗』として記録された事実は実在しない (原則 #19)。ADR-136 の GSN 借金は満期到来で解消 (`DEBT_BASELINE` 35→34)、**ADR-126 の満期機構が実際に発火して借金を減らした最初の例**。同じ PR で ADR-137 の木が exploring を 1 つ足すので 34→35 と再申告 (差引きゼロでも両方向を宣言する)。GSN 論証木は `docs/gsn/adr-137-the-boot-view-is-framed-not-hard-coded.gsn`) | 2026-09-19 | ADR-136, ADR-089, ADR-090, ADR-096, ADR-103, ADR-115, ADR-125, ADR-126, ADR-068 |
| [ADR-136](ADR-136-world-unit-is-millimeters-robotics-converts-at-the-wire.md) | **世界単位は mm — ロボット/URDF/grasp-contract はワイヤ境界でだけメートルに変換する** | Accepted (実装済み。出所はユーザーの「アセットサンプルと Add で追加する縮尺が違わないか」「ロボットも Add のペデスタルと大きさが全然違う」の 2 段の指摘。実測すると Solid/Layout DSL は mm を生数値で world-unit に使う一方、`skeleton_arm.urdf`(実物 UR5e、ROS 標準のメートル)と grasp-contract の `plan.reachMin/reachMax`・`robot.base` 例値・`core/tests/data/grasp-req.json` は一貫してメートル前提で、無変換のまま同じ Three.js world 座標を共有していた。見た目のズレ(ロボット骨格が 1000 分の1近いサイズで描かれる)だけでなく、`GraspController._resolveRobotDeclaration()` が mm 系の `worldPoseOf()` 結果をそのまま `robot.base` に、`surfaceSamplesFor`/`obstaclesExcluding`(Layout DSL 由来、mm)をそのまま `target`/`obstacles` に乗せており、reach 判定の距離計算自体が意味を失っていた(ADR-119 が「対象幾何の文書由来/シーン由来の非対称」として保留していた実害側)。応答側も同型 — `GraspGhostView.showCandidate()` が `cartesianFrame.position`(メートル)を mm 系シーンへ無変換で置く。**Option A(world-unit=mm を正、ロボティクス境界だけ変換)を採用** — Option B(world-unit=m、Solid/Layout DSL 側で変換)は `examples/`/`templates/`/`schema/`(公開スキーマ)と未知の量の既存 scene-JSON を巻き込み、変換点を列挙しきれない。新設 `src/domain/worldUnits.js`(`MM_PER_METER`/`mmToM`/`mToMM`)を軸に変換をちょうど 4 箇所へ集約: (1) `RobotStage` コンストラクタで URDF ルートに `scale.setScalar(1000)`、(2) `robotSkeleton.js` の `TCP_LOCAL_SEED` を ×1000、(3) `GraspController` 送信側で `robot.base`/`target.surfaceSamples[].point`/`obstacles[].center,.radius`(+ `GraspDeclarationCatalog.visionFromViewportCamera`)を ÷1000、(4) `GraspController` 受信側で候補姿勢の `position` を ×1000 してから ghost/最近傍判定へ渡す。副次的に `CuboidModel.createInitialCorners()` の既定キューブを ±1(2mm)から ±50(100mm)へ、`ROBOT_FRAME_DEFAULTS`/`ROBOT_ADD_OFFSET` を ×1000 へ改訂(ユーザー報告の起点だった Add の既定サイズも解消)。ドメイン層(`graspTargets.js`・`GraspGhostMath.js`)は単位を知らないまま純粋: 変換は境界役の `RobotStage`/`GraspController`/`GraspDeclarationCatalog` にだけ置く(原則 #3)。`contractVersion` は不変(フィールドの意味の明確化であって構造変更ではない — ADR-083/084 の先例)。GSN 論証木は `docs/gsn/adr-136-world-unit-is-millimeters.gsn`) | 2026-09-18 | ADR-084, ADR-119, ADR-127, ADR-129, ADR-090, ADR-137 |

| [ADR-138](ADR-138-scale-dependent-quantities-declare-their-kind.md) | **尺度に依存する量は自分の単位を名乗る — world-unit を持つ定数の個数を数える** | Accepted (実装済み 2026-09-19。出所は ADR-136 実装後の `/whiteboard` レビュー。**ADR-136 を覆さない** — その『変換点はちょうど 4 箇所』は正しく、4 箇所の変換も正しい。問いが一段上に在った: 4 は『ロボティクスがシーン境界を跨ぐ点』の個数であって『world-unit を単位に持つ定数』の個数ではない。**実測された欠陥**: 実装後の起動直後の既定シーンが壊れており、Outliner は `Cube selected` と出ているのに画面に何も写らない(カメラ距離 ‖(6,−4,3)‖ = √61 ≈ 7.81 < starter cube 半幅 25 ⟹ **カメラが cube の内側**、far = 100 で可視域も打ち切り、ロボット既定位置 ‖(−2000,2000,0)‖ ≈ 2828 は far の 28 倍)。**力学 1**: world-unit は ADR-136 以前から一意ではなかった — repo 内に 1 unit = 1 m を明記した権威が複数在る (`SceneView.js:111`「the 20-unit grid is sized for meter-scale scenes」/ `placement.js:62`「接地判定の許容 (1 mm)」= `0.001` / `SceneService.js:2671`「1 mm」= `0.001` / `FaceExtrudeHandler.js:21`「world units」= `0.15` / `AppController.js:1141`「a 1 m cube」)。ADR-136 は対のうち **Add 側だけ**を mm へ動かし、対になる view / 判定側を動かさなかった。**力学 2 (原則 #31)**: 裸の数値リテラルは型でも命名でも自分の単位を名乗らないので、*在るもの* (変換点) を辿る数え方では**定義上出てこない**。**力学 3**: ADR-136 の実測「1157 本中 1147 green / fail 10 は環境依存」は `node_modules` 不在下の値で、差分 136 本は three/ajv/typescript を要求する = **まさに触った view 層**。依存を入れ直すと 1293/1293 green — 落ちたのではなく**母集団から静かに外れていた**。**力学 4**: 正しい導出は既に在り boot だけが呼んでいない — `CameraMath.focusPose`/`clipPlanesFor` (ADR-068) は near を `radius·0.001` でシーンに比例させ、その doc は固定 near が **mm-authored layouts** で Z-fight を起こすことまで書いている。**D1** 尺度依存量の種を 3 つに閉じ第四の種 (裸の定数) を禁じる: 画面空間(`*_PX`) / シーン由来 (radius を引数に取る関数) / 宣言された物理長 (`mm(…)` 経由)。**D2** `worldUnits.js` に `mm(n)` を足し単位を**呼び出し側の構文**へ乗せる (`MM_PER_METER`/`mmToM`/`mToMM` はワイヤ変換の権威として不変 — `mm()` は変換ではなく宣言)。**D3** boot も既存の 1 つの導出を通し (`fitCameraToSphere` → `focusPose`/`clipPlanesFor`/`_updateGridScale`)、`0.1/100`・`(6,-4,3)`・`GridHelper(20,20)` を**retune せず種類ごと消す** (retune は今日の尺度にだけ正しく、m 系アセットを読んだ日に同じ欠陥へ戻る)。**D4** 数えるのは三種のどれでもない量の個数で、母集団は*定数*ではなく**sink の構文**から導出する (`camera.near/far` への代入・`camera.position.set`・`scale.setScalar`・`GridHelper`・`fitCameraToSphere` の radius 実引数・world 座標と比較される閾値) — ratchet のベースラインを定数に焼き上下どちらへ外れても fail (ADR-100 の手)、`src/census/sources.js` へ登録 (ADR-102)。**D5** 問い所を散文に置かない — ADR-136 は `docs/CODE_CONTRACTS.md` に 1 行足したが**その行が在る状態でこの欠陥が commit された**ので、成果物は検査 (`src/WorldUnitCensus.test.js` + `e2e/boot-framing.spec.js`)。却下: A 見つけた定数を mm へ retune するだけ (母集団が人の記憶のまま — **ADR-136 がまさにこれをやった**)、B world-unit を branded type で型に載せる (原則 #21 と同じ最強手だが `Vector3` 全域に及び過大 — 核 §5。**D4 の sink 表が手書きである限界が耐えられなくなった日のトリガ**として名指し)、D 現状維持。**限界の宣言**: D4 は個々の定数の母集団を導出するが *sink の一覧そのものは手書き*で、ADR-102 の `place-list` の形が一段上に残る。状態・基数は増やさないので `docs/STATE_LEDGER.md` に新しい行は起きない (**宣言であって既定ではない** — 数える対象は実体ではなく定数で、累積器は台帳ではなく census)。**実装時に判明した差分 5 点** (本文 §実装時に判明した差分): (1) **D4 の sink 表は初回実行が自分の誤りを出した** — `.scale.setScalar` を world 長の sink に数えて 8 箇所を誤報告したが、`.scale` は**比率**で無次元。category error は表のほうで、母集団を導出させた初回実行でしか気づけない種類の誤り (ADR-102 の 3 例と同形)。(2) **e2e の下限 0.05 は「写っている」を問えていなかった** — `_frameStarterScene()` を外しても通った (seed 半径だけで coverage ≈ 0.058)。D3 は 2 つの半分から成り 0.05 は前者しか問えないので **0.3** へ。*検証の形そのものが主張*。(3) boot framing の**ヘッドルームを入れようとして撤回した** — カメラを引くと固定 px のドラッグが対応する world 距離が変わり、smoke の grab/stack テスト (100 px sweep) が落ちた。ヘッドルームは装飾で決定は「同じ 1 つの導出を通す」(D3) なので装飾を捨て、理由をコードに残した。**framing に自分の係数を足すのは「retune せず種類ごと消す」と言った D3 の小さい違反である**。(4) `SNAP_THRESHOLD` は150mm ではなく **15mm** — 絶対 0.15 m 読みは既定キューブより大きい snap になり、その読み自体シーン内容には一度も成り立っていなかった (Layout DSL は元から mm)。(5) `checkGroundClearance` の既定値は `SUPPORT_TOLERANCE` を**引く** — doc が「matches the stack-snap rest tolerance」と書きながら数値は写しで、**同じ事実が 2 箇所** (§1.1) だったのが 1 µm 化の経路そのもの。**検査が噛むことを実測**: census は `camera.far = 100` 再注入で fail、e2e は ADR-136 状態で`distance 7.81 < radius 43.30` で fail、再フィットだけ外すと `coverage 0.051 < 0.3` で fail。検証: `pnpm test` 1295/1295 · `typecheck` · `build` · `test:contract` 30/30 · `test:e2e` 58 passed/10 skipped/0 failed · `test:adr` 137 本 · `test:gsn` · `test:gsn-debt` · `test:deferrals` すべて green。GSN 論証木は `docs/gsn/adr-137-a-world-unit-constant-has-no-field-of-its-own.gsn`) | 2026-09-19 | ADR-136, ADR-068, ADR-100, ADR-102, ADR-115, ADR-119, ADR-138 |

| [ADR-139](ADR-139-an-imported-coordinate-declares-its-frame-before-it-enters-the-world.md) | **外から来た座標は自分の frame を宣言してから世界に入る — 単位・利き手・up 軸を 1 つの値にし、既定で埋めない** | Proposed (2026-09-20 起票・未実装。出所は当事者の経験的指摘「他のツールからデータをインポートした時に必要なんじゃないか。何回も world の型が合わなくて失敗してます。右手系左手系、上が +Z なのか +Y なのかとか」。**ADR-137 の却下案 B が言う「型」は単位ブランドのことで、当事者が名指しした問題より狭い** — 利き手と up 軸は単位と**独立な軸**なので単位ブランドでは 1 つも捕まらない。**力学 1**: 壊れ方は 4 軸 (単位 mm/m/inch · 利き手 right/left = **鏡像** · up 軸 +Z up の ROS/CAD か +Y up の glTF/OBJ/DCC · forward 軸) あり互いに独立で、ADR-136/137 が閉じたのは 1 軸目だけ、残り 3 軸は無傷で残る。**力学 2 (実測)**: この repo の world 規約の唯一の正本は `CLAUDE.md` の 1 行で、**読む機械が 1 つも無い** — `schema/layout-1.0` の top-level は `version/meta/strategy/strategyOptions/entities/constraints`、`schema/scene-1.3` は `version/objects/links/transformGraph/operationGraph` (`required: [version, objects]`)、どちらにも frame も unit も無く、`SceneService._applyGeometryUpdate` は STEP 変換由来の生 `positions` を**無変換・無宣言**で `BufferGeometry` へ書いて `initCorners()` で AABB を作る。**ADR-115 と同じ形** (宣言は在るが読む機械が無い)。**力学 3 (原則 #31)**: `positions: number[]` は「どの frame の値か」を言えないので、間違った frame のデータは**エラーを出さずに通る** — 90° 転んだモデルが痕跡を残さず乗る。最悪の既定は「宣言が無ければ ROS」で、Yellow Card「必須入力の欠落に既定値を与えない」(幽霊ロボットの先例) そのもの。**力学 4**: 原則 #21「座標空間は型・API 形状で静的に区別する」は §写像 に行を持つ #1 #2 #14 #18 #19 #24 #30 #31 #32 の中に無く、**#21 は宣言されていてこの軸では一度も具体化されていない** (Local⇄World の親子は ADR-018/037 が持つが World **そのものの規約**は型でも API 形状でも区別されていない)。**D1** `Frame` = 4 欄 (`handedness`/`up`/`forward`/`mmPerUnit`) の閉じた値、`WORLD` をその 1 インスタンスとして `src/domain/worldFrame.js` に置き **CLAUDE.md は名指しするだけ**にする (核 §1.1 — 規約が散文から値へ移る = 読む機械を持てる)。**D2** `toWorld(frame)` は純粋関数で `up ∥ forward` や「利き手と det の符号の不一致」は**構築時に throw** (「+Y up にしたのに forward を +Y のままにした」が止まる — `EXPLICIT_DEFAULTS` / `PLACEMENT_BY_KIND` と同じ手)。**D3** ingress (`_applyGeometryUpdate` / `parseImportJson` / `LayoutCompiler`) は frame を**必須入力**として受け、宣言なき入力は既定に落とさず取り込みを止めて理由を画面に出す (原則 #11)。**D4** egress も frame を刻む — **我々の出力は次のツールの「宣言なき入力」**なので、刻まなければ往復した瞬間に宣言が消える (`scene-1.4` / `layout-1.1` へ版上げ。grasp-contract の response には触れない — あちらの `frame` は pose の親参照で別の事実)。**D5** 検証は**キラルな fixture**で行う — `det(toWorld(f)) < 0 ⟺ 利き手相違` と往復恒等 (原則 #28) を焼くが、**立方体や球で書くと鏡像写像でも一致する**ので対称な fixture は自分が裏返ったことを知らない。*検査の書き方そのものが主張*である (ADR-120 と同型)。却下: A 単位だけのブランド (4 軸のうち 1 軸しか覆わず、報告された失敗のほうが残る)、B 型引数つきの点型だけで持つ (**frame は実行時の事実** — どのファイルをどのツールが書いたかをコンパイル時の型は知りえない。加えて ingress の大半が `@ts-nocheck` の下。ゆえにブランドは**主たる防御ではなく追加の網**として縮めて併用し、重心は「宣言を必須入力にし既定を与えない」側へ)、D 現状維持。**限界の宣言**: 既存ファイルの移行 UI は**まだ決めていない** — 今決めると画面設計を先取りするので「対象外」とは書かず未決として数える (DEF-034/035 と同じ扱い)。`Frame` を 4 欄に閉じたので原点オフセット・非一様スケールは表現できない (意図的 — **規約と配置は別の事実**で、混ぜると §1.1 の源が 2 つになる)。状態は増えないが**基数の行は起こす** — `Frame` の基数は「世界に 1 つ、入力ごとに 0 or 1」で、**0 が正当に見えてしまうのが本 ADR の敵**なので `docs/STATE_LEDGER.md` に「0 のときは既定に落ちるのではなく取り込まない」と書く。GSN 論証木は `docs/gsn/adr-138-an-imported-coordinate-declares-its-frame.gsn`) | 2026-09-20 | ADR-137, ADR-136, ADR-115, ADR-120, ADR-018, ADR-037 |
| [ADR-140](ADR-140-a-click-resolves-by-depth-not-by-the-order-the-handler-was-written.md) | **クリックは書かれた順ではなく奥行きで解決する — ロボットを「最後の手段」から body へ戻し、3 つの写しを 1 つの決定点へ畳む** | Accepted (実装済み 2026-09-20。出所は当事者の「ロボット本体をクリックしても選択できない」。調べると解 (`hitRobotStage()` → `robot_base`) は ADR-084 §2 以来ずっと在り、欠けていたのは**その経路に到達する条件**だった: 3 ハンドラすべてが `if (!result) result = hitRobotStage()` と書いており、これは優先順位ではなく「光線上のどこにも何も当たらなかったときだけ腕を見る」= **無条件の敗北**で、距離は比較に一度も入らない。同梱セル 3 種はロボットをペデスタルの上・作業台の手前に置くので**負ける構図のほうが既定**だった。規則を純粋関数 `src/domain/clickTarget.js` へ出し、scope rank (frame 0 < {solid, robot} 1 < annotation 2) → 同 rank 内は距離、で決める。**solid と robot が同 rank であることが決定の本体** — 腕は body であって格下ではなく、rank の共有だけが 2 つを距離比較へ送る。未宣言の種は**勝敗より先に** throw (原則 #31)。写しは 3 つあり既にドリフト済み (`_onDblClick` に CF-descendant 例外も annotation フォールバックも無し) で、4 ジェスチャ (pointerdown / contextmenu / dblclick / **hover**) を 1 入口へ。hover も通したのでカーソルがクリックのしない約束をしなくなった。所有者の外の優先順位を数える検査の母集団は決定点の本体から構文で導出 (ADR-102) し、初回実行が**死んでいた 4 つ目の写し** `_hitAnyEntityForLink` を出した — 誰からも呼ばれておらず、*在る呼び出し*を辿る読み方では出てこない) | 2026-09-20 | ADR-084, ADR-090, ADR-102, ADR-103, ADR-108, ADR-137 |
| [ADR-141](ADR-141-the-arm-on-screen-is-the-arm-the-search-solves-for.md) | **画面の腕と、探索が解く腕は同じ 1 台である — 「どのロボットか」を 1 つのモデル行へ畳む** | Accepted (実装済み 2026-09-20。出所は当事者の「Add で追加できるロボットモデルに統一してほしい」。「どの腕か」を語る源が **5 人**いた: 描く URDF (UR5e) · `REACH_PRESETS` の 3 本 (**うち 2 本は出荷したことのない腕**) · examples の `f_robot.attrs.reach` · バックエンド受け入れフィクスチャ。UR5e を見ながら「large arm (≈1.3)」を宣言でき、`reach_margin` は画面の腕より 50 cm 長い腕に対して採点される — 痕跡は残らない。**問題は値ではなく 1 つの事実に著者が複数いること** (§1.1)。ADR-136 (単位) → 137 (消費点) → 138 (尺度依存量) と同じ形の 4 回目で、軸が初めて*量*ではなく**同一性**になった。包絡は DECLARATION のまま (URDF は述べていない事実) だが、**描く URDF の DH から導出した帯 `[|a2|+|a3|, +d5+d6]` の外なら build が落ちる** — UR5e 実測 [817, 1017] mm に対し宣言 850 mm、退役する 500/1300 は両方とも帯の外。導出しきれないものを宣言し、宣言をそれ自身のループの外側へ縛るのは ADR-127 の `FLANGE_APPROACH_AXIS` と同じ手。**到達可否は一度も判定しない** — 答えるのは「腕の長さ」だけで、把持点が包絡の内側かは契約の向こうの `core/` に留まる。提示は固定 3 行から「シーンに居る腕」**0〜1 行**へ (0 台なら 0 行 — 置かれていないロボットの包絡は ADR-120 が禁じる発明された既定)。違う腕の宣言は塞がないが食い違いはカードに出る (原則 #11)。種の自動投入は `touched` で止める — **空欄と「意図した 0.5」は違う状態**。`contractVersion` 不変 6) | 2026-09-20 | ADR-127, ADR-128, ADR-120, ADR-136, ADR-137, ADR-138, ADR-090, ADR-103 |

| [ADR-142](ADR-142-a-picked-scene-is-not-someone-elses-furniture.md) | **選んだシーンは「他人のセル」ではない — テンプレート/import 由来のロボットは既定で表示する** | Accepted (実装済み 2026-09-20。出所は当事者の「サンプルシーンでロボットがあるシーンを選択してもロボットがビジュアライズされないのはなんで?」。`EXPLICIT_DEFAULTS[ROBOT_BASE_SEEDED]` の既定 `false` (ADR-096 §Decision 3) の根拠は「空のブートシーンに腕 1 本はノイズ」(ADR-089 follow-up) だったが、**2026-08-14 の ADR-132 D5 がブートの robot seed 経路そのものを廃止**しており、今日 `VISIBILITY_ENTRY.SEED` はテンプレート読み込み/import だけを指す。前提(空シーン)が消えた後も既定値だけがコードに残っていた — 原則 #24 の片側だけが起きた事故。ユーザーがロボット入りのテンプレートを選んでも Outliner の目を開かない限り見えないのは、ADR-096 が消したはずの「無言の no-op」(原則 #11) の別形。`ROBOT_BASE_SEEDED` の既定を `true` に反転し `ROBOT_BASE_ADDED` と揃えたが、**kind 自体は畳まない**(入口を区別する経路は将来のために残す — Evidence の範囲を超えて一般化しない)。ブートは引き続きロボット 0 台なので無影響) | 2026-09-20 | ADR-096, ADR-089, ADR-132, ADR-090 |
| [ADR-143](ADR-143-a-declared-default-must-reach-the-loaded-scene.md) | **宣言された既定は、読み込んだシーンにも届かなければならない — import は合成を一度通る** | Accepted (実装済み 2026-09-20。**ADR-142 の検証中に発見**。実ブラウザで Home のピック&プレイスセルを選ぶと腕は描かれる (ADR-142 の Goal は達成) が、同じロボットの `robot_base` が `explicit=true` / `drawn=false` — 行は「表示」と言い画素は伏せている、ADR-096 G1 そのものの破れ。原因は合成 `applyEntityVisibility()` の**呼び出し元が 4 つしか無かった**こと: reattach / eye / declare / selection は通るが `importFromJson` は `addObject` + `emit` だけで終わり、`CoordinateFrameView` は構築時 `visible=false` なので読み込まれた CF は種が何を宣言していても伏せられたままだった。**3 年ぶん誰にも見えなかったのは値がたまたま一致していたから** — ADR-142 以前 import 経路で到達しうる宣言は全部 `false` 側で、「一度も合成していない」と「合成して非表示」が同じ画面を出す (原則 #31)。しかも同じ不変条件を問う e2e は既に在った (「the row never lies」) が、**母集団が `atBoot`** でブートは import を通らない — 検査は正しく、覆えていなかったのは経路のほう。ADR-142 が偶然の一致を壊して隠れ場所を無くした (ADR-132 D5 と同型)。import 末尾で全実体を合成へ 1 周通す。末尾なのは `ensureRobotFrames()` が `robotRole` を刻む前だと `robot_base` がただの CF として `false` を拾うから。合成点は今も 1 箇所で、増えたのは*呼び出し元*であって*ピクセルを書く場所*ではない (原則 #4 は不変)。回帰は修正前に `robot_base(explicit=true, drawn=false)` で赤・修正後に緑を両方向で実行済み) | 2026-09-20 | ADR-096, ADR-142, ADR-132, ADR-115, ADR-090 |
| [ADR-144](ADR-144-an-approximate-preview-is-not-a-second-solver.md) | **クライアントの腕プレビューは「未決定の埋め合わせ」であって「もう一つの解法」ではない — GitHub Pages では近似、core/ の権威は不動** | Accepted (実装済み 2026-09-21。起票時の判断 (D1〜D5) はすべて生きているが、実装で 6 点を明示へ変えた。**(1)** 最近傍サンプル 1 回では中央値 175mm 外れて常に `null` になり Goal に届かないので、同じ格子を窓を半分ずつ狭めて再サンプルする多スタート版にした — 新しい方程式は 1 行も足しておらず `sampleConfigs`+`forwardKinematics` の反復のみ (実測: 到達可能点 100 件の 98% が 1mm 以内・1 回 57ms)。**(2)** 手を動かせない関節 (UR の `wrist_3`、軸がフランジを通る) は**測って** 1 サンプルに畳む — 手書きの除外表は別の URDF が来た日に黙って嘘になる。このため `sampleConfigs` に関節ごとのサンプル数 (配列形・長さ不一致は throw) を加法的に追加。**(3)** 限界 ±2π なので格子は手首が 6.1 rad 巻いた**等価解**を拾う。返す前に (−π, π] へ書き直す — 探索ではなく表記の正規化で、FK が変わっていないことは 「宣言した `errorMm` = 返り値を FK に通した実測」で焼く。**(4)** 一致させているのは**手の位置だけ**で姿勢は見ない — 精度の不足ではなくこの近似が何でないかの宣言で、半透明ゴーストと行のキャプションが要る理由そのもの。**(5)** 既定シーンでは Shift+A のロボットが原点から 2.8m (ADR-083)・UR5e の到達は 0.9m なので近似は正直に `null` を返す。e2e は Home の単腕ピック&プレイスセルで「腕が動き、かつ未検証と名乗る」(S11) を、既定シーンで「動かない」(S11b) を**対照として**焼く — 常に一番近い配置を出す実装は S11 では緑になり S11b でだけ落ちる。**(6)** 「GitHub Pages で腕が動く」は画素の主張でユニットレーンでは原理的に問えないので、`window.__easyExtrude.armPreview()` が**書き込んだ後の**関節角と unverified フラグを読み戻す (ADR-137 `worldSpan()` と同じ手)。型分離は ajv が実際に拒否することで焼き、同じ validator が `solved`/`undeclared` を通すことも 同時に主張する。`contractVersion` は 6 のまま・`packages/grasp-contract` / `core/` / `server/` / `mocks/graspStub/` は 1 行も変えていない) | 2026-09-21 | ADR-135, ADR-053, ADR-127, ADR-120 |
| [ADR-145](ADR-145-the-arm-between-base-and-tcp-is-invisible-to-interference.md) | **base と TCP の間にある腕は干渉判定から見えていない — 進入経路チェックに腕リンクの FK スイープを足す** | Accepted (**実装済み 2026-09-21**。起票 2026-09-21。出所は当事者の「供給ビンを Grasp 対象にすると腕がペデスタルにめり込む」報告。`/whiteboard` で3点を切り分けた: (1) 座標系 (`camera.up.set(0,0,1)` 明示、ADR-136) は原因ではない。(2) `UniversalRobotsIkSolver.solve()` は UR 解析解で内部的に最大8解を計算するが ADR-127 D5 により代表解1つのみ公開、代替解を選ぶ手段は無い。(3) **本丸**: `NaiveSphereCollisionChecker`/OBB版 (ADR-133 D5) は `pre_grasp→把持点` の TCP 進入線分1本しか障害物と照合しておらず、関節解も腕リンクの幾何も一切見ていない — ペデスタルは `fixture`/`static` 障害物として正しく宣言されているが、進入経路がそこを通らないため肘・前腕の回り込みは判定のどの入力にも現れない。8解のどれを選んでも判定結果は不変 (判定が関節配置を見ていないため)。Option A (採用予定): `ur_kinematics.py` に `forward_kinematics_chain()` を新設し各関節原点を FK で求め、`base→shoulder→elbow→wrist1→wrist2→wrist3→tcp` の各リンクを既存 `distance_point_to_segment` で障害物と照合する `NaiveArmSweepCollisionChecker` を追加 (response契約 `reachSolution` は不変、`robot.kinematics` 未宣言時は既存TCP経路判定へ無言でフォールバック)。Option B (見送り・DEF-037): 干渉なしになるまで代替解を探索するモード (ADR-127 D5 の再開) は A の実測を経てから判断。**実装で本文の前提が 6 つ動いた**: (1) 本文が「在る」と書いた **OBB 版チェッカは存在しない** — 障害物は今も球のみで ADR-133 D5 は未実装 (DEF-036)、手元の ADR だけを読んで書いた ADR-102 型の誤り。(2) **素朴に全リンクを照合すると症状が直るどころか全候補が棄却される** — front が送るのは箱の外接球 (`obstaclesExcluding`) で、ペデスタル (300×300×120) の外接球は半径 **0.2205m**、ベースはその中心から **0.060m** の位置 = **ロボットは自分の据付台の球の内側から生えている**。ベース→関節1 の区間は θ1 の回転軸そのもので原点が `(0,0,d1)` に固定 = **どの配置でも同じ場所**なので候補を区別する力が無い一方ほぼ常に衝突と答える。よって除外した (`_JOINT_INDEPENDENT_LINKS`) — これは見逃しではなく*配置に依存しない事実*を判定から外す操作。(3) **その成立余裕は実測 2.5mm** (肩原点 0.2225m vs 半径 0.2205m)。台が少し高いか広ければ再び全棄却に戻る — 腕スイープの限界ではなく**外接球という表現の限界**で、DEF-036 が満期を迎えるべき理由がここで初めて*測れる数*になった。同じ限界で `worktable` は外接球半径 0.559m が把持点 (z=0.95) を飲み込むため受け入れテンプレから外した (README に宣言)。(4) **安い順フィルタの並びの根拠が古い** — ADR-081 の順序は naive IK の 5µs で測られており、再測すると最も高い段は干渉ではなく**解析解 IK の 368µs** (ADR-127 以降ずっと)。腕スイープは IK の解を入力に取るので IK より後にしか置けず干渉が最後段なのは不変だが、把持性・可視性を IK の前へ出す余地が在る (棄却の帰属が変わるので別判断 — **DEF-038**)。(5) **D3 の「既存実装は新引数を無視してよい」は成り立たなかった** — 2 引数で書かれた注入テストダブルは落ちる。`interference_free` 側で引数の有無を嗅いで吸収する手は**採らなかった** (吸収すると「腕を見るはずのチェッカが実は見ていない」が同じ形で静かに通る)。(6) 引数名は `joints` ではなく **`solution`** — 渡すのは数の列ではなく IK 解そのもので、判断は `isinstance(solution, JointSolution)` という型の問い。`joints` は「6 個の数」を期待させ、占位解を詰める経路を誘う。受け入れ: `templates/single-arm-pedestal-cell` — 宣言あり 36 生成/**10 干渉棄却**/26 通過、宣言を外した対照は棄却 **0**。**対照が 0 であることが主張の本体** (TCP 経路は一度も台に触れないので 10 件は腕を見たからこそ出た差)。棄却された代表解は上腕が z=0.718 まで降りる = ペデスタル上面より 200mm 下。`pnpm test:core` 195 passed (179→+16)、既存 179 件は 1 件も書き換えず = D4 の証拠) | 2026-09-21 | ADR-081, ADR-127, ADR-133, ADR-135, ADR-136 |
| [ADR-146](ADR-146-a-closed-form-may-be-copied-an-idea-may-not.md) | **閉形式は複製してよい、アイデアは複製してはならない — 置き場所を決める 3 軸** | Accepted (**2026-09-21**。出所は当事者の「探索が必要な処理はバックエンドサーバに、一意な解が出るものはクライアントでもできるように」→「アイデアが必要なコスト関数とかを含まない一般的なロジック。アイデアはサーバ側に隠すとともに、重たい処理もバックエンドの責務」→「**この境界の判断は毎回迷われるため、どこかに記録しておいてほしい**」。最後の一文が本 ADR の存在理由。**なぜ今日それが成り立っていなかったか**: CLAUDE.md の AI ガードは「IK / 干渉 / リーチ / 安定性の解き方」という**話題の列挙**で、(a) 列挙に無い計算が来た日に何も答えない (ADR-145 が足した FK チェーンはどれでもない)、(b) 列挙に在る計算でも理由が違えば答えが違う (ADR-053 は FK による到達判定を front に**許している**)、(c) 理由を説明できないので当たるたびに議論が振り出しに戻る。**実際にやり直した** — ADR-144 が Accepted になった**同じ日**に当事者が逆向きの規則を述べており、規則が判定可能な形で書かれていなかったので 1 日ももたなかった。**D1 アイデア軸 (最優先・排他)**: コスト関数・重み・ランキング・キュレーションされた判断知は `core/` のみ — 理由は性能でも一意性でもなく**競争上の知だから**。一意で軽くてもアイデアなら出さない。**D2 重さ軸**: 探索・組合せ・反復は `core/`。**D3 一意性軸**: 公知の閉形式・一般ロジック (FK・DH 変換・幾何プリミティブ・単位変換・正規形) は `src/` 可 — **ただし第二の源ではなく*導出*として = 共有フィクスチャの準拠テストを同じ PR で置くこと**。これが ADR-144 の §1.1 への回答で、**§1.1 が禁じるのは*複製*ではなく源が 2 つになること** (BFF が schema から型を導出し `pnpm test:contract` が焼くのと同じ形)。**D4** 判定順序は D1→D2→D3、どれも真にならない計算は「**まだ決めていない**」で**「対象外」と書かない** (原則 #29 の二状態)。**D5** 規則は CLAUDE.md の AI ガードを**書き換えて**置き (列挙を残して足すと源が 2 つになるので `Retires:` に載せた)、降ろせる部分を機械へ: `fixtures/cross-language/` + 登録簿 `CROSS_LANGUAGE_DERIVATIONS` が、フィクスチャの実在・**両言語の消費者が実際にそれを読んでいること**・数の一致・複製の個数 (ratchet) を問う。**実装で分かったこと**: 既に在った複製 (URDF を四元数で解く JS の FK と、そこから導出された DH を行列で解く Python の FK) は**24 配置すべてで最大差 < 1e-9 で一致していた** — つまり今日まで正しかったが、**正しいことを誰も問うていなかった**。負の対照 (フィクスチャの `a3` を 1mm 動かす) で 2 件 fail し、報告された最大差 0.001000 = 与えた摂動そのもの。却下: B 話題の列挙を伸ばす (列挙が長くなるほど列挙に無い計算に出会う確率が上がる)、C「探索か一意か」の 1 軸だけ (**コスト関数が front へ出てしまう** — 加重スコアの重みは一意で軽いがまさに隠したい知)、D §1.1 で複製を一切禁じる (ADR-144 の立場。BFF が同じ機構で複製を導出として許しているのに front だけ禁じるのは一貫しない)。**最も弱い辺を隠さない**: 登録簿の母集団は人の記憶であり、「同じ計算が 2 言語に在る」は構文から導出できない (DEF-039)。同じ理由で D1「アイデアか」の判定自体も機械からは見えない。D6 UR 解析解 IK の JS 移植は**未実装**のまま — 理由は失ったが結論は維持し、**両方書く** (理由だけ書き換えて結論を黙って残すと次に読む人は「まだ §1.1 で禁じられている」と読む。DEF-040)) | 2026-09-21 | ADR-144 (却下案 B の**理由**を差し替え・結論は維持), ADR-053, ADR-060, ADR-102, ADR-115, ADR-141, ADR-145 |

## How to Add a New ADR

1. Assign the next sequence number (`NNN = max + 1`)
2. Create the file: `ADR-NNN-title.md`
3. **Add a row to the index table in this README**
4. Add a reference in related existing ADRs' `References` section
5. Commit and push
