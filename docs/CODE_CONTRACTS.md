# Code Contracts — easy-extrude

Accumulated rules and policies derived from real bugs and design decisions.
Full details in `docs/code_contracts/`.
**Read the relevant detail file before modifying code in that area.**

---

## Maintenance guidelines

### What belongs here

- Rules learned from **real bugs** (not hypothetical ones)
- Policies where violating them causes **hard-to-find state inconsistencies**
- Ownership contracts between classes/modules that aren't obvious from the code
- Decisions that were **consciously chosen** over a simpler alternative

> Do NOT add: general best practices, things already obvious from the code,
> or temporary notes about in-progress work (use a task/plan for those).

### When to update

| Trigger | Action |
|---------|--------|
| A bug was caused by violating an implicit rule | Add the rule here |
| A new ADR establishes a coding contract | Summarize the contract here, link the ADR |
| An existing rule turns out to be wrong or too narrow | Update or remove it |
| A rule is already enforced by the code itself (e.g. type system) | Remove it — code is the source of truth |

### How to update

1. Add/edit the rule in the relevant detail file in `docs/code_contracts/`.
2. Update the summary row in the index table below.
3. Commit together with the code change that motivated it.
4. If the rule is substantial, create an ADR first and link it here.

---

## 1. Architecture & State Management

Detail: `docs/code_contracts/architecture.md`

| Rule | Core Takeaway |
|------|--------------|
| Mode Transition Flow | `setMode()` is the single entry point; always call `setMode('object')` before `_switchActiveObject()` from edit mode |
| State Restoration on Mode Exit | Restore `_objSelected = true` + `setObjectSelected(true)` when returning to Object mode if `_activeObj` exists |
| Entity Capability Contracts | Use `instanceof`, not `dimension`; each entity type's capabilities are its type contract — Sketch extrude swap via `SceneService.extrudeSketch()`, restricted ImportedMesh/CoordinateFrame, MeasureLine 1D edit, Solid R-key rotation via the ADR-040 primary triple (`_position`, `orientation`, `localCorners`); child CFs follow automatically via ROS TF forward kinematics. |
| Solid Pose Mutation Must Use Public API | `_rebuildWorldCorners()` is private to `Solid.js`. External code uses the public API: `restorePose()` (undo/cancel), `move()` (translation drag), `rotate()` (rotation drag), `setPose()` (deserialization), `setWorldCorners()` (legacy). Never mutate corners directly — stack snap's Z offset goes through `move()`. |
| Preview Pipeline (applyPreviewTranslation / applyPreviewRotation / applyPreviewEndpointMove) | Entity mutation during live drag previews belongs in `SceneService`: `applyPreviewTranslation` (Grab/mouse-drag), `applyPreviewRotation` (R-key), `applyPreviewEndpointMove` (1D endpoint drag). Controllers compute input deltas only; cancel paths also go through the service methods. |
| Rotate Pivot Must Use `_position` Directly | Any Solid pivot/centroid used in a state-mutating computation must come from `obj._position` (with `.clone()` where aliasing matters, e.g. TC rotate), never `getCentroid(obj.corners)` — the averaged world-space corners feed FP error back into `_position` and compound into visible drift far from origin (PHILOSOPHY #24c). `getCentroid` is permitted for read-only display only. |
| Centroid Is Validation-Only | `getCentroid(corners)`/`avg(corners)` accumulate FP error — never use for geometry traversal, SpatialLink endpoints, snap, undo/redo, or solver inputs. Use `obj._position` (Solid, ADR-040) or `_worldPoseCache` (CF); centroid is display/heuristic-proximity/annotation only. |
| MeasureLineView No-Op Interface | Every `_meshView` method called in AppController must exist as a no-op or real impl on MeasureLineView; `setEndpointHover(index)` and `clearEndpointHover()` are real impls used in `'1d'` edit substate |
| Measure Snap Display | Use `_measure.snapMeshView` (not `_meshView`) for all snap display during measure placement |
| MeasureLineView Label Lifecycle | Call `updateLabelPosition()` every animation frame for every MeasureLine in scene |
| CoordinateFrame Depth Rendering | Hidden by default; `setParentSelected()` controls visibility+X-ray; `setObjectSelected()` only changes sphere color |
| Auto Origin Frame (ADR-037) | `createSolid()`, `extrudeProfile()`, `duplicateSolid()` each call `createCoordinateFrame(id, 'Origin', null)` immediately after emitting `objectAdded`. Supersedes ADR-033 §3. `_ensureOriginFrames()` migrates legacy scenes on load. User CFs are always children of Origin CF (not direct Solid children). |
| Command Factory Naming Convention | All commands use `createXCommand` factory exports; never class-style `XCommand`. Import and call site must match the export name exactly |
| Add Sketch Auto-Enters Edit Mode 2D; Controller Wiring Has No Static Guard | `_addProfileObject()` = `createProfile()` + `createAddProfileCommand` (Profile ≠ Solid: no cuboid redo path, no child CFs — #2) + auto `setMode('edit')` → `'2d-sketch'` (#12); `onAfterUndo` exits Edit Mode via `setMode('object')` first; `_enterEditMode2D` without a `sketchRect` clears stale `_sketch.p1/p2`. `checkJs` excludes the controller layer, so every Add-menu entry must be exercised by the smoke E2E — a dangling handler method is a silent no-op (#11) |
| CommandStack push() vs execute() | `_confirm*()` handlers use `push()` (post-hoc recording); never use `execute()` for already-completed operations |
| FrameRotateCommand Must Call _updateWorldPoses | `FrameRotateCommand.apply()` calls `sceneService._updateWorldPoses()` (not just `invalidateWorldPose()`) so fastened constraints propagate immediately on every undo/redo — matching the `SolidRotateCommand` pattern. Omitting the full pass leaves the source Solid one animation-frame behind after undo. |
| Post-Hoc Push Requires Prior Service Call | UI class-change callbacks (`onIfcClassChange`, `onLynchClassChange`) must call the service method **before** `push()`; `push()` alone is a no-op — the domain object is never updated |
| Entity Swap Must Emit Events | Any direct `removeObject()`/`addObject()` call must also emit `objectRemoved`/`objectAdded` domain events |
| Soft-Delete Pattern | `_deleteObject()` uses `detachObject()` + `setVisible(false)`; `dispose()` only in cascade-delete and `_clearScene()` |
| N Panel Read-Only Rows | Always pass `val` argument through to `row()`; never substitute hardcoded 0 |
| Euler Angle Convention | Use `'ZYX'` order (= ROS RPY); never `'XYZ'` for CoordinateFrame rotation display |
| Mouse Rotation Sign | `angle = currentAngle - segmentStartAngle`; screen-CCW swipe → positive angle → CCW on-screen rotation for both PC and mobile |
| Visual State Ownership | `hlMesh.visible` owned by `setFaceHighlight()`; `boxHelper.visible` by `setObjectSelected()`; `cuboidMat.emissive` owned by `_syncEmissive()` — composes `_selected` (blue 0x112244) and `_constraintViolated` (red 0x550000); never set emissive directly from outside MeshView |
| Frame View Must Be Hidden Before Detach | `AddSolidCommand.undo()` must call `meshView.hide()` + `hideConnection()` before `detachObject()`; after detach `_scene.getObject()` returns null so `_hideFrameChain()` silently skips the frame |
| _updateMouse Before Coordinate Picking | Call `_updateMouse(e)` immediately after the canvas guard in `_onPointerDown`; touch devices have no preceding `pointermove` so `_mouse` is stale at first tap |
| CommandStack Clear After Init | Call `_commandStack.clear()` at the end of the constructor after `_addObject()` + `setMode()`; the auto-created initial solid must not appear in undo history. EVERY `clear()` must be followed by `_refreshUndoRedoState()` — `setMode()` snapshots `canUndo` into the uiStore while the boot commands are still stacked, so an unrefreshed clear leaves the header Undo rendered enabled (stale until the next command; found by the ADR-065 Phase 3 locked-state rendering) |
| Urban Placement Confirm No Auto-Select | `_confirmUrbanPlacement()` must NOT call `_switchActiveObject()` after creating the entity; previous selection is preserved and toolbar returns to initial object-mode slots |
| Rect Selection Null Cuboid Guard | `_finalizeRectSelection()` must use `obj.meshView.cuboid?.visible` (optional chaining); Urban entities and CoordinateFrame return `null` for `.cuboid` and would throw TypeError without the guard |
| CoordinateFrame.localOffset vs Geometry.corners | `CoordinateFrame` exposes `localOffset` (LocalVector3[]); geometry exposes `corners` (WorldVector3[]). `.corners` does NOT exist on `CoordinateFrame`. Use `_grabHandlesOf(obj)` for grab/move; use `_worldPoseCache` for world position. (PHILOSOPHY #21 Phase 3) |
| CoordinateFrame ROS TF Local-Frame Semantics | `cf.translation`/`cf.rotation` are parent-local (ROS TF / URDF); world pose is derived by `_updateWorldPoses()` forward kinematics. Grab deltas must be converted to local via the parent world quaternion conjugate before `cf.move()`; the N-panel displays and edits local space directly. |
| `worldPoseOf()` Self-Healing on Cache Miss | `SceneService.worldPoseOf()` calls `_updateWorldPoses()` when the requested frame is not in `_worldPoseCache`. Do NOT add per-call-site guards (`if (CF) _updateWorldPoses()`) — the accessor owns the freshness guarantee (PHILOSOPHY #23). Never call `_updateWorldPoses()` manually before `worldPoseOf()`. |
| HTML Overlay Active Camera | Views projecting 3D→screen for HTML labels must use `SceneView.activeCamera`, not `AppController._camera` (perspective-only); pass `this._sceneView.activeCamera` at each animation-loop call site |
| TC Gizmo Force-Update After Proxy Repositioning | Call `_tc.getHelper().updateMatrixWorld()` after `tc.attach()` in `_attachMobileTransform()`; call `_syncMobileTransformProxy()` from `_toggleTcMode()`. For Solids: proxy position and quaternion are copied from Origin CF world pose (ADR-037 §3); TC arrows align with `Solid.bodyRotation` after R-key rotation. |
| TC Gizmo Does Not Block Object Selection | `if (this._tcDragging) return` must precede the whole `if (result)/else` block in object-mode `_onPointerDown` — otherwise a TC-arrow drag either deselects (arrow off the Solid) or re-attaches the proxy mid-drag to the wrong entity (arrow over a Solid). Empty-space taps still deselect normally. |
| TC Mode Must Match _tcMode | `_attachMobileTransform()` must set `_tcMode = 'translate'` in the non-CoordinateFrame branch alongside `tc.setMode('translate')`; `_tcMode` must always reflect the current TC mode for all code paths |
| Semantic Move Guardrail (checkMoveGuardrail) | Before G-key grab and mouse quick-drag, call `SceneService.checkMoveGuardrail(_selectedIds)`; on `{blocked:true}` show a warn toast and do not enter the drag state. It fires on fastened/mounts links to unselected peers and on Solids with a (transitive) fastened-child CF. All movement preconditions live inside the predicate — call sites never add inline guards (PHILOSOPHY #25). |
| Tension Skips Topological Links | `getLinkDragTension()` must skip links whose `jointType === null` — those links carry no kinematic constraint and drag resistance has no physical meaning for them. Applying tension to them wrongly impedes free movement (PHILOSOPHY #11) and creates asymmetric damping (free toward linked entity, up to 85% resistance away) that users perceive as jitter/oscillation. |
| TC Drag Blocked on Fastened-Source CF | `_updateFastenedFrames()` overwrites a fastened-source CF (and its parent Solid) every frame, so TC drag must be blocked: detect via `isFastenedSource()` / `hasFastenedChild()` (transitive — walks the full CF ancestor chain), set `_tcFastenedBlocked`, toast; on drag end sync the proxy and push no command. |
| R-key Rotation Blocked on Fixed-Joint Source Chain | `_startRotate()` (Solid branch: transitive `hasFastenedChild`) and every CF-rotation entry point (via the centralised `_isFastenedRotationBlocked()` helper) must block rotation on a fixed-joint source chain with a toast — otherwise `_updateFastenedFrames()` silently overrides the input every frame (PHILOSOPHY #11). Named after `jointType='fixed'`, not `semanticType`. |
| _confirmRotate Must Call _syncMobileTransformProxy | After R-key rotation the Solid centroid moves. `_confirmRotate()` must call `_syncMobileTransformProxy()` to re-anchor the TC gizmo to the new centroid — otherwise the gizmo stays at the pre-rotation position until the next pointermove. Pattern mirrors the undo/redo handler. |
| CoordinateFrame Provenance (ADR-034) | Before Grab / R-key / rename / delete of a CoordinateFrame, call `RoleService.canEdit(frame)`; mismatch → `showToast` and return. `frame.declaredBy` is set to `RoleService.getRole()` at creation time. `window.__easyExtrude.setRole()` / `.getRole()` are the console API entry points |
| CoordinateFrame Scale Cap | `updateScale()` must always receive a finite `maxWorldSize`; use `sceneRadius × 0.3` (floor 1.0) as fallback for CFs without a solid parent — otherwise axes balloon to huge size when zooming out |
| Annotation Marker Screen-Space Scale | `AnnotatedPointView.updateScale()` runs every animation frame with `maxWorldSize = max(sceneRadius × 0.05, 0.25)` — world-unit constant sizes (`MARKER_RADIUS` 0.25) are sub-pixel in mm-scale scenes while the HTML label keeps rendering ("icon missing"). `tick()` animation scales multiply by `_viewScale`, never overwrite it |
| Ground Grid Scales With Scene Radius | `SceneView.fitCameraToSphere()` calls `_updateGridScale(radius)`: the 20-unit GridHelper is scaled by `10^max(0, ceil(log10(radius/10)))` (power-of-10 cells, scale 1 for radius ≤ 10) — otherwise the grid is an invisible dot in mm-scale scenes (PHILOSOPHY #27). Any new "frame the scene" entry point must route through `fitCameraToSphere`, not set camera position directly |
| CoordinateFrame Tap Selection | `_hitAnyObject()` never hits a CF (cuboid = null); `_onPointerDown` runs **both** `_hitAnyCoordinateFrame()` and `_hitAnyObject()` then picks via `_isCfDescendantOf()`: CF wins only when it is a child of the found Solid (PHILOSOPHY #22); otherwise the Solid wins — the 0.4-unit bbox fallback must not block selection of nearby unrelated Solids. |
| _promptAddFrame Must Select Frame After Creation | After pushing the command in `_promptAddFrame()`, call `_switchActiveObject(frame.id, true)` — otherwise the frame stays hidden in the 3D viewport. Undo restores parent selection; redo re-selects the frame. |
| _hitAnyEntityForLink CF Priority | `_hitAnyEntityForLink()` must call `_hitAnyCoordinateFrame()` as Step 0 before the cuboid raycast; CFs sit on top of Solids so the cuboid step would return the Solid, causing `_computeLinkOptions(CF, Solid)` to omit "Fixed · Fastened". |
| SpatialLink Two-Layer Taxonomy | Every `SpatialLink` has `jointType` (URDF kinematic, may be null) + `semanticType` (domain annotation). Constraint solver activates when `jointType === 'fixed'` for CF-to-CF links (except `semanticType === 'mounts'` which has its own path). Unfasten context menu uses `jointType === 'fixed'` not `semanticType === 'fastened'` — all fixed joints are unfastenable. See ADR-038. |
| Fastened Constraint Limitations | Full 6-DoF rigid-body solve derives the absolute world pose from invariant local data only — never `_worldPoseCache` / `avg(corners)` (PHILOSOPHY #24). One fastened source CF per Solid; `loadScene`/`importFromJson` must call `_updateWorldPoses()` + `_reactivateLiveLinks()`; CF chains propagate via `_findAncestorChain()`; cycles are detected per frame, skipped, and surfaced via `constraintCycleDetected`. |
| SemanticInferencer Integration (ADR-041) | `_runSemanticInference()` runs only at the end of `_confirmGrab()` / QuickDrag confirm (single moved Solid; never inside the animation loop). The inferrer is pure. Starting a new drag dismisses any pending SemanticSuggestion banner first — a stale banner's "Link" button leaves the new drag's ghost/tooltip alive. |
| Drag Suggestion Lifecycle (ADR-041 Phase 2) | Ghost `SpatialLinkView` + drag tooltip created in `showDragSuggestion()` must be disposed on all four exit paths (inference lost / pointerup confirm / Enter accept / banner "Link" click). `AppController._ghostLinkView` is the sole owner and `hideDragSuggestion()` the sole disposer (PHILOSOPHY #4/#9). |
| importFromJson Solid v1.3 Coverage | `_reconstructEntity` must mirror `_deserializeEntities` (v1.3 primary triple via `setPose()` before the legacy `vertices` path). Scene-JSON format changes touch BOTH deserialization paths in one commit; new scene-JSON producers must be verified through `importFromJson` end-to-end — golden JSON equality alone does not prove loadability (PHILOSOPHY #11). |
| stated→derived Promotion Bails, Never Throws | `promoteAdmissible()` runs on the compile path; `tryPromote()` wraps BOTH `compileMonotoneExpr` and `invertCriterion` in one `try/catch (NotPromotable)` and returns null (stays `stated`; R9 governs). Never throw, never emit an invalid interval — a throw aborts the whole context compile. Input requirements are never mutated. |
| Validator Rule Order (R0' → promote → R6/R7/R9/R8) | R0' validates human-authored requirements first; `promoteAdmissible()` yields `liveRequirements` + `promoted[]`; R9/R6/R7/R8 and the Decision-`relaxes` check all read `liveRequirements`. R8 keys on the Actor `discipline` field via `RoleKpiCatalog`. Form projection reads only the validator's `openQuestions`, never re-validates. |
| R6 Region Conflict Is AABB-Only (Helly-2D Caveat) | Region admissibles intersect per axis via `RegionGeometry.intersectBoxes` (the single half-open `[min,max)` helper). Per-axis emptiness decomposition is valid for axis-aligned boxes only (Helly caveat; polygons rejected at R0'). Mixed interval/region variable buckets are skipped, not thrown. A region admissible still constrains ONE variable. |
| PredicateEngine Purity & Blocked-Before-Evaluate | `evaluatePredicate()` is pure, THREE-free, returns `{pass, violations}`, throws only `MalformedPredicate` (surfaced as a validator error). A blocked check (assumed/unknown required fact) is never evaluated. Robotics kinds `robot_reach`/`collision_free` (`context/0.4`) consume pre-baked measurement operands; empty `contacts` is a legitimate pass, empty `targets` is malformed. Adding a kind = `VALID_PREDICATE_KINDS` + engine switch + version bump. |
| Validator Additive Return Contract (`checkResults`) | `validateContext` must return `checkResults` (predicate `pass\|fail\|blocked` records) on **every** return path including the early `!ctx` null-guard — same discipline as `promoted` (ADR-049 Phase 2). `compileContext` passes it through. A new scene/predicate consumer assumes the field exists; omitting it on one path is a silent `undefined`. |
| Robotics Measurement Instrument: Pure Kernel + ComputeBackend Seam + RoboticsService Receptacle (ADR-053 Phase 2) | The operand producer lives in `src/robotics/*` (pure, THREE-free) behind the single `ComputeBackend.run(job)` seam; the pure-JS `LocalComputeBackend` is the blessed initial form and KDL/ruckig-WASM/BVH/server swap in behind the same seam. `RoboticsService` (no pure logic, backend injected) bakes results into the check's predicate as a NEW doc and throws on a missing check / kind mismatch. `margin` is a length-unit workspace clearance, NOT a singularity margin. |
| C++→WASM Build Lane: Committed Artifact, Off the Default Build, Toolchain via Setup Script (ADR-053 Phase 3) | `robotics-wasm/` (KDL + ruckig + Eigen as pinned submodules) builds ONE embind module whose artifact under `src/engine/robotics-wasm/` is committed — `vite build`/CI never need a C++ toolchain. `build:robotics-wasm` is NOT wired into `pnpm build`; toolchain via `scripts/setup-toolchain.sh`; KDL uses the old Tree interface (no Boost); the smoke test stays out of `test:context`. |
| PersonaProjection Consumes `validateContext`, Not Raw `detectConflicts` (ADR-049 Phase 4) | The pure projections read the validator result (`conflicts[].resolvedBy` etc.), never raw `detectConflicts` — that leaves `resolvedBy` undefined and Decision-settled conflicts silently render as live conflicts. The opt-in `{approvedRefs}` gate distinguishes `proposed` from `resolved`; omitting it stays backward-compatible. `projectRegionGhosts` reuses `RegionGeometry.intersectBoxes` and sorts regions by `ctx.actors` for a deterministic palette (colour is view-side). Matrix/Cluster render as tabs inside the existing Context Inspector, never a new edge panel (PHILOSOPHY #26). |
| ContextService Owns the Canonical Doc; Scene Is Derived (ADR-050) | Side-effect coordinator with no pure logic (PHILOSOPHY #3). `loadContext(doc, vc)` is the single authoritative entry (validate → compile → import → rebuild ref maps → emit `contextLoaded`); it throws on failure (caller toasts). Every doc change yields a NEW doc (#6). Approval is a real doc mutation (`proposed→agreed`) and does not regenerate the scene; `applyAdmissible` does. `applyContextDoc` compiles BEFORE mutating state; accessors own their freshness (#23). |
| ContextController Is a Persistent Overlay; Approval Goes Through the CommandStack (ADR-050 Phase 2) | The production overlay coordinator (like `MapModeController`, NOT a `setMode()` FSM state); consumes `ContextService`, never re-implements validation/compile/projection; separate uiStore slice (`context`) from the demo (`demo`). Approval must go through `createApproveDecisionCommand` + `push()`; the controller re-projects only from the `contextChanged` subscription so approve/undo/redo repaint through one path (PHILOSOPHY #5). Matrix/Cluster components are prop-driven and shared demo/production. |
| ContextController Authoring/Ghost: Recolour Live, Commit Once, Regenerate on Drag-End (ADR-050 Phase 3) | Single `_mode` (`negotiate\|author\|ghost`). A live drag recolours only — re-validate a cloned `_editCtx`, never the canonical doc (PHILOSOPHY #6/#7); pointer-up commits ONCE via `createEditAdmissibleCommand` (regen on resolve; on reject roll the widget back + toast). Widgets/ghosts are solely owned: created on enter, disposed in `exit()` (#4/#9). `_reproject()` dispatches by `_mode`. No bundled-example bootstrap — a missing doc gets a guiding warn toast. |
| ContextController Blank-Doc Entry: adoptDoc Skips Compile/Layout; addDocEntry Goes Through CommandStack (ADR-051 Phase 1) | Blank docs go through `ContextService.adoptDoc` (validate, clear scene, `_compiled=null`, emit) — never `loadContext`, which throws on the intentionally missing `specification`. `addDocEntry` snapshots before/after docs through `createAddDocEntryCommand` + `push()`; `_reproject()` refreshes `context.actors`/`variables` so IntakePanel dropdowns stay current (PHILOSOPHY #5). |
| ContextController Template Gallery: Pure Catalog, Side-Effectful Doc Resolution (ADR-051 Phase 2) | `TemplateCatalog` is pure metadata (no JSON imports; never holds a doc); resolving an example file to a doc is the controller's `TEMPLATE_DOCS` import map with a module-load coverage check (PHILOSOPHY #11). `selectTemplate` exits any active overlay first (#9); blank → `adoptDoc`, example → `_loadThen(doc, _startNegotiation)`. The gallery footer states the scene-replacement consequence — no second confirm dialog. |
| ContextController Fork & Tweak: Seed Is a Read-Only Mirror, Not a Second Source (ADR-058 Phase 1 + actor/variable seed chips) | `forkExample` deep-clones the bundled example into the working doc through the single authoritative path (`_loadThen` → `loadContext`), then sets `context.authorSeed` AFTER `_startNegotiation` (which resets it). The seed is a read-only mirror, never a second source of truth. `SeedAnchor` is the pure index (skips entries without a string `ref`); the shared `SeedChips` component fills actor/variable/requirement forms, suffixing `ref` with `_copy` to avoid duplicating the forked doc's refs. |
| ContextController In-Place Edit: Every Entry Is a Click-to-Edit Card; the Ref Is Locked; the Command Is Generic (ADR-058 Phase 2) | Existing actor/variable/requirement entries render as `EntryCard`s that unfold (`Reveal`) the SAME form in `mode:'edit'`, pre-filled with the `ref` **locked** (identity; a rename would orphan referencing `by`/`constrains`, so rename = remove + re-add). The pure `updateActor`/`updateVariable`/`updateRequirement` (replace by `ref`, upsert if absent — never silently drop, #11) + `removeDocEntry` in `DocBuilder.js` spread `initial` first so unmanaged fields (`evidence`/`note`/`source`) survive an edit (#6). `ContextController.editDocEntry`/`removeDocEntry` snapshot before → pure builder → the generic `createDocEditCommand` (`AddDocEntryCommand` now delegates to it — one implementation, §1.1) pushed on the CommandStack (undoable); a compile-orphaning edit throws → toast, no push (#11). `context.requirements` (via `contextSetRequirements`) is projected by `_startNegotiation`/`_reproject` so the cards stay current (#5). Region requirements are read-only cards + a `region` badge — the interval form can't represent a region, so they route to Author-mode widgets, not a lossy edit (#11). The edit form drives the live `onIntakePreview` band too (text ⇄ 3-D). |
| Intake Live Checks Are the Validator's Own Predicates; the Gap List IS the Submit Predicate (ADR-058 UX) | Field-level live checks import the validator's exported predicate (`isInterval` — reference identity machine-tested); each form's `*Gaps()` list is the single submit predicate (disables the button AND prints the reason line — no silent disabled, #11). The playful layer (seed tint, ref suggestion, KPI chips, dual slider) is form-local derivation via pure `IntakeAssist`; the only doc-mutating exit stays `onAddDocEntry`. |
| KPI Expression Assets: One Catalog Feeds R8 AND the Chips; Instantiation Never Guesses (ADR-063 Phase 1+2) | `ROLE_KPI_CATALOG` (`role-kpi/2.0`) is the single source for BOTH the R8 mandatory check (`requiredKpis` normalizes via `kpiEntryName` and must keep accepting legacy 1.0 name arrays — a `ctx.kpiCatalog` override may be 1.0-shaped) and the intake chips (`kpiCatalogChips` passes 2.0 assets through whole; fabricates nothing for a 1.0 name). `instantiateKpiExpr` leaves unresolved `{…}` placeholders verbatim, and `requirementGaps` names the leftover — never silently commit a non-promotable templated expr (#11). The form re-instantiates `{var}` ONLY while the expr is still the pristine instantiation for the previous variable; user edits are never rewritten. Selection vocabularies come from `IntakeVocabulary` (schema enums by array reference; `DISCIPLINES` derives from the catalog keys so every R8-obligated discipline is selectable); unit datalists suggest, never restrict. |
| Wizard Is an Ordered Vessel, Never a Write Path; the FSM Is Pure and Its Gate Reads Only Committed Entries (ADR-063 Phase 3) | `WizardCatalog.js` owns the definition asset (`wizard/1.0` — step order lives in data, never scattered in components) AND the pure FSM (`null` / `step(k)` / `review`); `nextWizardState` returns the SAME state when `wizardStepGaps` is non-empty (illegal transitions unrepresentable, never thrown) and the same reason list is what the panel prints (no silent disabled — #11). Each step's `formGaps` is the IntakeAssist predicate by REFERENCE (machine-tested — a looser wizard copy re-opens "passes the form, fails the commit"). `ContextController` is the sole writer of `context.wizard` (replaced wholesale, like `context.grasp` — #5) and enforces the `next` gate against the authoritative `getDoc()`; the panel derives the same gaps from the projected slice — one predicate, two projections. `WizardPanel` embeds the SAME exported IntakePanel forms (+ shared `IntakeSharedDefs`); the only doc-mutating exit stays `onAddDocEntry` (per-step CommandStack commits — exiting mid-wizard keeps a valid, undoable doc; `finishWizard` is a view transition, not a commit). Blank docs open on the `'wizard'` tab; the expert Intake tab is never removed. |
| Parametric Asset: the 3-D Viewer Is an Input Device; Commit Writes Numbers, Never Boxes (ADR-063 Phase 4+5) | `ParametricAssets.js` is the pure registry (`parametric/1.0`): param schema + `fragment(values)` -> `layout/1.0` fragment (data ON the DSL, never a language change). `clampParams` is TOTAL (runs per slider keystroke - the preview must not die mid-drag, #11); `applyAssetCommit` upserts variables by ref + replaces ONE asserted fact, so a recommit overwrites and never duplicates (#6). The ghost preview (`ParametricPreviewView`) deliberately does NOT go through `importFromJson` - the canonical scene and undo stack stay untouched by a live drag; the ONLY doc-mutating exit is `commitAsset` -> the generic `createDocEditCommand` (one commit = one undo). `ContextController` is the sole writer of `context.assetViewer` and sole owner of the view (disposed on close / overlay exit / `_startNegotiation` re-entry - #9). The wizard variables step offers the same panel via the declarative `assetSource` flag (choice sources live in the definition asset, not the component - kernel 1.1). |
| ContextController Live Intake Preview: One Ghost, Updated In Place (ADR-051 Phase 3) | One `UncertaintyGhostView` (`_intakeGhost`, sole owner — disposed in `previewIntake(null)` and `exit()`). Keystrokes call the additive `setIntervalPreview` (moves/rescales in place, no geometry rebuild; no-op while collapsing). Camera framed once on first appearance. The form clears the preview on unmount and after submit; a committed requirement clears WITHOUT a collapse animation — it is not a Decision (PHILOSOPHY #11). |
| NL Intake Bridge Is Pure, Deterministic, and Conservative (ADR-051 Phase 4) | `extractFacts` is pure and conservative: definite numbers → `asserted {value, unit}`; anything vague/unknown → `status:'unknown'` with the raw text preserved in `note`; un-parseable segments go to `unparsed`, never dropped (PHILOSOPHY #11). Commit folds the batch into ONE `createAddDocEntryCommand`. The additive `canonical={subject,attr}` field (via `SynonymQuotient.canonicalKey`) is inert — never wire it into rendering; surface terms stay verbatim. |
| ContextController Form + Persistence: AnswerQuestion Goes Through CommandStack; `.ctx.json` Is the Project File (ADR-050 Phase 4) | `answerQuestion` snapshots before/after docs through `createAnswerQuestionCommand` + `push()` (a form answer regenerates, unlike approval). Re-projection is event-driven via `contextChanged` — one path for answer/undo/redo (PHILOSOPHY #5); the form is empty exactly when `projectForm()` returns `[]` (#11). `.ctx.json` import = `_loadThen(doc, _startNegotiation)` (project-open boundary); export = `JSON.stringify(getDoc())` — the doc IS the artifact. |
| ProvenanceTree Is Pure φ⁻¹ Over the Canonical Doc; Adds No Data Structure (ADR-052) | `buildWhyTree`/`recoverProvenance` are pure φ⁻¹ over the canonical doc and add NO doc field (they synthesise already-scattered relations). Every edge is oriented derived→source (What-ward → Why-ward). Marker collection does NOT resolve `$fact`/`$decision` values — provenance must stay total on an uncompilable doc (PHILOSOPHY #11). The Gap stays owned by `validateContext().conflicts`; callers join by variable ref. The service delegates and returns `null` before a doc loads (#3/#23). |
| Why Breadcrumb: Selection Is the Single Provenance Trigger; the Gap Join Lives in the Service (ADR-052 Phase 2) | `AppController._syncContextProvenance()` is the single authoritative reader of selection → `ContextController.showProvenance(id\|null)` (PHILOSOPHY #5/#23); only a single, context-derived selection in negotiate mode shows a breadcrumb — everything else clears it (#11). The R6 Gap join lives in `ContextService.recoverProvenance` (additive `gaps[]`), NOT in the pure layer; `_reproject()` re-joins for the tracked entity so approve/edit/undo/redo refresh through one path. |
| Why Tree Overview Is the Whole-Doc Complement to the Selection-Driven Breadcrumb (ADR-052 Phase 3) | `WhyTreeView` renders the entire doc as the Why-rooted 5W1H tree (`buildWhyTree`), roots surfaced first; pushed by `_startNegotiation()` and re-pushed by `_reproject()` in negotiate mode (PHILOSOPHY #5). It complements the selection-driven `WhyBreadcrumb`; no new doc field, no pure/service-layer change. |
| SynonymQuotient Is the Quotient Map; ProvenanceNarrative Is the doc → NL Return Leg (ADR-052 Phase 4) | `canonicalize(term)` projects a surface term onto its equivalence-class key (φ on the lexicon); `localize(key, lang)` picks ONE representative back — never the full preimage (ADR-044 §φ⁻¹). The narrator renders provenance through `localizeOperator`; unknown terms pass through verbatim (PHILOSOPHY #11). `QUOTIENT_TABLE` is frozen; adding a row widens the quotient without touching either bridge. This dictionary is where `NlIntake` (φ) and `ProvenanceNarrative` (φ⁻¹) meet. |
| A Requirements-Only Doc Has No Renderable Layout; Every Entry Path Must Accept It (ADR-051 Entry A / ADR-050) | A valid authoring-stage doc may derive no scene (blank doc without `specification`; requirements-only doc with `entities:[]`). Four guards: validator R3 gated on `ctx.specification !== undefined`; `loadContext` skips `compileLayout` on empty entities (shared `_emptyScene()`); `AppController._onContextLoaded` accepts `compiled === null`; `applyContextDoc({regenerate})` mirrors load/adopt gating. Accept the authoring-stage doc on EVERY entry path. |
| GraspController Owns the Grasp FSM as a Discriminated Union; the Panel Is the `'grasp'` Tab, Not a Modal; Scoring Reads Only the Contract (ADR-054 thread, ADR-057 placement + state machine) | `GraspController` (uiStore injected; THREE-free tests) owns `context.grasp` as a status-discriminated union (`idle/no-layout/compiling/solving/results/error`) replaced wholesale — sole writer (PHILOSOPHY #5), panel only reads. The ONLY DSL source is `ContextService.getCompiled()?.layoutDsl` (never `decompileLayout`). `runGraspSearch` is a query — never on the CommandStack; the UI never sets `contractVersion`. The panel is the `'grasp'` tab inside `ContextLayer` (no new edge footprint — #26); scoring reads only the contract `score`; only the *typed* `pose.kind:'endEffector'` branch reaches 3-D, through the ADR-059 capability gate (opaque/`jointSpace` never interpreted). |
| Grasp Ghost Is a Gate-Kept Derived Projection; the View Is Factory-Injected; the Frame Convention Lives in ONE Constant (ADR-059 Stage 1) | The pure gate `renderableEndEffectorFrame` is the ONLY ghost-eligibility decision, shared by `GraspController._syncGhost` and the panel caption (never duplicated inline); a failing pose gets no ghost + an honest caption (#11). `GraspGhostView` (THREE) is factory-injected into the THREE-free-tested controller; hover stays controller-local (never in the `context.grasp` union); disposal = new run / idle re-seed / `ContextController.exit()`; loop clock is seconds, view constants ms. The base-frame assumption is sealed in the single `FRAME_CONVENTION` constant; target highlight uses the view's own EdgesGeometry, never `cuboidMat.emissive`. |
| Grasp Diagnostics Is a Wire Fact Displayed, Never a Judgment Re-Implemented (ADR-061) | The contract-v3 `diagnostics` rejection funnel rides the `results` state verbatim, plus the explicit `prevDiagnostics` carry-over (a derived display aid for the delta view, not a second source); the `error` state never carries either. ALL presentation (stage bars, dominant-stage highlight, delta chips, the near-miss meter curve `1/(1+miss)`, wording) derives via the pure `GraspFunnelMath` (THREE-free; malformed/absent → `null`, never a fabricated funnel — #11). Never re-implement reach/IK/collision reasoning client-side; `candidatesGenerated === 0` renders the input guide instead of bars; `reachNearestMiss` always prints the raw number (geometry unit) next to the feel meter. |
| LayoutDecompiler Is the Scene→DSL Normal-Form Inverse; the Scene Fixpoint Is the Law; Context Stays Canonical (ADR-055) | `decompileLayout(sceneJson) → {dsl, warnings}` is the pure inverse of `compileLayout` up to a normal form (`strategy:'manual'`, explicit positions, prefix-stripped refs — PHILOSOPHY #28); the law is the scene fixpoint `compileLayout(decompileLayout(scene)) ≡ scene`. Solid `rotation` is additive within layout/1.0 (no version bump). Inexpressible types and unconvertible links go to `warnings[]` (#11). NOT wired into the Context flow — the canonical DSL for a loaded Context stays `getCompiled().layoutDsl` (ADR-054). |
| CanonicalForm Is the Computed WL Normal Form on the Quotient; Colours Need a FIXED Round Count to Be Cross-Doc Comparable (ADR-056) | Pure WL colour-refinement signature over `buildWhyTree` on the synonym quotient; adds no doc field. THE non-obvious invariant: WL runs a FIXED `WL_ROUNDS=16`, never an early stop — a doc-dependent round count silently breaks cross-doc colour comparability, i.e. `structuralDiff`/`reconcile`. `identityPayload` holds only ref-free canonical scalars. `canonicalForm(ctx)` is the versioned JSON-serializable output (`canonical-form/1.0`, no Map/internal leakage); `verify(a,b)` = signature equality; diff aligns by colour then id, reconcile is max matching within colour classes. Embedding/similarity ranking is out-of-scope (§3). |
| Proof-Feedback Presentation: Shared Primitives, Fact-Fed Only, History Is Component-Local (ADR-062 Phase 1–3) | `DeltaChip`/`LandingFlash`/`flashAnim`/`FeedbackDefs`/`usePrevOnChange` (`src/components/Feedback/FeedbackPrimitives.jsx`) + pure `refsSignature`/`listDelta`/`settledRefs` (`src/view/FeedbackMath.js`) are the ONE feedback vocabulary — new input surfaces wire them, never re-implement delta/flash inline. Inputs are always proof-layer facts (`projectForm()`, validator `conflicts`, matrix `variableSummary`, order `approved`, contract `diagnostics`); an unkeyable snapshot degrades to `null`, never a guessed identity (#11). The previous snapshot behind a chip/flash is component-local React state — NEVER a uiStore or wire field (same rule as grasp hover, ADR-059); `refsSignature` absorbs re-projection array-identity churn. 0/null renders nothing; `FeedbackDefs` must be mounted with each overlay root (`ContextLayer`, demo `ContextInspector`, `IntakeSharedDefs`). |
| Checks Surface: Verdicts Verbatim, One Meter Curve, Blocked Means No Meter (ADR-062 Phase 4) | `ContextService.projectChecks()` is the ONE join of validator `checkResults` (verdicts, verbatim) with the doc's baked predicate operands (ADR-053 §9), re-projected through `_reproject()` (one path — #5). Status flips need the status-aware `checkStatusKeys` snapshot (`refsSignature` alone misses them); `checkMeter`'s closeness is the imported ADR-061 `nearMissCloseness` — never a second curve — with raw worst/required always printed. A blocked check gets NO meter (its operands were never evaluated); its card names the blocking question refs instead (#11). |
| Template Structure Preview Derives From the Canonical Form and Never Compares (ADR-062 Phase 5) | Gallery card previews derive from `canonicalForm(doc)` (ADR-056 fact) via the pure `structurePreview` (layer counts + signature prefix only — no colour recomputation, no diff/reconcile, no similarity ranking, ADR-056 §3). `ContextController._templatePreviews()` memoizes the map from bundled `TEMPLATE_DOCS`; a failing derivation yields `null` → the card shows no preview (#11). |
| Reduced Motion Degrades the Play Layer to a Static Cue, Never to Nothing; matchMedia Is Read at ONE Boundary (ADR-064 Phase 4) | The proof-feedback flashes/pulses respect `prefers-reduced-motion`: under reduced motion `flashAnim`/`LandingFlash` return a *static* tint and the `ContextInspector` Badge infinite pulse + `IntakePanel` Section badge scale-pop drop to `animation:none` — the information ("a fact changed / needs attention") is preserved, only the movement is dropped (the meaning is the notification, not the fade — PHILOSOPHY #29; never a silent disappearance — #11). The shape decision is the pure `flashStyle(tone, reduced)` in `FeedbackMath.js` (unit-tested for both branches); the ONLY side-effect read of `window.matchMedia('(prefers-reduced-motion: reduce)')` lives in `src/theme/motion.js` (MOVED there by ADR-065 Phase 1 so the 3D MotionGovernor shares it; `FeedbackPrimitives.jsx` re-exports `prefersReducedMotion()` for the non-hook `flashAnim` and keeps the `useReducedMotion()` hook for live components; the grep test `src/theme/motion.test.js` pins the single read) — guarded so a non-browser env (node --test) reads `false` = motion allowed, leaving default rendering unchanged. New animated surfaces consult that one boundary; they never call `matchMedia` inline (§1.1). `useReducedMotion()` is a hook — call it unconditionally at the top of the component, never behind `&&`/`?:` (a prop-gated short-circuit like `pulse && !useReducedMotion()` changes the hook count when the prop flips at runtime and React throws — real crash: demo ConflictsTab `pulse={!c.resolvedBy}` on Decision approval). |
| Smoke E2E Verifies Wiring Liveness Only, on a Separate CI Job, Against the Dev Server (ADR-064 Phase 4) | `e2e/smoke.spec.js` (Playwright, `playwright.config.js`) is a NARROW smoke — boot → box add → undo → sketch add → rect drag → extrude → undo → template load → negotiate tab, plus a reduced-motion pass — that proves the experience layer's wiring is live end-to-end, NOT a coverage net (PHILOSOPHY #20). Add-menu entries MUST each appear here: `checkJs` is scoped to types/domain, so controller wiring has no static guard (the Sketch entry was a dangling call for weeks). It is a SEPARATE CI job (`e2e`) from the unit `gate` so its flakiness never blocks type/contract checks. It runs against `pnpm dev` (not `preview`) because the dev server sets COOP/COEP, so `crossOriginIsolated` is true and the coi-serviceworker self-skips — no registration/reload loop under headless Chromium. Assertions lean on user-visible text/roles (`Scene Collection`, `+ Add`, `Negotiate`, `Matrix`) — no test-ids added. `@playwright/test` is pinned to 1.56.x (ships the sandbox's Chromium build 1194); CI fetches the matching browser via `playwright install chromium`. Artifacts (`test-results/`, `playwright-report/`) are gitignored. |
| DSL Schema Is the SHAPE Contract; the JS Validator Keeps the MEANING; Enums Are Pinned by a Drift Test (ADR-064 Phase 2) | `schema/{layout-1.0,context-0.4}.schema.json` are the closed, versioned JSON Schema artifacts for the two DSLs this repo declares as its surface (`additionalProperties:false` + version field, same treatment as grasp-contract — PHILOSOPHY #29). They fix ONLY the shape: the closed field set and the enum vocabularies. The MEANING contract stays in `LayoutValidator.js` (ref resolution/uniqueness) and `ContextValidator.js` (R1–R9: orphan-spec, conflicts, negotiation clusters, blocked acceptance, stated→derived promotion, role-KPI obligations) — a schema cannot express "every spec element has a trace link". Domain-open bags (`Fact.attrs`, `constraint.properties`, `meta.baseline`) are deliberately `additionalProperties:true`, like grasp-contract's `graspSearchDeclaration`. §1.1 single-source: the schema enums and the `*DslSchema.js` constants are pinned equal by the drift tests in `src/schema/{Layout,Context}Schema.test.js` (a constant gaining a value the schema lacks fails CI) — never re-list a vocabulary as a third source. Conformance is example-driven: every `examples/*.json` must pass, a smuggled field / bad enum / bad version must fail. The schema `version` accepts `SUPPORTED_VERSIONS` (0.1–0.4) since each context/0.x is an additive superset; authoring-stage docs (requirements-only, no `specification`) must still conform (ADR-051 Entry A). ajv lives at root devDep (a separate instance from the BFF's). |
| Design Tokens Are the Single Colour/Duration Vocabulary; the Palette Table Is Drift-Tested (ADR-065 Phase 0) | `src/theme/tokens.js` (`COLOR`/`DURATION`/`EASING`/`Z`, pure/THREE-free — pure math modules may import it) is the ONE source of the UI vocabulary; `docs/LAYOUT_DESIGN.md` § Color Palette is pinned equal to `COLOR` in BOTH directions by `src/theme/tokens.test.js` (§1.1 — same mechanism as the ADR-064 schema drift tests; never re-list the palette as a third source). Opportunistic migration: any line you TOUCH carrying a palette hex literal must be rewritten to consume the token; untouched literals stay until touched. No theme switcher — dark-only until a trigger (核 §5). |
| MotionGovernor Is the Single Owner of Transient 3D Effects; the Reduced-Motion Read Lives in ONE Module (ADR-065 Phase 1) | Every transient effect (RippleEffect lineage: constructor adds / `tick(t)→done` / `dispose()`) spawns through `MotionGovernor.spawn(reduced => fx)` — the governor enforces the concurrency budget (oldest evicted WITH `dispose()`, #9; undo/redo mashing is absorbed here, not at call sites) and prunes done→dispose in `AppController._animate`. The ONLY `matchMedia` read of `prefers-reduced-motion` is `src/theme/motion.js` (grep-pinned by `src/theme/motion.test.js`; `FeedbackPrimitives` re-exports — the ADR-064 boundary MOVED, it did not fork). Under reduced motion an effect renders a STATIC held cue (fixed scale/opacity for its duration), never a silent skip (#11, PHILOSOPHY #30). Persistent animated views (GraspGhostView etc.) keep their existing owners — the governor owns only transients. |
| Celebration Renders a Fact TRANSITION, Budget 1, Persisted Nowhere (ADR-065 Phase 4) | A celebration is the high-production rendering of a transition between two committed fact snapshots — never a level, never the initial load (`prev=null` fires nothing — machine-tested on every predicate in `CelebrationMath.js`). The predicates are pure (`clearedTransition`/`allGreenTransition`/`commandMilestone`); malformed snapshots → null (#11). Budget 1 concurrent is STRUCTURAL, not counted: `pickCelebration` returns at most one descriptor (priority all-green > conflicts-cleared > questions-cleared — one re-projection can satisfy several), and exactly ONE `ContextCelebration` watcher mounts at the overlay ROOT (a panel-local watcher would lose its `usePrevOnChange` history on tab switch). The burst replay key is the WINNING fact's own tick, never a combined tick — a stale-prev descriptor across an unrelated re-projection must not replay. 3D milestone: `commandMilestone` runs BEFORE the landing-label filter (every push counts; the fact is `CommandStack.depth`, a session fact — cross-session achievements stay forbidden); `CelebrationField` is InstancedMesh (1 draw call), RippleEffect lineage, `MotionGovernor.spawn` only. Session-fact history (`_lastCommandDepth`) is controller-local, seeded after boot `clear()`. Nothing persists, nothing rides the wire. |
| Landing Effects Speak Only Entity Lifecycle; Routine Pose Ops Are Silent by Design (ADR-065 Phase 2, volume revision) | The ONE fact source stays `CommandStack.setLandingListener` (`{phase, label}` after push/undo/redo — committed facts only; the listener attaches AFTER the constructor's `clear()`, so the boot solid fires nothing). A landing effect fires ONLY for an entity APPEAR/VANISH transition (`lifecycleDescriptor` maps Add/Extrude/Delete labels × phase → materialize/dissolve; undo inverts, redo re-applies). Move/Rotate/Face Extrude and their undo/redo return `null` — machine-pinned SILENCE: their result is already visible at the anchor, so a same-shaped per-operation pulse carries zero information = decoration under the #30 one-sentence test (the 2026-07-11 user feedback: "every-operation fireworks are noise"). `lifecycleDescriptor`/`boundsOf`/`voxelFrame`/`voxelJitter`/`glitchGate` (`CommandFeedbackMath.js`) are pure; malformed/unrecognised → `null` (#11 as honest silence). Anchors come from the domain events (`objectAdded` / the additive `objectRemoved` 2nd arg — a vanished entity cannot be re-read after detach), held in controller-local `_lifecycleAnchors` (last-wins; frames have no corners and never override) and CONSUMED (reset) on every landing so a scene load's event flood cannot anchor a later effect. `VoxelBurst` is one InstancedMesh overlay (1 draw call) — never entity emissive (#4), deterministic directions/jitter (replay-identical), sized from entity bounds (#27); glitch flicker rides per-instance scale (one shared material has a single opacity) and never runs under reduced motion (static held shell, #30). |

---

## 2. Events & Interaction (Touch/Pointer)

Detail: `docs/code_contracts/interaction.md`

| Rule | Core Takeaway |
|------|--------------|
| Touch vs. Pointer Asymmetry | Re-run hit tests in `_onPointerDown` before `_handleEditClick`; hover does not fire before touch tap |
| Gesture-Based Interaction Priority | Auto-start face extrude on touch tap in Edit 3D; gesture-only, no toolbar button |
| Interaction Confirmation Lifecycle | FaceExtrude confirms on `pointerup`; Grab on touch confirms via toolbar only, never in `pointerup` |
| Mobile Rotate Interaction Lifecycle | Rotate button calls `_startRotate(true)`; each canvas touch re-anchors `segmentStart*`; `_onPointerUp` keeps rotate active; confirm via toolbar only |
| Grab State: allStartCorners vs segmentStartCorners | `allStartCorners` = undo anchor (never updated mid-grab); `segmentStartCorners` = per-drag delta, re-snapshot on touch re-grip AND on `_setGrabAxis()` axis switch (also reset `startMouse` and update `_grab.centroid`/`pivot` to the current position — otherwise the axis guide stays at the original grab-start). |
| Rotate Segment Re-snapshot on Axis Change | `_setRotateAxis()` must mirror `_setGrabAxis()`: on axis change re-snapshot `segStartOrientation`/`segmentStartRot` from the current orientation, update `segStartPos`/`segStartPivot` from `obj._position`, and reset the angle accumulators — otherwise the next `_applyRotate()` discards accumulated rotation and the first delta jumps. |
| Global Event vs. UI Event Delegation | First guard in `_onPointerDown`: `if (e.target !== renderer.domElement) return` |
| OrbitControls Disable Strategy | Use `matchMedia('(pointer: coarse)')` not `innerWidth < 768`; disable only for single-finger-consuming ops |
| Registered Window Handlers Must Stay Defined | Every `this._handlers.X` registration in `_bindEvents()` must have a defined `_onX` dispatcher on AppController — deleting one during handler-class extraction throws a TypeError per event that the window listener swallows silently. `_onWheel` dispatch order: Map zoom → rotate `cycleStepSize` (Ctrl) → grab `cycleGridSize` (Ctrl) → OrbitControls. |
| Context Authoring Pointer Delegation (ADR-049 Phase 3 demo, ADR-050 Phase 3 production) | `_onPointerDown/Move/Up` delegate to `onAuthor*(e)` after `updateMouse` and before the selection branches, for BOTH `_demoCtrl.isAuthoring` and `_ctxCtrl.isAuthoring`; handlers return true only when a handle consumed the event (orbit stays independent — PHILOSOPHY #22b). Live drags re-validate a cloned `_editCtx` (#6); production commits once on pointer-up via `createEditAdmissibleCommand`. |

---

## 3. UI & Layout Adaptability

Detail: `docs/code_contracts/ui_layout.md`

| Rule | Core Takeaway |
|------|--------------|
| Three.js Canvas Must Mount in #canvas-container | `SceneView` appends the renderer to `#canvas-container` (z-index 0), never directly to `document.body` — the implicit stacking order is fragile with a React overlay. Explicit contract: canvas 0 → GizmoView 10 → `#react-ui-root` 100. |
| Mobile Toolbar Stability | Fixed slot counts per mode; use `disabled` + `{spacer:true}`, never removal. Object mode 5 slots; CF slots intentionally mirror Solid positions for cross-entity muscle memory; transient operation bars: slot 1 = Cancel/Back, slot 4 = Confirm, regardless of operation. |
| CF Label Position Order | `_updateWorldPoses()` must run BEFORE `updateLabelPosition()` in the animation loop; calling label update on a stale `_group.position` (previous frame) causes the label to lag one frame and appear to vibrate at startup. |
| CF Label getBoundingClientRect Cache | `CoordinateFrameView.updateLabelPosition()` caches `getBoundingClientRect()` keyed on canvas client size (per-frame calls return varying values during mobile viewport-resize animations → 0.5-px jitter) and writes the DOM transform only when the rounded position changes. |
| Mobile Touch Gesture Model | Touch: tap=select, one-finger-drag=orbit, long-press=context menu; no rect selection or _objDragging |
| Long-Press Context Menu | `showContextMenu()` with Grab/Dup/Rename/Delete; items filtered by entity type |
| Long-Press for Non-Draggable Entities | CF/MeasureLine/Annotated* early-return must set long-press timer for touch BEFORE returning; without this CF "Link to..." is unreachable on mobile |
| _confirmFastenFrame Type Guard | Check `instanceof CoordinateFrame` for both IDs and call `_service._updateWorldPoses()` before `fastenFrame()`; type mismatch must show "Select a coordinate frame as source and target" not "frame pose unknown" |
| Measure Point Placement | Confirm in `_onPointerUp`; hold shows live snap feedback before release |
| Stack Mode | Ray origin must be Z=10000; runs in both `_grab.active` and `_objDragging` paths |
| Viewport-Aware Z-Index | Toast `bottom: 96px` on mobile (above 86px toolbar); status in `_infoEl` on mobile |
| Edge-Anchored Panels Must Coordinate Occupancy | Persistent fixed panels on one screen edge must offset past each other, never layer. The gizmo offset is owned solely by `AppController._updateGizmoOffset()` driven by one uiStore subscription over all right-edge occupancy — never call `setRightOffset()` from toggle handlers. |
| Mobile Header Overflow | Export/Import collapse into the `⋯` menu on mobile; `_headerStatusEl` uses `visibility:hidden` (not `display:none`) to stay a flex spacer; the header has `overflow:hidden`, so escaping popups (mode dropdown) mount on `document.body` with `position:fixed` + `getBoundingClientRect()` positioning. |
| Disabled-as-Quest Chrome Controls (ADR-065 Phase 3) | A disable-able chrome control derives `disabled` AND its rendered reason from ONE `ChromeGates` predicate return (`{enabled, reason}` — locked ⇒ non-empty reason, open ⇒ null, machine-tested); tapping the stylized locked state (dashed border, `cursor:help`) surfaces the reason as a toast — never set the native `disabled` attribute (it swallows the tap and the reason), never a silent no-op (#11/#25). Tier A chrome motion (press spring / breathing glow / entry slide) derives via pure `ChromeMath` — motion keys only, colours stay component-owned so affordance motion cannot fake a Tier F judgment; reduced motion degrades to the static colour/glow cue (#30). |

---

## 3.5 Server-Side Async (Node.js BFF)

Detail: `docs/code_contracts/server_async.md`

| Rule | Core Takeaway |
|------|--------------|
| All DB calls must be awaited | Every `sceneStore.*()` call must be `await`ed; async wrappers must use try/catch |
| PRAGMA journal_mode | Run WAL pragma as standalone `db.execute()` before any `db.batch()` |
| Unguarded JSON.parse | Wrap `JSON.parse(row.data)` in try/catch in sceneStore.js |
| occt-import-js Geometry Structure | Extract geometry from `mesh.attributes`, not `mesh.faces` |
| Camera Far Clip for Imports | Call `fitCameraToSphere()` after every `updateGeometryBuffers`; never hard-code `camera.far` |
| setIndex Requires BufferAttribute | Wrap raw `Uint32Array` (e.g. from `base64ToU32`) in `new THREE.BufferAttribute(arr, 1)` before `setIndex()`; plain JS arrays are handled automatically |
| Grasp Contract Is Derived, Never Defined | The `vendor/grasp-contract` submodule schema is the source of truth; the BFF only derives types/validators and never defines/extends the contract. `POST /api/grasp/search` validates + version-checks both ends (inbound mismatch → 400, upstream drift/non-conformance → 502, unreachable → 503) and delegates solving upstream. `pnpm test:contract` detects drift. |
| BffClient Grasp/Layout Walkthrough Surfaces the Contract-Error Envelope (ADR-054) | `compileLayout`/`graspSearch` go through `_postContract`, which surfaces the BFF's `{error, details}` envelope + HTTP status (400 contract mismatch / 502 upstream drift / 503 upstream unreachable) instead of collapsing into `BffUnavailableError` (reserved for a genuine BFF network failure) — PHILOSOPHY #11. The caller must NOT set `contractVersion` (the BFF stamps it). |
| /api/scenes Validates the Graph Skeleton, Accepts Geometry as an Opaque Blob; the BFF Stamps the Version (ADR-064 Phase 3) | `POST`/`PUT /api/scenes` validate `data` against `schema/scene-1.3.schema.json` before persisting; a non-conforming skeleton is rejected with 400 (`{error, details}`) instead of becoming DB garbage (#11/#29). Designed on the Blender datablock split: the graph skeleton (`objects[]` typed fields + refs, `links[]` edges, `transformGraph` nodes/edges) is closed (`additionalProperties:false`); bulk geometry (`ImportedMesh` base64 buffers) is an opaque blob leaf, envelope-validated only (`operationGraph` likewise open — ADR-017 Phase B). `server/src/scenes/sceneContract.js` *reads* the in-repo schema (source of truth is here) and *derives* the validator + `SCENE_VERSION` from `version.const` — never restated; the BFF stamps the version if absent (non-breaking, like grasp's `contractVersion`). The MEANING contract (ref resolution across objects/links/graph) is the domain reconstruct path's job, not the schema's. `/api/ws` (geometry session) and `/api/import` (STEP) are DECLARED out of rigor scope with a Phase-C deadline in their route headers — the WS scene-write path (`updateScene` direct) bypasses this REST validation on purpose, as its own uncontracted wire (#29 (b)). BFF `test:contract` is glob (`test/**/*.test.js`) so a new contract test is auto-run (Phase 1 silent-failure removal). |

---

## 4. Memory Management (Three.js)

Detail: `docs/code_contracts/memory_management.md`

| Rule | Core Takeaway |
|------|--------------|
| WsChannel Listener Cleanup | Save bound handlers as instance properties; `removeEventListener` all 4 in `close()` |
| Read-Only Entity Early-Return | Show `showToast('Imported geometry is read-only')` before any early-return that blocks op |
| Object Lifecycle Symmetry | Every `scene.add()` must have matching `scene.remove()` + `.dispose()` in `dispose()` |
| _clearScene Emit Order | Emit `objectRemoved` for each object BEFORE replacing `this._model` |
| SceneSerializer Entity Coverage | Every domain entity must be explicitly handled (or commented) in `serializeScene()` |
| SceneImporter KNOWN_TYPES Coverage | `KNOWN_TYPES` in `SceneImporter` must include every type in `serializeScene()` + `_deserializeEntities`; add in same commit as domain entity |
| ImportedMesh Serialization | Base64 buffers; restore `cuboid.position` from `offset` BEFORE calling `initCorners()` |
| THREE.Mesh Requires Valid Geometry | Never pass `null` to `new THREE.Mesh(null, mat)` — use `new THREE.BufferGeometry()` as placeholder; `updateMorphTargets()` throws on null geometry |
| Zone Rim Ring Must Use Polygon Geometry | Use `ShapeGeometry` + polygon hole for the rim ring; `RingGeometry` always produces a circle regardless of Zone shape |
| BoxHelper Forbidden for World-Space Baked Geometry | `THREE.BoxHelper` computes AABB; use `THREE.LineSegments`+`EdgesGeometry` for `MeshView` selection highlight so it matches the solid's actual orientation after rotation |
| Annotation View Materials Must Use depthTest: true | `depthTest: false` makes annotations render over all Solid objects; use `depthTest: true` + `depthWrite: false` for transparent materials + `polygonOffset` for flat ground-plane meshes |
| Ground Markers Must Not Straddle Z=0 | Marker bodies rest ON the plane (geometry translated, not z-centered); flat overlay parts (ring/crosshairs) lift to the body's top via `_applyLift()` (scales with `_viewScale`). The disc uses `transparent:true, opacity:1` so renderOrder — not the opaque/transparent split — layers it over region fills, whose slope-scaled `polygonOffset` otherwise pulls them in front at glancing angles (purple Anchor × green Zone → blue-grey ghost) |
